# Backend Core Roadmap Restructure Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rework the backend core and architecture handbook into a concrete, roadmap-aligned backend learning path with sharper explanations, practical examples, review rubrics, and verification gates.

**Architecture:** `public/handbook/backend-engineering-handbook.html` is the source document for the backend overview. `scripts/generate-engineering-bundles.mjs` assembles `public/handbook/engineering-backend-core-handbook.html` from backend, architecture, and auth/security source documents. `npm run generate:handbook` converts the public handbook HTML into React document modules under `src/handbook/documents`.

**Tech Stack:** Static HTML handbook, Node.js generation scripts, Node test runner, Vite build.

---

## Source Basis

Use the external roadmap as the ordering reference, not as copy text:

- Backend fundamentals and request lifecycle
- HTTP, REST, API contracts, error handling, pagination, versioning
- Database, SQL, transaction, indexes, isolation, migration
- Language/framework depth, especially Java/Spring/JPA in this handbook
- Auth, authorization, OWASP, secrets, threat modeling
- Cache, async processing, messaging, idempotency
- Testing strategy and test pyramid
- Docker, deployment, CI/CD, runtime operations
- Observability, SLO, logging, monitoring, incident response
- Architecture, system design, portfolio, interview readiness

## File Map

- Modify: `public/handbook/backend-engineering-handbook.html`
  - Reorder and rewrite backend source chapters.
  - Replace vague conceptual paragraphs with concrete failure modes, decision tables, examples, and checklists.
  - Add roadmap, practice gates, and portfolio/interview evidence sections.
- Modify: `scripts/generate-engineering-bundles.mjs`
  - Make the `engineering-backend-core` bundle intro and optional inserted roadmap sections reflect the new learning path.
- Modify: `scripts/handbook-html.test.mjs`
  - Add semantic assertions for the new structure and evidence markers.
- Generated: `public/handbook/engineering-backend-core-handbook.html`
  - Regenerated by `node scripts/generate-engineering-bundles.mjs`.
- Generated: `src/handbook/documents/backend.ts`
  - Regenerated by `npm run generate:handbook` if the generator emits it from `backend-engineering-handbook.html`.
- Generated: `src/handbook/documents/engineering-backend-core.ts`
  - Regenerated by `npm run generate:handbook`.

---

### Task 1: Replace the Backend Source Navigation with a Roadmap Flow

**Files:**
- Modify: `public/handbook/backend-engineering-handbook.html`

- [ ] **Step 1: Update the source document hero and nav**

Change the document framing from a broad “핸드북” to a concrete roadmap:

```html
<div class="nav-title">백엔드 로드맵 · 핵심과 아키텍처</div>
```

Use this chapter order:

```text
BE-00 백엔드 학습 지도
BE-01 요청 생명주기와 서버 책임
BE-02 HTTP·REST·API 계약
BE-03 Java·Spring 실행 모델
BE-04 관계형 DB·SQL·트랜잭션
BE-05 ORM·JPA와 영속성 경계
BE-06 동시성·락·멱등성
BE-07 인증·인가·보안 기본값
BE-08 캐시·Redis·정합성
BE-09 비동기·메시징·이벤트
BE-10 테스트 전략
BE-11 배포·런타임·운영 준비
BE-12 관측가능성·장애 대응
BE-13 아키텍처와 시스템 설계
BE-14 포트폴리오·면접 증거화
BE-15 용어집
```

Expected: the nav reads as a learning path instead of a topic dump.

- [ ] **Step 2: Keep useful existing material but move it**

Move current content into the new order:

```text
Current BE-01/BE-01B -> New BE-00/BE-01
Current BE-02 and BE-14 -> New BE-02
Current BE-03 -> New BE-04
Current BE-04 -> New BE-05
Current BE-05/BE-17 -> New BE-06
Current BE-10 plus security source cross-reference -> New BE-07
Current BE-06 -> New BE-08
Current BE-07 -> New BE-09
Current BE-11 -> New BE-10
Current BE-09/BE-15/BE-18 -> New BE-11/BE-12
Current BE-12/architecture source cross-reference -> New BE-13
Current BE-19 -> New BE-14
Current BE-13 -> New BE-15
```

Expected: duplicate “심화” chapters are merged back into their parent concepts.

### Task 2: Add a Concrete Learning Map and Readiness Gates

**Files:**
- Modify: `public/handbook/backend-engineering-handbook.html`

- [ ] **Step 1: Add `BE-00 백엔드 학습 지도`**

Add a section with this exact marker:

```html
<span class="sc-label">BACKEND ROADMAP 2026</span>
```

The section must include a table with these columns:

```text
단계
배울 것
실무에서 증명할 것
부실한 이해의 신호
```

Rows:

```text
서버 기본기 | 요청 생명주기, HTTP, 상태코드 | API 하나를 계약으로 설명 | 컨트롤러 메서드만 설명
데이터 계층 | SQL, 트랜잭션, 인덱스, 격리수준 | EXPLAIN과 트랜잭션 경계 설명 | ORM 메서드 이름만 설명
보안 기본값 | 인증, 인가, OWASP, secret | 객체 단위 권한과 threat model 제시 | 로그인만 보안으로 봄
분산 기본기 | 캐시, 큐, 재시도, 멱등성 | 중복·부분 실패 방어 설계 | Redis/Kafka 이름만 나열
품질과 운영 | 테스트, 배포, 로그, 메트릭, SLO | 배포 전후 검증과 장애 추적 | 로컬 성공만 증거로 제시
아키텍처 | 경계, 모듈, MSA, 시스템 설계 | 제약 기반 트레이드오프 설명 | 패턴명을 정답처럼 말함
```

- [ ] **Step 2: Add `BACKEND READINESS GATE`**

Add a serial-card that defines completion criteria:

```text
BACKEND READINESS GATE
1. API contract: request, response, error, authZ, idempotency를 문서화한다
2. Data contract: table, constraint, transaction boundary, migration order를 설명한다
3. Failure contract: timeout, retry, duplicate request, rollback, compensation을 정한다
4. Test evidence: unit, integration, API, contract, regression 중 필요한 증거를 남긴다
5. Ops evidence: log key, metric, traceId, alert, runbook을 배포 전에 확인한다
6. Interview evidence: 선택한 기술의 비용과 대안을 90초 안에 말한다
```

Expected: the opening gives the reader a concrete destination before concepts begin.

### Task 3: Rewrite API and HTTP as a Contract Chapter

**Files:**
- Modify: `public/handbook/backend-engineering-handbook.html`

- [ ] **Step 1: Merge existing API material into `BE-02 HTTP·REST·API 계약`**

Keep method idempotency, status code boundaries, authN/authZ, versioning, pagination, and `API CONTRACT MINI`.

- [ ] **Step 2: Add practical API examples**

Add these exact markers:

```text
ORDER API CONTRACT EXAMPLE
API ERROR CONTRACT
API COMPATIBILITY RULES
```

The examples must cover:

```text
POST /orders with Idempotency-Key
201 Created, 202 Accepted, 400, 401, 403, 404, 409, 422, 429, 500
problem+json-style error shape
offset vs cursor pagination
field addition vs field deletion vs enum addition
```

Expected: API design is no longer only conceptual; it gives a reusable template.

### Task 4: Deepen Database, Transaction, and JPA Sections

**Files:**
- Modify: `public/handbook/backend-engineering-handbook.html`

- [ ] **Step 1: Rewrite `BE-04 관계형 DB·SQL·트랜잭션`**

Preserve ACID, B-tree index, isolation level, and N+1 explanations. Add these markers:

```text
TRANSACTION BOUNDARY DECISION TABLE
EXPLAIN ANALYZE CHECKLIST
MIGRATION SAFETY RULES
LOCK TRIAGE QUESTION
```

The chapter must explain:

```text
what must commit together
read committed vs repeatable read practical impact
unique/check/foreign key constraints as final guardrails
covering index, composite index leading column, cardinality
expand-contract migration
idle in transaction
lock wait vs connection wait
```

- [ ] **Step 2: Rewrite `BE-05 ORM·JPA와 영속성 경계`**

Add these markers:

```text
JPA SQL VISIBILITY RULE
PERSISTENCE CONTEXT FAILURE MODES
JPA FETCH PLAN CHECKLIST
```

The chapter must cover:

```text
flush timing
dirty checking
lazy loading and LazyInitializationException
OSIV tradeoff
fetch join vs entity graph vs batch size
repository method naming limits
when to use QueryDSL/native SQL
```

Expected: the DB/JPA chapters teach mechanisms and failure modes, not only definitions.

### Task 5: Make Security, Cache, and Messaging Operational

**Files:**
- Modify: `public/handbook/backend-engineering-handbook.html`

- [ ] **Step 1: Rewrite `BE-07 인증·인가·보안 기본값`**

Add these markers:

```text
AUTHN AUTHZ DECISION MATRIX
OBJECT AUTHORIZATION CHECKLIST
SECURITY REVIEW MINI RUBRIC
```

The chapter must cover:

```text
session vs JWT tradeoff
access token + refresh token rotation
RBAC vs ABAC
tenant isolation
IDOR negative tests
SQL injection and parameterized queries
XSS/CSRF boundary
secret storage and log redaction
file upload and webhook risk pointers
```

- [ ] **Step 2: Rewrite `BE-08 캐시·Redis·정합성`**

Add these markers:

```text
CACHE DECISION TABLE
CACHE INVALIDATION PLAYBOOK
REDIS PRACTICAL USES
```

The chapter must cover:

```text
cache-aside
TTL and jitter
stampede
hot key
negative caching
distributed lock cautions
rate limit and session store use cases
```

- [ ] **Step 3: Rewrite `BE-09 비동기·메시징·이벤트`**

Add these markers:

```text
ASYNC DECISION TABLE
OUTBOX INBOX FLOW
DLQ RECOVERY PLAYBOOK
```

The chapter must cover:

```text
queue vs event stream
at-least-once and idempotent consumer
outbox/inbox
consumer lag
poison message
retry with backoff and jitter
event schema compatibility
```

Expected: these chapters explain how failures are handled in production.

### Task 6: Reframe Testing, Deployment, Observability, and Architecture

**Files:**
- Modify: `public/handbook/backend-engineering-handbook.html`

- [ ] **Step 1: Rewrite `BE-10 테스트 전략`**

Keep `BACKEND TEST PLAN`, but add:

```text
BACKEND TEST PYRAMID DECISION
CONCURRENCY REGRESSION TEST
CONTRACT TEST BOUNDARY
```

The chapter must say which risk each test type proves:

```text
domain unit proves business invariant
repository integration proves DB mapping and constraints
API slice proves validation, status code, authZ, error shape
contract test proves consumer/provider compatibility
E2E smoke proves critical path wiring
regression test proves a fixed bug stays fixed
```

- [ ] **Step 2: Rewrite `BE-11 배포·런타임·운영 준비`**

Add:

```text
DEPLOYMENT READINESS CHECKLIST
BACKWARD COMPATIBLE RELEASE FLOW
RUNTIME CONFIG CHECKLIST
```

The chapter must cover:

```text
Docker image and environment variables
health checks
graceful shutdown
connection pool sizing
feature flags
expand-contract DB migration
rollback limits after schema changes
```

- [ ] **Step 3: Rewrite `BE-12 관측가능성·장애 대응`**

Keep `OPERATIONS READINESS CHECKLIST`, but add:

```text
OBSERVABILITY SIGNAL MATRIX
SLOW REQUEST TRIAGE
INCIDENT FIRST 15 MINUTES
```

The chapter must cover:

```text
structured logs
traceId/correlation id
RED and USE metrics
p95/p99
high-cardinality labels
SLO burn rate
runbook
postmortem evidence
```

- [ ] **Step 4: Rewrite `BE-13 아키텍처와 시스템 설계`**

Keep current design question flow, decision matrix, order case study, and design rubric. Add:

```text
ARCHITECTURE CHOICE MATRIX
MODULAR MONOLITH FIRST
SERVICE BOUNDARY CHECKLIST
```

The chapter must connect to the architecture source document:

```text
layered architecture
hexagonal architecture
DDD bounded context
modular monolith vs MSA
sync vs async service communication
CQRS/event sourcing as advanced choices
resilience patterns
```

Expected: quality and architecture become evidence-based disciplines, not slogans.

### Task 7: Add Portfolio and Interview Evidence Section

**Files:**
- Modify: `public/handbook/backend-engineering-handbook.html`

- [ ] **Step 1: Rewrite `BE-14 포트폴리오·면접 증거화`**

Add exact markers:

```text
BACKEND PORTFOLIO PROJECT LADDER
BACKEND EVIDENCE PACKET
INTERVIEW ANSWER RUBRIC
```

The section must include project ladder rows:

```text
Level 1 CRUD API | validation, error contract, authZ, tests
Level 2 DB-heavy API | transaction, index, migration, EXPLAIN
Level 3 async workflow | outbox, idempotent consumer, DLQ, retry
Level 4 production-ready service | Docker, CI/CD, health, metrics, logs, runbook
Level 5 system design case | scale estimate, tradeoff, failure mode, architecture boundary
```

Evidence packet:

```text
OpenAPI or API docs
ERD and migration notes
transaction boundary notes
EXPLAIN before/after
test report
dashboard screenshot or metric list
incident/runbook drill
ADR for one tradeoff
```

- [ ] **Step 2: Preserve and improve interview answer formulas**

Use this answer structure:

```text
정의 1문장 → 왜 문제가 되는가 → 실패 모드 → 방어선 → 트레이드오프 → 확인 증거
```

Expected: the reader knows what to build and what evidence to collect after reading.

### Task 8: Update the Integrated Backend Bundle

**Files:**
- Modify: `scripts/generate-engineering-bundles.mjs`
- Generated: `public/handbook/engineering-backend-core-handbook.html`

- [ ] **Step 1: Update `engineering-backend-core.indexDescription`**

Set the backend bundle intro to:

```text
백엔드는 요청 생명주기 → API 계약 → Java/Spring 실행 모델 → 트랜잭션과 데이터 모델 → ORM/JPA → 동시성·멱등성 → 인증·인가·보안 → 캐시·비동기 → 테스트 → 배포·운영 → 관측가능성 → 아키텍처 경계 → 포트폴리오·면접 증거 순서로 읽습니다. 이 통합 문서는 기술명을 나열하지 않고, 제품 서버를 설계·리뷰·운영할 때 필요한 판단 기준과 증거물을 먼저 세웁니다.
```

- [ ] **Step 2: Add backend-specific bundle inserted section**

Add `afterIndexNav` and `afterIndexHtml` for the backend bundle with:

```text
BACKEND ROADMAP OVERVIEW
REQUEST TO OPERATIONS MAP
BACKEND STUDY OUTPUTS
```

Expected: the integrated page has a curated front section before source documents.

- [ ] **Step 3: Regenerate bundle**

Run:

```bash
node scripts/generate-engineering-bundles.mjs
```

Expected:

```text
Wrote public/handbook/engineering-backend-core-handbook.html
```

### Task 9: Regenerate React Documents and Add Semantic Tests

**Files:**
- Modify: `scripts/handbook-html.test.mjs`
- Generated: `src/handbook/documents/backend.ts`
- Generated: `src/handbook/documents/engineering-backend-core.ts`

- [ ] **Step 1: Add tests for the new roadmap structure**

Add assertions to the existing backend core test for:

```js
assert.match(source, /BACKEND ROADMAP 2026/);
assert.match(source, /BACKEND READINESS GATE/);
assert.match(source, /ORDER API CONTRACT EXAMPLE/);
assert.match(source, /TRANSACTION BOUNDARY DECISION TABLE/);
assert.match(source, /JPA FETCH PLAN CHECKLIST/);
assert.match(source, /AUTHN AUTHZ DECISION MATRIX/);
assert.match(source, /CACHE DECISION TABLE/);
assert.match(source, /OUTBOX INBOX FLOW/);
assert.match(source, /BACKEND TEST PYRAMID DECISION/);
assert.match(source, /DEPLOYMENT READINESS CHECKLIST/);
assert.match(source, /OBSERVABILITY SIGNAL MATRIX/);
assert.match(source, /ARCHITECTURE CHOICE MATRIX/);
assert.match(source, /BACKEND PORTFOLIO PROJECT LADDER/);
assert.match(source, /BACKEND EVIDENCE PACKET/);
```

- [ ] **Step 2: Add anti-regression tests for vagueness**

Add checks that the backend source includes practical evidence terms:

```js
assert.match(source, /EXPLAIN ANALYZE/);
assert.match(source, /expand-contract/);
assert.match(source, /traceId/);
assert.match(source, /consumer lag/);
assert.match(source, /idempotency key/);
assert.match(source, /negative test|negative tests/);
assert.match(source, /ADR/);
```

- [ ] **Step 3: Regenerate React document modules**

Run:

```bash
npm run generate:handbook
```

Expected:

```text
Generated handbook documents
```

### Task 10: Verify Build and Review Output

**Files:**
- Verify: `public/handbook/backend-engineering-handbook.html`
- Verify: `public/handbook/engineering-backend-core-handbook.html`
- Verify: `src/handbook/documents/backend.ts`
- Verify: `src/handbook/documents/engineering-backend-core.ts`

- [ ] **Step 1: Run tests**

Run:

```bash
npm test
```

Expected: all Node tests pass.

- [ ] **Step 2: Run production build**

Run:

```bash
npm run build
```

Expected: Vite production build completes.

- [ ] **Step 3: Inspect generated document markers**

Run:

```bash
rg -n "BACKEND ROADMAP 2026|BACKEND PORTFOLIO PROJECT LADDER|OBSERVABILITY SIGNAL MATRIX|ARCHITECTURE CHOICE MATRIX" public/handbook src/handbook/documents
```

Expected: markers appear in both public HTML and generated React document modules.

- [ ] **Step 4: Final review checklist**

Confirm:

```text
The backend source no longer reads as 19 loosely related chapters.
Each major roadmap topic has a failure mode, decision table or checklist, and evidence output.
The integrated backend bundle starts with a curated roadmap before source material.
Tests protect the new structure from accidental removal.
```

---

## Self-Review

Spec coverage:

- External roadmap ordering is reflected in Tasks 1-2.
- API, DB, framework, security, cache, async, tests, deployment, observability, architecture, portfolio, and interview preparation are each assigned to a concrete task.
- Build and generated documents are covered in Tasks 8-10.

Placeholder scan:

- No `TBD`, `TODO`, or vague “add appropriate content” instructions remain.
- Every required new section has exact markers and topic coverage.

Risk notes:

- This is a content-heavy rewrite. The safest implementation path is to edit the source HTML first, regenerate bundles/documents second, and only then update tests.
- Generated files should not be hand-edited except to verify output.
