import type { HandbookDocumentContent } from "../types";

const document: HandbookDocumentContent = {
  navHtml: "<div class=\"nav-brand\">CHEAT SHEET</div><div class=\"nav-title\">Docker</div>\n  <a href=\"#ch1\"><span class=\"code\">DK-CS-01</span>개요</a>\n  <a href=\"#ch2\"><span class=\"code\">DK-CS-02</span>핵심 명령/개념</a>\n  <a href=\"#ch3\"><span class=\"code\">DK-CS-03</span>판단 기준</a>\n  <a href=\"#ch4\"><span class=\"code\">DK-CS-04</span>자주 나는 실수</a>\n  <a href=\"#ch5\"><span class=\"code\">DK-CS-05</span>면접/리뷰용 한 줄 답변</a>\n  <a href=\"#ch6\"><span class=\"code\">DK-CS-06</span>Red Flags</a>\n  <a href=\"#ch7\"><span class=\"code\">DK-CS-07</span>Review Gate</a>\n  <a href=\"#ch8\"><span class=\"code\">DK-CS-08</span>Failure Playbook</a>",
  mainHtml: "<header class=\"hero\">\n    <div class=\"hero-serial\"><span>DOC : CHEAT-DOCKER-V1</span><span>SCOPE : IMAGE · CONTAINER · NETWORK · VOLUME · COMPOSE</span><span>LANG : KO</span></div>\n    <h1>Docker<br>Cheat Sheet</h1>\n    <p class=\"hero-sub\">Docker 실행 오류를 이미지, 컨테이너, 포트, 볼륨, 네트워크, Compose 기준으로 빠르게 좁히는 참조입니다.</p>\n    <div class=\"hero-meta\">COMPILED : 2026-06 · FORMAT : QUICK REFERENCE</div>\n  </header>\n  <section id=\"ch1\">\n    <div class=\"ch-head\"><span class=\"ch-code\">DK-CS-01</span><h2>개요</h2></div>\n    <p class=\"lede\">이미지는 불변 실행 패키지이고 컨테이너는 그 이미지를 실행한 프로세스입니다. 런타임 문제는 build와 run 단계를 분리해 봅니다.</p>\n    <table>\n      <tr><th>상황</th><th>먼저 확인할 것</th><th>실패 신호</th></tr>\n      <tr><td>빌드 실패</td><td>Dockerfile 순서, build context, cache, secret 노출</td><td>로컬에는 있는데 이미지 안에는 파일이 없음</td></tr>\n      <tr><td>실행 실패</td><td>entrypoint, env, port, volume, working directory</td><td>컨테이너가 즉시 종료되거나 health check 실패</td></tr>\n      <tr><td>연결 실패</td><td>container network, service name, port mapping</td><td>컨테이너 내부에서는 되는데 host에서는 안 됨</td></tr>\n      <tr><td>데이터 사라짐</td><td>container layer와 volume/bind mount 구분</td><td>컨테이너 재생성 후 DB/업로드 파일 유실</td></tr>\n      <tr><td>이미지 과대</td><td>base image, build artifact, package cache</td><td>배포와 scan 시간이 길고 취약점 수가 많음</td></tr>\n    </table>\n  </section>\n  <section id=\"ch2\">\n    <div class=\"ch-head\"><span class=\"ch-code\">DK-CS-02</span><h2>핵심 명령/개념</h2></div>\n    <table>\n      <tr><th>명령</th><th>압축 설명</th><th>해석 포인트</th></tr>\n      <tr><td><code>docker build -t app .</code></td><td>현재 context로 이미지 빌드</td><td><code>.</code> 아래 모든 context가 전송되므로 <code>.dockerignore</code> 중요</td></tr>\n      <tr><td><code>docker run --rm -p 3000:3000 app</code></td><td>컨테이너 실행 후 종료 시 삭제</td><td>앞이 host port, 뒤가 container port</td></tr>\n      <tr><td><code>docker ps -a</code></td><td>실행/종료 컨테이너 확인</td><td>STATUS와 PORTS를 먼저 확인</td></tr>\n      <tr><td><code>docker logs -f name</code></td><td>컨테이너 로그 추적</td><td>앱 로그가 stdout/stderr로 나오는지 확인</td></tr>\n      <tr><td><code>docker exec -it name sh</code></td><td>실행 중 컨테이너 내부 진입</td><td>이미지에 shell이 없을 수 있음</td></tr>\n      <tr><td><code>docker inspect name</code></td><td>컨테이너 설정과 network, mount 확인</td><td>env, IP, mount source/target 검증</td></tr>\n      <tr><td><code>docker compose up</code></td><td>여러 서비스를 선언 파일 기준으로 실행</td><td>service name이 compose network의 DNS 이름</td></tr>\n    </table>\n  </section>\n  <section id=\"ch3\">\n    <div class=\"ch-head\"><span class=\"ch-code\">DK-CS-03</span><h2>판단 기준</h2></div>\n    <table>\n      <tr><th>선택</th><th>기준</th><th>주의점</th></tr>\n      <tr><td>bind mount</td><td>로컬 개발에서 파일 변경을 즉시 반영해야 함</td><td>host OS 파일 권한과 성능 영향을 받음</td></tr>\n      <tr><td>named volume</td><td>DB 데이터처럼 컨테이너 교체 뒤에도 보존해야 함</td><td>삭제 전 volume 목록과 소유자를 확인</td></tr>\n      <tr><td>multi-stage build</td><td>빌드 도구와 런타임 산출물을 분리해야 함</td><td>빌드 stage의 secret/cache가 최종 이미지에 남지 않게 함</td></tr>\n      <tr><td>slim image</td><td>운영 이미지 크기와 취약점 노출을 줄여야 함</td><td>디버깅 도구가 없을 수 있으니 로그/health를 강화</td></tr>\n      <tr><td>compose</td><td>앱, DB, 캐시 등 로컬 다중 서비스를 함께 띄워야 함</td><td>운영 오케스트레이션 대체물이 아님</td></tr>\n      <tr><td>healthcheck</td><td>프로세스 생존보다 서비스 준비 상태가 중요함</td><td>DB 연결, migration 완료, dependency 준비 상태 고려</td></tr>\n    </table>\n  </section>\n  <section id=\"ch4\">\n    <div class=\"ch-head\"><span class=\"ch-code\">DK-CS-04</span><h2>자주 나는 실수</h2></div>\n    <ul>\n      <li><code>.dockerignore</code> 없이 <code>node_modules</code>와 <code>.git</code>을 build context에 포함한다. 빌드가 느려지고 secret 노출 위험이 생긴다.</li>\n      <li>Dockerfile에 secret을 굽거나 build arg로 남긴다. image history와 registry에 흔적이 남을 수 있다.</li>\n      <li>container port와 host port 방향을 헷갈린다. <code>-p 8080:3000</code>은 host 8080에서 container 3000으로 보낸다.</li>\n      <li>컨테이너 내부 파일을 영속 데이터로 믿는다. 컨테이너 삭제와 재생성은 layer 상태를 버린다.</li>\n      <li><code>latest</code> 태그를 production 배포 식별자로 사용한다. 정확한 rollback과 감사가 어려워진다.</li>\n      <li>Compose에서 <code>localhost</code>로 다른 서비스를 찾는다. 컨테이너 간 통신은 service name을 사용한다.</li>\n    </ul>\n  </section>\n  <section id=\"ch5\">\n    <div class=\"ch-head\"><span class=\"ch-code\">DK-CS-05</span><h2>면접/리뷰용 한 줄 답변</h2></div>\n    <div class=\"glossary\">\n      <div class=\"g-row\"><div class=\"g-term\"><b>이미지와 컨테이너</b><span>model</span></div><div class=\"g-def\">이미지는 불변 스냅샷이고 컨테이너는 그 이미지를 실행한 격리된 프로세스입니다.</div></div>\n      <div class=\"g-row\"><div class=\"g-term\"><b>레이어 캐시</b><span>cache</span></div><div class=\"g-def\">Dockerfile은 자주 바뀌지 않는 의존성 레이어를 앞에 두고 자주 바뀌는 소스를 뒤에 둬야 빌드가 빨라집니다.</div></div>\n      <div class=\"g-row\"><div class=\"g-term\"><b>볼륨</b><span>volume</span></div><div class=\"g-def\">컨테이너 파일시스템은 휘발성이므로 보존해야 하는 상태는 volume이나 외부 스토리지로 빼야 합니다.</div></div>\n      <div class=\"g-row\"><div class=\"g-term\"><b>포트 매핑</b><span>network</span></div><div class=\"g-def\">포트 매핑은 host와 container의 포트를 연결하는 설정이고, 컨테이너 내부 서비스는 container port에 bind되어 있어야 합니다.</div></div>\n      <div class=\"g-row\"><div class=\"g-term\"><b>Compose</b><span>local stack</span></div><div class=\"g-def\">Compose는 로컬 다중 서비스 개발을 재현 가능하게 만드는 도구이며 service name 기반 네트워크를 제공합니다.</div></div>\n    </div>\n  </section>\n  <section id=\"ch6\">\n    <div class=\"ch-head\"><span class=\"ch-code\">DK-CS-06</span><h2>Red Flags</h2></div>\n    <table>\n      <tr><th>신호</th><th>왜 위험한가</th><th>즉시 확인</th></tr>\n      <tr><td>root user로 운영 실행</td><td>컨테이너 탈출/파일 권한 사고 시 피해가 커짐</td><td><code>USER</code>, runtime uid/gid, writable path</td></tr>\n      <tr><td>secret을 Dockerfile/ARG에 기록</td><td>image history와 registry에 남을 수 있음</td><td><code>docker history</code>, secret scanning</td></tr>\n      <tr><td><code>latest</code>로 배포</td><td>정확한 rollback과 감사가 불가능</td><td>image digest, immutable tag</td></tr>\n      <tr><td>PID 1 signal 처리 없음</td><td>SIGTERM을 무시해 graceful shutdown 실패</td><td>entrypoint, tini, app shutdown log</td></tr>\n      <tr><td>healthcheck가 프로세스 생존만 확인</td><td>준비 안 된 앱에 traffic이 들어감</td><td>readiness endpoint, dependency 상태</td></tr>\n    </table>\n  </section>\n  <section id=\"ch7\">\n    <div class=\"ch-head\"><span class=\"ch-code\">DK-CS-07</span><h2>Review Gate</h2></div>\n    <table>\n      <tr><th>변경</th><th>merge/deploy 차단 기준</th><th>필수 증거</th></tr>\n      <tr><td>Dockerfile</td><td>secret 포함, root 실행, cache 비효율, 불필요한 build context</td><td><code>.dockerignore</code>, image scan, layer size</td></tr>\n      <tr><td>Base image 변경</td><td>OS/arch, libc, CVE, runtime 호환성 미검증</td><td>scan 결과, smoke test, image digest</td></tr>\n      <tr><td>Compose 변경</td><td>volume 삭제 위험, service name/DNS 오해, port 충돌</td><td><code>docker compose config</code>, volume 목록</td></tr>\n      <tr><td>Runtime env</td><td>secret baked-in, build-time/runtime env 혼동</td><td>env 주입 방식, redacted config dump</td></tr>\n      <tr><td>Healthcheck</td><td>startup/readiness/liveness 구분 없음</td><td>실패 시 재시작/traffic drain 동작</td></tr>\n    </table>\n  </section>\n  <section id=\"ch8\">\n    <div class=\"ch-head\"><span class=\"ch-code\">DK-CS-08</span><h2>Failure Playbook</h2></div>\n    <div class=\"serial-card\"><span class=\"sc-label\">CONTAINER WON'T START</span>1. <code>docker ps -a</code>로 exit code 확인<br>2. <code>docker logs name</code>로 앱 오류 확인<br>3. <code>docker inspect name</code>로 env/mount/network 확인<br>4. <code>docker run --rm -it --entrypoint sh image</code>로 이미지 내부 확인<br>5. port mapping과 app bind address 확인<br>6. volume 권한과 working directory 확인</div>\n    <div class=\"callout warn\"><span class=\"co-label\">운영 원칙</span><p>컨테이너는 VM이 아니라 제한된 프로세스입니다. 상태는 이미지가 아니라 volume, external storage, env, network, orchestration 설정에 분산되어 있으므로 한 곳만 보고 원인을 단정하지 않습니다.</p></div>\n  </section>",
};

export default document;
