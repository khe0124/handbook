import type { HandbookDocumentContent } from "../types";

const document: HandbookDocumentContent = {
  navHtml: "<div class=\"nav-brand\">OPERATIONS · DNS</div>\n  <div class=\"nav-title\">DNS·TLS·도메인 운영</div>\n  <a href=\"#chindex\"><span class=\"code\">INDEX</span>문서 구조</a>\n  <a href=\"#ch1\"><span class=\"code\">DNS-00</span>DNS record와 TTL</a>\n  <a href=\"#ch2\"><span class=\"code\">DNS-01</span>split-horizon DNS</a>\n  <a href=\"#ch3\"><span class=\"code\">DNS-02</span>TLS 인증서</a>\n  <a href=\"#ch4\"><span class=\"code\">DNS-03</span>도메인 변경 절차</a>\n  <a href=\"#ch5\"><span class=\"code\">MODEL</span>개념 모델</a>\n  <a href=\"#ch6\"><span class=\"code\">CHECK</span>실무 체크리스트</a>\n  <a href=\"#ch7\"><span class=\"code\">PLAYBOOK</span>실무 플레이북</a>\n  <a href=\"#ch8\"><span class=\"code\">PRACTICE</span>출력 판독 훈련</a>\n  <a href=\"#ch9\"><span class=\"code\">TERM</span>용어사전</a>\n  <a href=\"#ch10\"><span class=\"code\">CASE</span>장애/운영 시나리오</a>\n  <a href=\"#ch11\"><span class=\"code\">RISK</span>자주 틀리는 판단</a>\n  <a href=\"#ch12\"><span class=\"code\">Q&A</span>면접 답변 템플릿</a>",
  mainHtml: "<header class=\"hero\">\n  <div class=\"hero-serial\">\n    <span>DOC : OPERATIONS-DNS-TLS</span>\n    <span>SCOPE : OPERATIONS · RELIABILITY · INCIDENT RESPONSE</span>\n    <span>LANG : KO</span>\n  </div>\n  <h1>DNS·TLS·도메인 운영</h1>\n  <p class=\"hero-sub\">이름 해석, 인증서, 전파, 만료를 변경 관리 대상으로 다룬다</p>\n  <div class=\"hero-meta\">FORMAT : 개념 → 증거 → 플레이북 → 장애 시나리오 → 면접 답변 · UPDATED 2026-06</div>\n</header>\n\n<section id=\"chindex\">\n<div class=\"ch-head\"><span class=\"ch-code\">INDEX</span><h2>문서 구조</h2></div>\n<p class=\"lede\">이 문서는 개념 요약이 아니라 운영 판단 연습용 플레이북입니다. 각 항목은 “무엇을 아는가”보다 “장애 때 어떤 증거를 보고 어떤 가설을 버리는가”를 기준으로 읽습니다.</p>\n<ul><li>핵심 개념</li><li>주제별 운영 증거</li><li>실무 플레이북</li><li>용어사전</li><li>Incident packet</li><li>주제별 답변 구조</li></ul>\n\n</section>\n\n      <section id=\"ch1\">\n        <div class=\"ch-head\"><span class=\"ch-code\">DNS-00</span><h2>DNS record와 TTL</h2></div>\n        <p class=\"lede\">A, AAAA, CNAME, ALIAS, TXT, CAA record의 목적을 구분한다. TTL은 장애 대응 속도와 resolver 부하 사이의 선택이다. 이전 작업 전 TTL을 낮추고, 작업 후 안정화되면 다시 올린다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>DNS record와 TTL이 변경/복구 시간에 맞게 설계되었는가?</td><td>A/AAAA/CNAME/ALIAS/TXT/CAA, authoritative answer, TTL, resolver cache</td><td>record 목적과 TTL 전파 지연을 작업 전부터 고려해야 한다.</td></tr>\n          <tr><td>DNS record와 TTL를 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>dig NS, dig +trace, dig CAA, provider DNS change history</td><td>권한 서버 응답과 public resolver cache를 분리한다.</td></tr>\n          <tr><td>DNS record와 TTL 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>이전 IP가 일부 resolver에 남아 사용자만 간헐 실패</td><td>TTL 사전 조정, 이전 record rollback, 전파 모니터링을 실행한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch2\">\n        <div class=\"ch-head\"><span class=\"ch-code\">DNS-01</span><h2>split-horizon DNS</h2></div>\n        <p class=\"lede\">같은 이름이 내부와 외부에서 다른 IP로 해석되는 split-horizon DNS는 VPN, 사내망, Private Endpoint에서 자주 필요하다. 장애 분석 시 사용자가 어느 resolver를 쓰는지 먼저 확인한다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>split-horizon DNS가 사용자 위치별로 의도한 IP를 반환하는가?</td><td>internal resolver, public resolver, VPN resolver, Private DNS zone, conditional forwarder</td><td>같은 이름이 내부/외부에서 다르게 해석되는 이유가 문서화되어야 한다.</td></tr>\n          <tr><td>split-horizon DNS를 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>dig @internal, dig @public, resolver rule, Private DNS zone link</td><td>잘못된 resolver를 쓰면 사설 서비스가 public endpoint로 흐를 수 있다.</td></tr>\n          <tr><td>split-horizon DNS 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>VPN 사용자만 내부 API 접속 실패</td><td>resolver rule과 Private DNS link를 수정하고 양쪽 결과를 기록한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch3\">\n        <div class=\"ch-head\"><span class=\"ch-code\">DNS-02</span><h2>TLS 인증서</h2></div>\n        <p class=\"lede\">TLS 운영은 인증서 발급만이 아니다. SAN, wildcard 범위, SNI, intermediate chain, OCSP, 자동 갱신, 만료 알림, CAA 정책을 함께 본다. 인증서 교체는 rollback 가능한 변경으로 관리한다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>TLS 인증서가 발급뿐 아니라 실제 handshake에서 유효한가?</td><td>SAN, wildcard scope, SNI, issuer, expiry, intermediate chain, OCSP, renewal alert</td><td>인증서 발급 성공과 사용자 handshake 성공은 다르다.</td></tr>\n          <tr><td>TLS 인증서를 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>openssl s_client -showcerts, curl -v, certificate inventory, renewal job log</td><td>SNI와 chain 누락, SAN mismatch를 반드시 확인한다.</td></tr>\n          <tr><td>TLS 인증서 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>일부 브라우저에서 certificate chain 오류</td><td>중간 인증서 연결, CDN binding, renewal alert를 복구한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch4\">\n        <div class=\"ch-head\"><span class=\"ch-code\">DNS-03</span><h2>도메인 변경 절차</h2></div>\n        <p class=\"lede\">도메인 변경에는 사전 TTL 조정, record diff, 인증서 준비, CDN/WAF 연결, smoke test, 전파 모니터링, rollback record가 필요하다. dig, curl -v, openssl s_client 결과를 변경 티켓에 남긴다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>도메인 변경이 rollback 가능한 변경으로 관리되는가?</td><td>TTL pre-change, record diff, certificate binding, CDN/WAF config, smoke test, rollback record</td><td>새 경로와 이전 경로가 전파 기간 동안 모두 안전해야 한다.</td></tr>\n          <tr><td>도메인 변경 절차를 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>dig before/after, curl --resolve, openssl s_client, CDN request log</td><td>전파 중 일부 사용자는 이전/새 endpoint를 모두 볼 수 있다.</td></tr>\n          <tr><td>도메인 변경 절차 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>새 CDN 전환 후 일부 지역 403</td><td>이전 record 복구 또는 CDN rule rollback을 실행하고 전파 상태를 추적한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch5\">\n        <div class=\"ch-head\"><span class=\"ch-code\">MODEL</span><h2>개념 모델</h2></div>\n        <p class=\"lede\">DNS·TLS·도메인 운영은 단일 지식 항목이 아니라 운영 판단의 일부다. 아래 표처럼 개념을 책임, 확인 지표, 실패 신호로 나누면 면접 답변과 실제 장애 대응이 모두 선명해진다.</p>\n        <table>\n          <tr><th>구성 요소</th><th>운영 책임</th><th>확인할 증거</th></tr>\n          <tr><td>DNS-00 · DNS record와 TTL</td><td>A, AAAA, CNAME, ALIAS, TXT, CAA record의 목적을 구분한다. TTL은 장애 대응 속도와 resolver 부하 사이의 선택이다. 이전 작업 전 TTL을 낮추고, 작업 후 안정화되면 다시 올린다.</td><td>A/AAAA/CNAME/ALIAS/TXT/CAA, authoritative answer, TTL, resolver cache</td></tr>\n          <tr><td>DNS-01 · split-horizon DNS</td><td>같은 이름이 내부와 외부에서 다른 IP로 해석되는 split-horizon DNS는 VPN, 사내망, Private Endpoint에서 자주 필요하다. 장애 분석 시 사용자가 어느 resolver를 쓰는지 먼저 확인한다.</td><td>internal resolver, public resolver, VPN resolver, Private DNS zone, conditional forwarder</td></tr>\n          <tr><td>DNS-02 · TLS 인증서</td><td>TLS 운영은 인증서 발급만이 아니다. SAN, wildcard 범위, SNI, intermediate chain, OCSP, 자동 갱신, 만료 알림, CAA 정책을 함께 본다. 인증서 교체는 rollback 가능한 변경으로 관리한다.</td><td>SAN, wildcard scope, SNI, issuer, expiry, intermediate chain, OCSP, renewal alert</td></tr>\n          <tr><td>DNS-03 · 도메인 변경 절차</td><td>도메인 변경에는 사전 TTL 조정, record diff, 인증서 준비, CDN/WAF 연결, smoke test, 전파 모니터링, rollback record가 필요하다. dig, curl -v, openssl s_client 결과를 변경 티켓에 남긴다.</td><td>TTL pre-change, record diff, certificate binding, CDN/WAF config, smoke test, rollback record</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch6\">\n        <div class=\"ch-head\"><span class=\"ch-code\">CHECK</span><h2>실무 체크리스트</h2></div>\n        <p class=\"lede\">DNS·TLS·도메인 운영 체크리스트는 DNS record와 TTL, split-horizon DNS, TLS 인증서, 도메인 변경 절차의 변경 위험을 사전에 드러내기 위한 검토표다. 각 항목은 리뷰, 배포 승인, 운영 인수에서 같은 증거로 다시 확인되어야 한다.</p>\n        <ul>\n          <li>record 종류와 TTL 변경 계획을 사전에 작성한다.</li>\n          <li>CAA, SAN, wildcard, SNI, intermediate chain을 확인한다.</li>\n          <li>인증서 만료 알림과 자동 갱신 실패 알림을 둔다.</li>\n          <li>도메인 변경 전후 dig, curl, openssl 결과를 기록한다.</li>\n        </ul>\n        <table>\n          <tr><th>작업 단계</th><th>필수 산출물</th><th>차단 기준</th></tr>\n          <tr><td>설계</td><td>목적, 영향 범위, 비목표, owner</td><td>영향 범위가 사용자·시스템·비용 기준으로 설명되지 않음</td></tr>\n          <tr><td>변경</td><td>diff, 승인 기록, 검증 명령</td><td>production 변경과 staging 검증의 차이가 설명되지 않음</td></tr>\n          <tr><td>복구</td><td>rollback 또는 forward fix, 담당자, 예상 시간</td><td>실패 시 되돌릴 수 없는데 사전 승인과 공지가 없음</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch7\">\n        <div class=\"ch-head\"><span class=\"ch-code\">PLAYBOOK</span><h2>실무 플레이북</h2></div>\n        <p class=\"lede\">DNS·TLS·도메인 운영 플레이북은 <strong>DNS TLS CHANGE PLAYBOOK</strong> 기준으로 증상, 범위, 최근 변경, 계층별 증거를 연결한다. 목표는 명령어 암기가 아니라 어떤 가설을 버리고 어떤 완화책을 실행할지 결정하는 것이다.</p>\n        <div class=\"serial-card\">\n          <span class=\"sc-label\">DNS TLS CHANGE PLAYBOOK</span>\n          symptom → scope → recent change → evidence<br>\n          → mitigation → verification → runbook diff\n        </div>\n        <table>\n          <tr><th>운영 표면</th><th>볼 증거</th><th>판단 기준</th></tr>\n          <tr><td>DNS record</td><td>record type, authoritative answer, TTL, resolver cache, CAA</td><td>전파 지연과 잘못된 권한 서버를 구분한다.</td></tr>\n          <tr><td>TLS chain</td><td>SAN, issuer, expiry, intermediate, SNI, OCSP</td><td>인증서 발급 성공과 사용자 handshake 성공은 다르다.</td></tr>\n          <tr><td>Rollback</td><td>previous record, previous certificate, CDN/WAF binding, smoke result</td><td>도메인 변경도 되돌릴 수 있는 변경으로 다룬다.</td></tr>\n        </table>\n\n        <table>\n          <tr><th>확인 단계</th><th>명령·확인 위치</th><th>해석 기준</th></tr>\n          <tr><td>권한 DNS</td><td>dig +trace app.example.com, dig NS example.com, dig CAA example.com</td><td>권한 서버와 resolver cache를 분리한다.</td></tr>\n          <tr><td>인증서</td><td>openssl s_client -servername app.example.com -connect app.example.com:443 -showcerts</td><td>SNI와 chain 누락을 확인한다.</td></tr>\n          <tr><td>사용자 경로</td><td>curl -v --resolve app.example.com:443:IP https://app.example.com/health</td><td>DNS 변경 전후 edge와 origin을 분리 테스트한다.</td></tr>\n        </table>\n        <h3>MARKER: artifact-ready — operations command evidence packet</h3>\n        <pre class=\"snippet-card\"><code>ops_command_evidence_packet:\n  document: operations-dns-tls-handbook.html\n  playbook: DNS TLS CHANGE PLAYBOOK\n  hypothesis: \"DNS record 계층이 사용자 영향과 연결되어 있는가\"\n  command_or_probe: \"dig +trace app.example.com, dig NS example.com, dig CAA example.com\"\n  normal_signal: \"권한 서버와 resolver cache를 분리한다.\"\n  abnormal_signal: \"일부 고객만 인증서 오류 또는 구버전 IP 접속\"\n  mitigation_then_permanent_fix:\n    mitigation: \"resolver, TTL, chain, CDN binding을 먼저 본다.\"\n    permanent_fix: \"갱신 실패가 장애가 되기 전에 알림이 와야 한다.\"\n  evidence_to_attach:\n    - timestamp\n    - environment\n    - owner\n    - trace_or_request_id\n    - dashboard_snapshot\n    - runbook_diff</code></pre>\n\n      </section>\n\n      <section id=\"ch8\">\n        <div class=\"ch-head\"><span class=\"ch-code\">PRACTICE</span><h2>출력 판독 훈련</h2></div>\n        <p class=\"lede\">DNS·TLS·도메인 운영은 개념을 아는 것보다 실제 출력에서 정상과 비정상을 구분하는 훈련이 중요하다. 아래 lab은 명령, 출력, 판단, 완화, 영구 수정을 한 번에 연결한다.</p>\n        <div class=\"practice-lab\">\n          <span class=\"sc-label\">PRACTICE LAB 01 · TLS chain/SNI 판독</span>\n          <p>모바일 브라우저 일부에서만 인증서 오류가 발생한다.</p>\n          <table>\n            <tr><th>Command / Query</th><th>목적</th></tr>\n            <tr><td><pre><code>openssl s_client -servername app.example.com -connect app.example.com:443 -showcerts</code></pre></td><td>SNI, SAN, 만료일, intermediate chain 누락을 확인한다.</td></tr>\n          </table>\n          <table>\n            <tr><th>정상 출력</th><th>비정상 출력</th><th>판단 훈련</th></tr>\n            <tr><td><pre><code>Verify return code: 0 (ok)\nsubjectAltName = DNS:app.example.com\nnotAfter=Sep 20 12:00:00 2026 GMT</code></pre></td><td><pre><code>Verify return code: 21 (unable to verify the first certificate)\nsubjectAltName = DNS:old.example.com</code></pre></td><td>서버 연결은 되지만 인증서 체인 또는 SNI binding이 잘못된 상태다.</td></tr>\n          </table>\n          <table>\n            <tr><th>즉시 완화</th><th>영구 수정</th><th>검증 기준</th></tr>\n            <tr><td>CDN/LB 인증서 binding rollback 또는 올바른 certificate chain 재배포.</td><td>certificate inventory, renewal alert, SNI smoke test를 배포 gate에 추가한다.</td><td>openssl verify ok, curl -v handshake 성공, 브라우저 오류 재현 중단.</td></tr>\n          </table>\n        </div>\n      </section>\n\n      <section id=\"ch9\">\n        <div class=\"ch-head\"><span class=\"ch-code\">TERM</span><h2>용어사전</h2></div>\n        <p class=\"lede\">DNS·TLS·도메인 운영에서 반복적으로 등장하는 운영 용어를 정의, 증거, 오해, 관련 항목 기준으로 정리한다. 장애 중에는 용어 자체보다 해당 용어가 어떤 로그, 지표, 설정, 책임 경계와 연결되는지를 확인한다.</p>\n        <table>\n          <tr><th>용어</th><th>정의</th><th>운영 증거</th><th>자주 하는 오해</th><th>관련 항목</th></tr>\n          <tr><td>TTL</td><td>resolver가 DNS 응답을 캐시하는 시간.</td><td>authoritative answer, resolver cache, provider change history</td><td>작업 직전에 낮추면 즉시 전파된다.</td><td>record rollback, resolver, propagation</td></tr>\n          <tr><td>CNAME/ALIAS</td><td>이름을 다른 이름이나 provider alias 대상으로 연결하는 record.</td><td>CNAME/ALIAS chain, dig result</td><td>모든 DNS provider의 ALIAS 동작은 같다.</td><td>A record, flattening, CDN binding</td></tr>\n          <tr><td>CAA</td><td>어떤 CA가 인증서를 발급할 수 있는지 제한하는 DNS record.</td><td>dig CAA, certificate issuance log</td><td>CAA는 인증서 운영과 무관하다.</td><td>TLS, issuer, renewal</td></tr>\n          <tr><td>split-horizon DNS</td><td>같은 이름이 내부와 외부에서 다른 IP로 해석되는 구성.</td><td>internal resolver, public resolver, Private DNS zone</td><td>한 resolver 결과가 모든 사용자 결과다.</td><td>VPN resolver, conditional forwarder, Private Endpoint</td></tr>\n          <tr><td>SNI</td><td>TLS handshake에서 접속하려는 hostname을 서버에 전달하는 값.</td><td>openssl s_client -servername, SAN mismatch</td><td>IP만 맞으면 인증서도 맞는다.</td><td>SAN, certificate binding, handshake</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch10\">\n        <div class=\"ch-head\"><span class=\"ch-code\">CASE</span><h2>장애/운영 시나리오</h2></div>\n        <p class=\"lede\">도메인을 새 CDN으로 이전한다. 24~48시간 전에 TTL을 낮추고, 새 CDN에 인증서를 미리 붙인다. CAA와 DNS record diff를 리뷰하고, 일부 트래픽으로 smoke test를 수행한 뒤 문제가 생기면 이전 record로 rollback한다.</p>\n        <div class=\"callout\">\n          <span class=\"co-label\">Triage frame</span>\n          <p>DNS·TLS·도메인 운영 장애는 증상 → 영향 범위 → 최근 변경 → DNS record 증거 → 완화책 → 복구 검증 → 재발 방지 순서로 기록한다. 이 흐름이 있어야 담당자가 바뀌어도 같은 결론에 도달한다.</p>\n        </div>\n        <table>\n          <tr><th>처음 10분</th><th>다음 30분</th><th>종료 조건</th></tr>\n          <tr><td>record type, authoritative answer, TTL, resolver cache, CAA 기준으로 영향 범위를 고정한다.</td><td>dig +trace app.example.com, dig NS example.com, dig CAA example.com 항목에서 증거를 모아 완화책을 실행한다.</td><td>갱신 실패가 장애가 되기 전에 알림이 와야 한다.</td></tr>\n        </table>\n        <table>\n          <tr><th>Incident packet</th><th>기록할 내용</th><th>판정 기준</th></tr>\n          <tr><td>증상</td><td>일부 고객만 인증서 오류 또는 구버전 IP 접속</td><td>resolver, TTL, chain, CDN binding을 먼저 본다.</td></tr>\n          <tr><td>완화</td><td>이전 record 복구, 인증서 재연결, CDN cache purge</td><td>전파 중에는 양쪽 endpoint가 모두 안전해야 한다.</td></tr>\n          <tr><td>사후</td><td>TTL 사전 변경, CAA/renewal alert, certificate inventory 추가</td><td>갱신 실패가 장애가 되기 전에 알림이 와야 한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch11\">\n        <div class=\"ch-head\"><span class=\"ch-code\">RISK</span><h2>자주 틀리는 판단</h2></div>\n        <p class=\"lede\">DNS·TLS·도메인 운영에서 자주 틀리는 판단은 DNS record와 TTL와 도메인 변경 절차의 책임 경계를 흐리는 데서 시작된다. 아래 신호가 보이면 설계, runbook, 답변 구조를 다시 점검한다.</p>\n        <ul>\n          <li>TTL을 작업 직전에 낮추면 이미 늦다.</li>\n          <li>인증서가 발급됐다는 이유로 SNI와 chain 검증을 생략한다.</li>\n          <li>내부 resolver와 외부 resolver 결과 차이를 놓친다.</li>\n          <li>CNAME flattening이나 ALIAS 동작을 provider마다 같다고 가정한다.</li>\n        </ul>\n      </section>\n\n      <section id=\"ch12\">\n        <div class=\"ch-head\"><span class=\"ch-code\">Q&A</span><h2>면접 답변 템플릿</h2></div>\n        <p class=\"lede\">DNS와 TLS 운영은 record 변경, 전파, 인증서, 갱신, rollback을 함께 다루는 변경 관리라고 답합니다. dig와 openssl로 실제 사용자가 보는 이름 해석과 인증서 체인을 검증해야 합니다.</p>\n        <div class=\"serial-card\">\n          <span class=\"sc-label\">OPERATIONS ANSWER FRAME</span>\n          1. DNS 변경은 전파와 rollback을 포함한 변경 관리라고 설명한다.<br>\n          2. TLS는 SAN/SNI/chain/만료/자동갱신까지 운영한다.<br>\n          3. dig, curl, openssl 결과를 변경 티켓에 남긴다.<br>\n        </div>\n\n      </section>\n<footer>\nOPERATIONS HANDBOOK · 2026-06<br>\nPRINCIPLE : 운영 지식은 경로, 증거, 복구 절차로 검증한다.\n</footer>",
};

export default document;
