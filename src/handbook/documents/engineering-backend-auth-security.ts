import type { HandbookDocumentContent } from "../types";

const document: HandbookDocumentContent = {
  navHtml: "<div class=\"nav-brand\">ENGINEERING · BACKEND SECURITY</div>\n  <div class=\"nav-title\">백엔드 인증·보안 · 세션 토큰 권한</div>\n  <a href=\"#bundle-index\"><span class=\"code\">INDEX</span>통합 문서</a>\n  <a href=\"#doc-auth\"><span class=\"code\">DOC</span>인증과 보안</a>\n  <a href=\"#doc-auth\"><span class=\"code\">SRC</span>인증과 보안</a>\n<a href=\"#auth-auth-ch1\"><span class=\"code\">SEC-01</span>보안 지도와 신뢰 경계</a>\n  <a href=\"#auth-auth-ch2\"><span class=\"code\">SEC-02</span>인증 vs 인가</a>\n  <a href=\"#auth-auth-ch3\"><span class=\"code\">SEC-03</span>세션·쿠키·CSRF·CORS</a>\n  <a href=\"#auth-auth-ch4\"><span class=\"code\">SEC-04</span>JWT와 토큰 생명주기</a>\n  <a href=\"#auth-auth-ch5\"><span class=\"code\">SEC-05</span>OAuth2·OIDC</a>\n  <a href=\"#auth-auth-ch6\"><span class=\"code\">SEC-06</span>비밀번호·MFA·계정 복구</a>\n  <a href=\"#auth-auth-ch7\"><span class=\"code\">SEC-07</span>RBAC·ABAC·ReBAC</a>\n  <a href=\"#auth-auth-ch8\"><span class=\"code\">SEC-08</span>Tenant Isolation</a>\n  <a href=\"#auth-auth-ch9\"><span class=\"code\">SEC-09</span>OWASP 서버 취약점</a>\n  <a href=\"#auth-auth-ch10\"><span class=\"code\">SEC-10</span>Secret·Key·Audit</a>\n  <a href=\"#auth-auth-ch11\"><span class=\"code\">SEC-11</span>Security Review Rubric</a>\n  <a href=\"#auth-auth-ch12\"><span class=\"code\">SEC-12</span>Security Tabletop Drills</a>\n  <a href=\"#auth-auth-ch13\"><span class=\"code\">SEC-13</span>SECURITY INCIDENT PACKET</a>\n  <a href=\"#backend-security-review-packet\"><span class=\"code\">PACKET</span>보안 리뷰 패킷</a>",
  mainHtml: "<header class=\"hero\">\n  <div class=\"hero-serial\">\n    <span>DOC : ENGINEERING-BACKEND-AUTH-SECURITY</span>\n    <span>SCOPE : AUTHENTICATION · AUTHORIZATION · OWASP · THREAT MODEL · INCIDENT</span>\n    <span>LANG : KO</span>\n  </div>\n  <h1>백엔드 인증·보안</h1>\n  <p class=\"hero-sub\">인증과 보안 문서를 세션, 토큰, OAuth/OIDC, 권한, 테넌트 격리, 위협 모델, 운영 대응 중심으로 독립 분리했습니다.</p>\n  <div class=\"hero-meta\">CURATED HANDBOOK PATH · SOURCES : 1 DOCUMENTS · UPDATED 2026-06</div>\n</header>\n\n<section id=\"bundle-index\">\n<div class=\"ch-head\"><span class=\"ch-code\">INDEX</span><h2>통합 문서</h2></div>\n<p class=\"lede\">백엔드 보안은 로그인 기능을 붙이는 일이 아니라 신뢰 경계, identity proof, session lifecycle, authorization decision, tenant isolation, audit evidence, incident response를 설계하는 일입니다. 이 문서는 세션/JWT/OAuth/OIDC와 RBAC/ABAC/ReBAC, CSRF/CORS/cookie, OWASP 취약점, secret 관리, 보안 리뷰와 사고 대응을 하나의 운영 흐름으로 읽습니다.</p>\n<ul><li>인증과 보안</li></ul>\n</section>\n\n<section id=\"doc-auth\">\n<div class=\"ch-head\"><span class=\"ch-code\">SOURCE</span><h2>인증과 보안</h2></div>\n</section>\n\n<!-- ============ AUTH CH 1 ============ -->\n<section id=\"auth-auth-ch1\">\n<div class=\"ch-head\"><span class=\"ch-code\">SEC-01</span><h2>보안 지도와 신뢰 경계</h2></div>\n<p class=\"lede\">보안 설계의 시작은 \"로그인 기능\"이 아니라 <strong>무엇을 누구에게서 보호하는가</strong>를 그리는 일입니다. 좋은 백엔드 보안 문서는 자산, 행위자, 진입점, 신뢰 경계, 실패 시 영향, 탐지 증거를 한 장에서 설명할 수 있어야 합니다.</p>\n<h3><span class=\"h3-tag\">1.1</span>BACKEND AUTH SECURITY MAP</h3>\n<table>\n<tr><th>축</th><th>질문</th><th>실무 산출물</th></tr>\n<tr><td>Trust boundary</td><td>데이터가 낮은 신뢰 수준에서 높은 신뢰 수준으로 들어오는 지점은 어디인가?</td><td>browser/API, webhook/API, API/worker, worker/DB 경계와 검증 책임</td></tr>\n<tr><td>Identity proof</td><td>요청 주체가 누구인지 어떤 증거로 믿는가?</td><td>password, MFA, session id, signed token, OIDC id token, service credential</td></tr>\n<tr><td>Session lifecycle</td><td>로그인 후 권한 상태를 어떻게 유지, 갱신, 폐기하는가?</td><td>cookie flags, access/refresh pair, rotation, revocation, device inventory</td></tr>\n<tr><td>Authorization decision</td><td>이 actor가 이 resource에 이 action을 해도 되는가?</td><td>policy engine, ownership check, tenant predicate, negative authorization tests</td></tr>\n<tr><td>Tenant isolation</td><td>한 고객의 데이터가 다른 고객에게 보이지 않는 불변식은 어디서 강제되는가?</td><td>tenant key, scoped query, RLS/view, cache key namespace, export guard</td></tr>\n<tr><td>Audit evidence</td><td>나중에 누가 무엇을 했는지 증명할 수 있는가?</td><td>append-only audit log, request id, actor/resource/action/context, alert query</td></tr>\n<tr><td>Incident readiness</td><td>침해가 의심될 때 무엇을 끊고 무엇을 보존하고 누구에게 알릴 것인가?</td><td>rotation runbook, kill switch, evidence packet, owner, customer/legal handoff</td></tr>\n</table>\n<div class=\"callout\">\n<span class=\"co-label\">인터뷰 답변 프레임</span>\n<p>\"인증 보안을 어떻게 설계하겠습니까?\"라는 질문에는 구현체부터 말하지 않습니다. 먼저 보호 자산과 신뢰 경계를 정의하고, 신원 증명(authN), 세션 생명주기, 요청별 인가(authZ), 테넌트 격리, 감사 로그, 사고 대응을 순서대로 설명합니다. 마지막에 세션 쿠키인지 JWT인지 같은 구현 선택의 트레이드오프를 붙입니다.</p>\n</div>\n<h3><span class=\"h3-tag\">1.2</span>신뢰 경계 체크리스트</h3>\n<ul>\n<li>클라이언트가 보낸 <code>userId</code>, <code>role</code>, <code>tenantId</code>, 가격, 상태값은 서버에서 다시 계산하거나 무시한다.</li>\n<li>웹훅, 큐 메시지, 캐시 값도 공격자가 간접 주입할 수 있으면 외부 입력으로 본다.</li>\n<li>관리자 콘솔은 \"내부라서 안전\"이 아니라 권한이 높아 더 엄격한 재인증과 감사가 필요하다.</li>\n<li>실패 응답은 공격자에게 계정 존재 여부, 테넌트 존재 여부, 내부 쿼리 구조를 과하게 알려주지 않는다.</li>\n</ul>\n</section>\n\n<!-- ============ AUTH CH 2 ============ -->\n<section id=\"auth-auth-ch2\">\n<div class=\"ch-head\"><span class=\"ch-code\">SEC-02</span><h2>인증 vs 인가</h2></div>\n<p class=\"lede\">인증(authentication, authN)은 \"요청 주체가 누구인지\"를 세우는 절차이고, 인가(authorization, authZ)는 \"그 주체가 특정 자원에 특정 행동을 해도 되는지\"를 매 요청 판정하는 절차입니다. 로그인 성공은 권한 판정의 시작이지 끝이 아닙니다.</p>\n<table>\n<tr><th>구분</th><th>인증 authN</th><th>인가 authZ</th></tr>\n<tr><td>핵심 질문</td><td>너 누구냐?</td><td>이 actor가 이 resource에 이 action을 해도 되나?</td></tr>\n<tr><td>입력 증거</td><td>비밀번호, MFA, session id, token signature, OIDC id token</td><td>actor id, tenant id, role, attribute, relation, resource owner, action</td></tr>\n<tr><td>실행 시점</td><td>로그인, 토큰 검증, 세션 복원</td><td>모든 읽기/쓰기/다운로드/export/background job</td></tr>\n<tr><td>실패 응답</td><td><code>401</code>: 신원 없음 또는 자격증명 불가</td><td><code>403</code>: 신원은 알지만 권한 없음. 존재 숨김이 필요하면 <code>404</code></td></tr>\n<tr><td>대표 사고</td><td>세션 탈취, 약한 비밀번호, MFA 우회, JWT 검증 누락</td><td>IDOR, tenant escape, 관리자 권한 남용, role만 보고 ownership 누락</td></tr>\n</table>\n<h3><span class=\"h3-tag\">2.1</span>Negative tests</h3>\n<table>\n<tr><th>테스트</th><th>공격 입력</th><th>기대 결과</th></tr>\n<tr><td>인증 없음</td><td>토큰 없이 <code>GET /me</code>, <code>POST /orders</code></td><td><code>401</code>, 동일한 에러 형식, 민감 정보 없음</td></tr>\n<tr><td>타인 객체</td><td>A 사용자 토큰으로 B 사용자의 <code>/orders/{id}</code></td><td><code>403</code> 또는 존재 은닉 <code>404</code>, audit event 기록</td></tr>\n<tr><td>본문 권한 변조</td><td><code>{\"role\":\"admin\",\"ownerId\":\"other\"}</code></td><td>서버가 본문 권한 필드를 무시하거나 검증 실패</td></tr>\n<tr><td>권한 변경 직후</td><td>관리자가 권한을 낮춘 뒤 기존 access token으로 중요 API 호출</td><td>짧은 만료, 권한 버전 재검증, 중요 작업 step-up 중 하나로 차단</td></tr>\n</table>\n<div class=\"callout warn\">\n<span class=\"co-label\">면접에서 자주 틀리는 지점</span>\n<p><code>401 Unauthorized</code>라는 이름 때문에 인가 실패로 오해하기 쉽지만, 실무적으로는 인증 실패입니다. 인가 실패는 보통 <code>403 Forbidden</code>입니다. 다만 \"리소스 존재 자체를 숨기는 것\"이 보안 요구사항이면 인증된 사용자에게도 <code>404</code>를 반환할 수 있습니다.</p>\n</div>\n</section>\n\n<!-- ============ AUTH CH 3 ============ -->\n<section id=\"auth-auth-ch3\">\n<div class=\"ch-head\"><span class=\"ch-code\">SEC-03</span><h2>세션·쿠키·CSRF·CORS</h2></div>\n<p class=\"lede\">브라우저 기반 인증의 핵심은 쿠키가 자동 첨부된다는 사실입니다. 이 자동성은 사용자 경험에는 좋지만 CSRF 위험을 만들고, CORS 설정과 섞이면 \"어떤 origin이 credential 요청을 보낼 수 있는가\"라는 운영 정책이 됩니다.</p>\n<h3><span class=\"h3-tag\">3.1</span>COOKIE SESSION CORS MATRIX</h3>\n<table>\n<tr><th>설계</th><th>동작</th><th>주요 위험</th><th>필수 통제</th></tr>\n<tr><td>서버 세션 + HttpOnly 쿠키</td><td>브라우저가 <code>sid</code>를 자동 전송, 서버가 Redis/DB에서 세션 조회</td><td>CSRF, session fixation, 세션 저장소 장애</td><td><code>HttpOnly</code>, <code>Secure</code>, <code>SameSite</code>, CSRF token, 로그인 후 sid 재발급</td></tr>\n<tr><td>JWT access token + Authorization 헤더</td><td>클라이언트 코드가 명시적으로 헤더 첨부</td><td>XSS로 token 탈취, refresh 설계 부실</td><td>짧은 access 수명, refresh rotation, CSP, token 저장 최소화</td></tr>\n<tr><td>Cross-site cookie</td><td>다른 site의 프론트엔드가 API cookie를 포함해 요청</td><td>느슨한 CORS, CSRF, third-party cookie 제한</td><td><code>SameSite=None; Secure</code>, 정확한 origin allowlist, CSRF token</td></tr>\n<tr><td>CORS credential 요청</td><td><code>fetch(..., {credentials:\"include\"})</code> 사용</td><td><code>*</code> origin과 credential 혼용, origin reflection</td><td><code>Access-Control-Allow-Origin</code>은 정확한 origin, <code>Vary: Origin</code>, preflight 검증</td></tr>\n</table>\n<pre class=\"snippet-card\"><code>Set-Cookie: sid=...; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=1800</code></pre>\n<table>\n<tr><th>쿠키 속성</th><th>실무 기준</th><th>검토 질문</th></tr>\n<tr><td><code>HttpOnly</code></td><td>JS에서 세션 식별자를 읽지 못하게 한다.</td><td>XSS가 발생해도 세션 쿠키를 직접 훔칠 수 없는가?</td></tr>\n<tr><td><code>Secure</code></td><td>HTTPS에서만 전송한다.</td><td>개발/운영 설정 차이로 운영 쿠키가 HTTP에 노출되지 않는가?</td></tr>\n<tr><td><code>SameSite=Lax</code></td><td>일반 웹 앱 기본값으로 적절하다.</td><td>외부 결제/SSO redirect 흐름이 깨지지 않는가?</td></tr>\n<tr><td><code>SameSite=None</code></td><td>cross-site embedding/SPA가 필요할 때만 쓴다.</td><td>반드시 <code>Secure</code>와 CSRF 방어가 함께 있는가?</td></tr>\n</table>\n<h3><span class=\"h3-tag\">3.2</span>CSRF와 CORS를 분리해서 설명하기</h3>\n<p>CORS는 브라우저가 다른 origin의 응답을 읽어도 되는지 제한하는 브라우저 정책입니다. CSRF는 브라우저가 쿠키를 자동 첨부해 상태 변경 요청을 보내게 만드는 공격입니다. CORS를 잘 설정해도 단순 form POST CSRF는 남을 수 있고, CSRF token이 있어도 CORS를 느슨하게 열면 credential API가 노출될 수 있습니다.</p>\n<div class=\"callout risk\">\n<span class=\"co-label\">Negative CORS cases</span>\n<p><code>Origin</code> 헤더를 그대로 <code>Access-Control-Allow-Origin</code>에 반사하지 마세요. credential 요청에서 <code>*</code>를 허용하려 하지 말고, 운영 origin allowlist와 <code>Vary: Origin</code>을 사용합니다. <code>localhost</code>, staging, preview URL을 운영에 넓게 남기는 것도 실제 사고 경로가 됩니다.</p>\n</div>\n</section>\n\n<!-- ============ AUTH CH 4 ============ -->\n<section id=\"auth-auth-ch4\">\n<div class=\"ch-head\"><span class=\"ch-code\">SEC-04</span><h2>JWT와 토큰 생명주기</h2></div>\n<p class=\"lede\">JWT는 서명된 claim 묶음입니다. payload는 Base64URL 인코딩이지 암호화가 아니며, stateless 검증은 확장성에는 유리하지만 만료 전 회수와 권한 변경 반영을 어렵게 만듭니다.</p>\n<h3><span class=\"h3-tag\">4.1</span>TOKEN LIFECYCLE RUNBOOK</h3>\n<table>\n<tr><th>단계</th><th>실무 기준</th><th>실패 모드</th></tr>\n<tr><td>발급</td><td><code>iss</code>, <code>sub</code>, <code>aud</code>, <code>exp</code>, <code>iat</code>, <code>jti</code>, tenant/permission version을 명확히 담는다.</td><td>audience 검증 누락으로 다른 서비스 토큰 재사용</td></tr>\n<tr><td>저장</td><td>access token은 짧게, refresh token은 HttpOnly 쿠키 또는 보안 저장소에 두고 서버에서 추적한다.</td><td>localStorage access token이 XSS로 유출</td></tr>\n<tr><td>검증</td><td>서버가 알고리즘과 key id를 allowlist로 고정하고 만료, issuer, audience, signature를 검증한다.</td><td><code>alg:none</code>, HS/RS confusion, 만료 무시</td></tr>\n<tr><td>갱신</td><td>refresh rotation을 사용하고 이전 refresh 재사용을 침해 신호로 본다.</td><td>탈취된 refresh token이 장기간 조용히 재사용</td></tr>\n<tr><td>폐기</td><td>로그아웃, 비밀번호 변경, MFA 변경, 의심 기기 차단 시 refresh family를 폐기한다.</td><td>사용자는 로그아웃했다고 믿지만 공격자 refresh는 살아 있음</td></tr>\n<tr><td>키 교체</td><td><code>kid</code> 기반 JWKS, overlap window, 이전 키 폐기 계획을 둔다.</td><td>갑작스러운 rotation으로 정상 사용자 전부 로그아웃</td></tr>\n</table>\n<pre class=\"snippet-card\"><code>{\n  \"iss\": \"https://auth.example.com\",\n  \"aud\": \"api.example.com\",\n  \"sub\": \"user_123\",\n  \"tenant_id\": \"tenant_456\",\n  \"auth_time\": 1782892800,\n  \"perm_ver\": 17,\n  \"jti\": \"tok_abc\",\n  \"exp\": 1782893700\n}</code></pre>\n<h3><span class=\"h3-tag\">4.2</span>Access/refresh 비교</h3>\n<table>\n<tr><th>토큰</th><th>권장 수명</th><th>서버 상태</th><th>권장 방어</th></tr>\n<tr><td>Access token</td><td>5분에서 15분 등 짧게</td><td>가능하면 stateless</td><td>signature/audience/issuer 검증, 민감 작업은 최신 권한 재조회</td></tr>\n<tr><td>Refresh token</td><td>일에서 주 단위</td><td>서버 저장 또는 hash 저장</td><td>rotation, reuse detection, device binding, logout/revoke API</td></tr>\n<tr><td>Session id</td><td>서버 정책에 따름</td><td>항상 stateful</td><td>저장소 삭제로 즉시 무효화, 로그인 후 재발급</td></tr>\n</table>\n<div class=\"callout warn\">\n<span class=\"co-label\">인터뷰 답변 프레임</span>\n<p>\"JWT는 stateless라 로그아웃이 어렵습니다\"에서 멈추지 말고, access token을 짧게 두고 refresh token만 stateful하게 추적해 회수 가능성을 확보한다고 설명하세요. 권한 변경 반영은 짧은 만료, permission version claim, 중요 작업의 서버 측 재검증을 조합합니다.</p>\n</div>\n</section>\n\n<!-- ============ AUTH CH 5 ============ -->\n<section id=\"auth-auth-ch5\">\n<div class=\"ch-head\"><span class=\"ch-code\">SEC-05</span><h2>OAuth2·OIDC</h2></div>\n<p class=\"lede\">OAuth2는 인가 위임 프로토콜이고, OIDC는 OAuth2 위에 인증 계층을 얹은 표준입니다. \"소셜 로그인\"을 안전하게 설명하려면 access token과 ID token을 구분해야 합니다.</p>\n<table>\n<tr><th>개념</th><th>목적</th><th>주의점</th></tr>\n<tr><td>OAuth2 access token</td><td>resource server API 호출 권한 위임</td><td>로그인 신원 증명으로 쓰지 않는다.</td></tr>\n<tr><td>OIDC ID token</td><td>사용자 인증 결과와 신원 claim 전달</td><td><code>iss</code>, <code>aud</code>, <code>nonce</code>, signature 검증 필수</td></tr>\n<tr><td>Authorization Code + PKCE</td><td>브라우저 URL에 token을 직접 노출하지 않고 code를 교환</td><td>SPA, 모바일, public client의 기본 선택</td></tr>\n<tr><td>Scope</td><td>위임 권한 범위 제한</td><td>필요 최소 scope만 요청하고 refresh scope를 분리한다.</td></tr>\n<tr><td>Redirect URI</td><td>인증 결과를 돌려받는 정확한 callback</td><td>wildcard, open redirect, preview URL 남용 금지</td></tr>\n</table>\n<h3><span class=\"h3-tag\">5.1</span>OIDC 로그인 검증 체크리스트</h3>\n<ul>\n<li>Authorization Code + PKCE를 사용하고 implicit flow를 새 설계에 쓰지 않는다.</li>\n<li><code>state</code>로 CSRF를 방어하고 <code>nonce</code>로 ID token replay를 방어한다.</li>\n<li>ID token의 issuer, audience, expiration, signature, nonce를 검증한다.</li>\n<li>provider 이메일을 내부 계정에 연결할 때 verified email 여부와 account linking 정책을 명시한다.</li>\n<li>redirect URI allowlist는 scheme, host, path까지 정확히 맞춘다.</li>\n</ul>\n<div class=\"callout risk\">\n<span class=\"co-label\">Negative tests</span>\n<p>다른 client id로 발급된 ID token을 넣어보기, redirect URI에 공격자 domain을 넣어보기, 이미 사용한 code를 재사용하기, state를 바꾸기, email_verified가 false인 계정 연결을 시도하기. 이 테스트들이 실패해야 OIDC 연동을 신뢰할 수 있습니다.</p>\n</div>\n</section>\n\n<!-- ============ AUTH CH 6 ============ -->\n<section id=\"auth-auth-ch6\">\n<div class=\"ch-head\"><span class=\"ch-code\">SEC-06</span><h2>비밀번호·MFA·계정 복구</h2></div>\n<p class=\"lede\">비밀번호 보안은 저장 방식만의 문제가 아닙니다. 등록, 로그인, 실패 제한, MFA, 복구, 비밀번호 변경 이후 세션 폐기까지 계정 생명주기 전체가 공격 표면입니다.</p>\n<table>\n<tr><th>영역</th><th>실무 기준</th><th>피해야 할 방식</th></tr>\n<tr><td>비밀번호 저장</td><td>Argon2id, bcrypt, scrypt 같은 password hashing function과 사용자별 salt를 사용한다.</td><td>평문, 복호화 가능한 암호화, 빠른 SHA/MD5 단독 해시</td></tr>\n<tr><td>비밀번호 정책</td><td>길이 중심, 유출 비밀번호 차단, 너무 잦은 강제 변경 금지</td><td>짧은 복잡도 규칙만 강요해 재사용을 유도</td></tr>\n<tr><td>로그인 실패</td><td>계정/아이피/디바이스 기준 rate limit, 지수 backoff, enumeration 방지</td><td>\"존재하지 않는 이메일\"처럼 계정 존재 여부 노출</td></tr>\n<tr><td>MFA</td><td>TOTP, WebAuthn/passkey, backup code, step-up auth</td><td>SMS만 단독 의존, MFA reset을 고객센터 전화 한 번으로 허용</td></tr>\n<tr><td>계정 복구</td><td>짧은 만료의 1회용 token, 사용 후 폐기, 복구 후 기존 세션 검토</td><td>장기 복구 링크, token 로그 노출, 이메일 변경과 복구를 같은 흐름에 둠</td></tr>\n</table>\n<h3><span class=\"h3-tag\">6.1</span>계정 복구 negative tests</h3>\n<table>\n<tr><th>시나리오</th><th>공격 시도</th><th>기대 결과</th></tr>\n<tr><td>Token 재사용</td><td>이미 사용한 reset link를 다시 호출</td><td>무효 처리, audit event 기록</td></tr>\n<tr><td>Token 추측</td><td>짧거나 순차적인 reset token 대입</td><td>충분한 entropy, rate limit, 동일한 실패 응답</td></tr>\n<tr><td>이메일 변경 우회</td><td>탈취 세션으로 이메일을 바꾸고 복구 수행</td><td>재인증, 기존 이메일 알림, cooling period 또는 step-up</td></tr>\n<tr><td>MFA reset 남용</td><td>지원팀 흐름으로 MFA를 제거</td><td>강한 신원 확인, 이중 승인, 지연 적용, 모든 세션 폐기</td></tr>\n</table>\n<div class=\"callout\">\n<span class=\"co-label\">면접 답변 프레임</span>\n<p>비밀번호는 \"bcrypt로 해시\"만 말하면 부족합니다. salt와 work factor, 유출 비밀번호 차단, rate limit, MFA, 복구 token 1회성, 복구 후 세션 무효화까지 계정 탈취 체인 전체를 끊는다고 설명해야 합니다.</p>\n</div>\n</section>\n\n<!-- ============ AUTH CH 7 ============ -->\n<section id=\"auth-auth-ch7\">\n<div class=\"ch-head\"><span class=\"ch-code\">SEC-07</span><h2>RBAC·ABAC·ReBAC</h2></div>\n<p class=\"lede\">인가는 권한 문자열을 확인하는 작업이 아니라 정책 결정입니다. 실무에서는 RBAC로 큰 권한 묶음을 관리하고, ABAC와 ReBAC로 소유권, 조직, 관계, 환경 조건을 보완합니다.</p>\n<h3><span class=\"h3-tag\">7.1</span>AUTHORIZATION DECISION TABLE</h3>\n<table>\n<tr><th>모델</th><th>판정 기준</th><th>잘 맞는 경우</th><th>위험</th></tr>\n<tr><td>RBAC</td><td>사용자의 role과 permission</td><td>admin, manager, member처럼 권한 묶음이 명확한 SaaS</td><td>role 폭증, role만 보고 객체 소유권을 놓침</td></tr>\n<tr><td>ABAC</td><td>actor/resource/environment attribute</td><td>부서, 지역, 시간, 데이터 등급, MFA 여부가 조건인 정책</td><td>정책이 흩어지면 디버깅과 감사가 어려움</td></tr>\n<tr><td>ReBAC</td><td>actor와 resource의 관계</td><td>문서 공유, 조직 그래프, 프로젝트 멤버십, 위임 관계</td><td>관계 그래프가 커질수록 성능과 설명 가능성 부담</td></tr>\n<tr><td>Ownership check</td><td>resource.owner_id == actor.id</td><td>주문, 프로필, 파일, 알림처럼 개인 소유 객체</td><td>bulk/export/search에서 빠지기 쉬움</td></tr>\n<tr><td>Tenant scope</td><td>resource.tenant_id == actor.tenant_id</td><td>B2B 멀티테넌트 서비스</td><td>캐시, background job, analytics에서 누락</td></tr>\n</table>\n<h3><span class=\"h3-tag\">7.2</span>정책 설계 원칙</h3>\n<ul>\n<li>기본값은 deny이고 허용 정책을 명시한다.</li>\n<li>권한 판정은 컨트롤러 장식이 아니라 service/repository/background job에서도 재사용 가능한 함수로 중앙화한다.</li>\n<li>중요 작업은 \"role이 admin인가\" 외에 tenant, resource relation, MFA freshness, reason code를 함께 본다.</li>\n<li>정책 변경은 테스트와 감사 로그를 동반한다. 권한 버전이 바뀌면 오래된 토큰 영향을 검토한다.</li>\n</ul>\n<div class=\"callout risk\">\n<span class=\"co-label\">IDOR negative tests</span>\n<p>단건 조회만 테스트하지 마세요. 목록 필터, 검색, bulk update, export, 첨부파일 다운로드, websocket subscription, worker job에 다른 사용자나 다른 tenant의 id를 섞어 넣어야 합니다. 성공 케이스보다 실패해야 하는 케이스가 인가 품질을 증명합니다.</p>\n</div>\n</section>\n\n<!-- ============ AUTH CH 8 ============ -->\n<section id=\"auth-auth-ch8\">\n<div class=\"ch-head\"><span class=\"ch-code\">SEC-08</span><h2>Tenant Isolation</h2></div>\n<p class=\"lede\">멀티테넌트 보안의 목표는 단순합니다. 한 테넌트의 사용자가 다른 테넌트의 데이터를 읽거나, 바꾸거나, 존재 여부를 추론할 수 없어야 합니다. 이것은 필터 옵션이 아니라 시스템 불변식입니다.</p>\n<h3><span class=\"h3-tag\">8.1</span>TENANT ISOLATION MODEL</h3>\n<table>\n<tr><th>계층</th><th>불변식</th><th>리뷰 질문</th></tr>\n<tr><td>Data model</td><td>테넌트 소유 row는 <code>tenant_id</code>가 필수이며 immutable에 가깝게 다룬다.</td><td>테넌트 없는 row 또는 tenant 변경 API가 존재하는가?</td></tr>\n<tr><td>Repository</td><td>조회, 수정, 삭제에 tenant predicate가 강제된다.</td><td><code>findById(id)</code>만으로 접근하는 경로가 있는가?</td></tr>\n<tr><td>Authorization</td><td>role은 tenant scope 안에서만 의미가 있다.</td><td>A tenant admin이 B tenant resource에 admin처럼 동작하지 않는가?</td></tr>\n<tr><td>Cache</td><td>캐시 키에 tenant namespace를 포함한다.</td><td><code>user:123</code>, <code>report:latest</code> 같은 전역 키가 섞이지 않는가?</td></tr>\n<tr><td>Async job</td><td>job payload와 worker query 모두 tenant context를 포함하고 재검증한다.</td><td>API에서 검증한 tenant를 worker가 신뢰만 하고 있지 않은가?</td></tr>\n<tr><td>Export/Search</td><td>검색 index, CSV export, analytics에도 동일한 tenant scope가 적용된다.</td><td>운영성 기능이 main API보다 더 넓게 읽지 않는가?</td></tr>\n<tr><td>DB defense</td><td>가능하면 RLS, view, mandatory predicate, DB role 분리로 2차 방어를 둔다.</td><td>애플리케이션 버그 하나가 전 테넌트 노출로 이어지는가?</td></tr>\n</table>\n<pre class=\"snippet-card\"><code>-- Example: tenant-scoped lookup shape\nSELECT *\nFROM invoices\nWHERE tenant_id = :current_tenant_id\n  AND id = :invoice_id;</code></pre>\n<h3><span class=\"h3-tag\">8.2</span>Tenant escape negative tests</h3>\n<table>\n<tr><th>표면</th><th>공격</th><th>기대 결과</th></tr>\n<tr><td>단건 API</td><td>다른 tenant의 id를 path에 입력</td><td>0건 또는 <code>404</code>, audit 가능한 denied event</td></tr>\n<tr><td>목록 API</td><td>query string에 다른 <code>tenantId</code> 삽입</td><td>서버 auth context의 tenant만 사용</td></tr>\n<tr><td>Bulk API</td><td>배열에 여러 tenant의 id를 섞음</td><td>전체 거부 또는 unauthorized 항목 분리, 정책 명확</td></tr>\n<tr><td>Cache</td><td>tenant A에서 생성한 캐시를 tenant B 요청으로 조회</td><td>cache miss 또는 scope mismatch</td></tr>\n<tr><td>Report job</td><td>job queue payload의 tenant id를 변조</td><td>worker가 actor/resource tenant를 재조회하고 차단</td></tr>\n</table>\n</section>\n\n<!-- ============ AUTH CH 9 ============ -->\n<section id=\"auth-auth-ch9\">\n<div class=\"ch-head\"><span class=\"ch-code\">SEC-09</span><h2>OWASP 서버 취약점</h2></div>\n<p class=\"lede\">OWASP 항목은 외울 목록이 아니라 리뷰 질문 묶음입니다. 백엔드 인증·보안 문맥에서는 접근 제어, 입력 신뢰, 암호화 실패, 설정 오류, 관측 부재가 반복적으로 사고를 만듭니다.</p>\n<table>\n<tr><th>위험</th><th>서버에서의 본질</th><th>대표 방어</th></tr>\n<tr><td>Broken Access Control</td><td>객체 단위 인가, tenant scope, 관리자 권한 통제가 빠짐</td><td>central policy, ownership check, negative test, audit</td></tr>\n<tr><td>Injection</td><td>데이터가 SQL, command, template, LDAP query의 코드로 실행됨</td><td>parameterized query, allowlist, shell 회피, output encoding</td></tr>\n<tr><td>Cryptographic Failures</td><td>민감 데이터가 평문 전송/저장되거나 약한 알고리즘 사용</td><td>TLS, password hashing, key management, at-rest encryption</td></tr>\n<tr><td>Identification/Auth Failures</td><td>세션 고정, 약한 reset, JWT 검증 누락, brute force 허용</td><td>session rotation, MFA, rate limit, token validation</td></tr>\n<tr><td>Security Misconfiguration</td><td>debug endpoint, default credential, 과한 CORS, 공개 bucket</td><td>secure defaults, config review, environment parity</td></tr>\n<tr><td>Vulnerable Components</td><td>취약한 라이브러리와 base image 방치</td><td>dependency scanning, patch SLA, SBOM</td></tr>\n<tr><td>Logging/Monitoring Failures</td><td>침해를 증명하거나 탐지할 로그가 없음</td><td>structured audit, alert, retention, redaction</td></tr>\n<tr><td>SSRF</td><td>서버가 공격자가 지정한 내부 URL로 요청</td><td>egress allowlist, metadata IP 차단, DNS pinning 주의</td></tr>\n</table>\n<h3><span class=\"h3-tag\">9.1</span>Injection code example</h3>\n<pre class=\"snippet-card\"><code>// Unsafe: user input changes SQL structure\n\"SELECT * FROM users WHERE email = '\" + email + \"'\"\n\n// Safer shape: input is bound as data\n\"SELECT * FROM users WHERE email = ?\"  // bind(email)</code></pre>\n<div class=\"callout warn\">\n<span class=\"co-label\">XSS와 CSRF 연결</span>\n<p>XSS는 피해자 브라우저에서 공격자 스크립트를 실행시키는 문제이고, CSRF는 피해자 브라우저가 인증 쿠키를 자동 첨부하게 만드는 문제입니다. XSS가 있으면 CSRF token을 읽거나 state-changing 요청을 직접 만들 수 있으므로 XSS 방어가 더 근본적인 방어선입니다.</p>\n</div>\n</section>\n\n<!-- ============ AUTH CH 10 ============ -->\n<section id=\"auth-auth-ch10\">\n<div class=\"ch-head\"><span class=\"ch-code\">SEC-10</span><h2>Secret·Key·Audit</h2></div>\n<p class=\"lede\">secret과 key는 인증 시스템의 루트 신뢰입니다. 감사 로그는 사고 이후 \"무슨 일이 있었는지\"를 증명하는 자료입니다. 둘 다 코드 뒤에 붙이는 운영 잡무가 아니라 보안 설계의 핵심입니다.</p>\n<table>\n<tr><th>대상</th><th>기준</th><th>사고 패턴</th></tr>\n<tr><td>API key / DB password</td><td>secret manager 또는 환경 주입, 최소 권한, 정기/사고 rotation</td><td>git commit, CI log, client bundle에 노출</td></tr>\n<tr><td>JWT signing key</td><td>key id, JWKS, rotation overlap, 알고리즘 allowlist</td><td>모든 서비스가 같은 shared secret 사용, rotation 불가</td></tr>\n<tr><td>Encryption key</td><td>KMS/HSM, envelope encryption, 접근 감사</td><td>데이터와 key를 같은 DB/backup에 저장</td></tr>\n<tr><td>Audit log</td><td>append-only, actor/action/resource/context, redaction, retention</td><td>디버그 로그만 있고 누가 무엇을 했는지 증명 불가</td></tr>\n<tr><td>PII/logging</td><td>token, password, full card, 주민번호 등은 저장하지 않거나 마스킹</td><td><code>request.body</code> 전체 로깅으로 credential 유출</td></tr>\n</table>\n<pre class=\"snippet-card\"><code>{\n  \"event\": \"tenant.member.role_changed\",\n  \"actor\": {\"type\": \"user\", \"id\": \"u_123\", \"tenant_id\": \"t_1\"},\n  \"resource\": {\"type\": \"member\", \"id\": \"m_456\", \"tenant_id\": \"t_1\"},\n  \"change\": {\"role\": {\"from\": \"viewer\", \"to\": \"admin\"}},\n  \"context\": {\"request_id\": \"req_789\", \"ip\": \"203.0.113.10\", \"reason\": \"ticket-42\"}\n}</code></pre>\n<h3><span class=\"h3-tag\">10.1</span>Audit event checklist</h3>\n<ul>\n<li>권한 변경, MFA 변경, 비밀번호 reset, 계정 복구, token revoke, export, 관리자 impersonation은 감사 이벤트를 남긴다.</li>\n<li>actor는 이메일만이 아니라 안정적인 id, service account, impersonation 여부를 포함한다.</li>\n<li>resource에는 tenant id와 resource id를 포함해 사고 범위를 query할 수 있게 한다.</li>\n<li>secret, raw token, 비밀번호, 전체 PII는 감사 로그에도 남기지 않는다.</li>\n</ul>\n<div class=\"callout risk\">\n<span class=\"co-label\">Secret leak first response</span>\n<p>노출된 secret은 \"삭제\"가 아니라 \"폐기와 재발급\"이 우선입니다. git history, CI artifact, log sink, client bundle에 남은 사본을 모두 노출 범위로 보고, last-used log로 악용 가능성을 확인합니다.</p>\n</div>\n</section>\n\n<!-- ============ AUTH CH 11 ============ -->\n<section id=\"auth-auth-ch11\">\n<div class=\"ch-head\"><span class=\"ch-code\">SEC-11</span><h2>Security Review Rubric</h2></div>\n<p class=\"lede\">보안 리뷰는 \"문제 없어 보임\"이 아니라 정해진 질문에 증거로 답하는 과정입니다. PR, 설계 문서, 면접 과제 리뷰에 아래 표를 그대로 붙여도 됩니다.</p>\n<h3><span class=\"h3-tag\">11.1</span>SECURITY REVIEW RUBRIC</h3>\n<table>\n<tr><th>영역</th><th>리뷰 질문</th><th>통과 증거</th></tr>\n<tr><td>신뢰 경계</td><td>새 입력이 어느 경계에서 들어오며 누가 검증하는가?</td><td>schema validation, signature verification, server-side recalculation</td></tr>\n<tr><td>인증</td><td>요청 주체를 어떤 증거로 확인하며 만료/폐기 정책은 무엇인가?</td><td>session/token validation, MFA/step-up 기준, logout/revoke 경로</td></tr>\n<tr><td>인가</td><td>actor-resource-action 판정이 중앙화되어 있고 negative test가 있는가?</td><td>policy test, IDOR test, ownership/tenant check</td></tr>\n<tr><td>Tenant</td><td>query, cache, job, export, search에 tenant scope가 강제되는가?</td><td>tenant predicate, RLS/view, scoped cache key, escape tests</td></tr>\n<tr><td>State change</td><td>CSRF, replay, idempotency, 동시성 문제가 다뤄졌는가?</td><td>CSRF token/SameSite, idempotency key, optimistic lock</td></tr>\n<tr><td>Secret/PII</td><td>secret과 PII가 코드, 로그, URL, analytics, client bundle에 남지 않는가?</td><td>redaction, secret manager, scanner result, retention policy</td></tr>\n<tr><td>운영 증거</td><td>침해를 탐지하고 조사할 audit event와 alert가 있는가?</td><td>event schema, dashboard/query, owner, runbook link</td></tr>\n</table>\n<h3><span class=\"h3-tag\">11.2</span>리뷰 코멘트 예시</h3>\n<table>\n<tr><th>약한 코멘트</th><th>좋은 코멘트</th></tr>\n<tr><td>권한 체크 필요해 보입니다.</td><td><code>GET /reports/{id}</code>가 id만으로 조회합니다. <code>tenant_id</code>와 report membership을 함께 확인하는 negative test를 추가해야 합니다.</td></tr>\n<tr><td>CORS 괜찮나요?</td><td>credential 요청을 쓰므로 운영 origin allowlist와 <code>Vary: Origin</code>이 필요합니다. preview wildcard가 운영에 남지 않는지도 확인해야 합니다.</td></tr>\n<tr><td>로그 조심하세요.</td><td>reset token이 query string과 access log에 남을 수 있습니다. token은 fragment가 아니라 POST body로 받고 log redaction을 추가해야 합니다.</td></tr>\n</table>\n</section>\n\n<!-- ============ AUTH CH 12 ============ -->\n<section id=\"auth-auth-ch12\">\n<div class=\"ch-head\"><span class=\"ch-code\">SEC-12</span><h2>Security Tabletop Drills</h2></div>\n<p class=\"lede\">SECURITY TABLETOP DRILLS는 취약점 이름 암기가 아니라 사고 대응 근육을 만드는 연습입니다. 제한 시간 안에 재현, 영향 범위, 임시 통제, 영구 수정, 고객/법무/지원 handoff까지 말할 수 있어야 합니다.</p>\n<h3><span class=\"h3-tag\">12.1</span>SECURITY TABLETOP DRILLS</h3>\n<table>\n<tr><th>Drill</th><th>상황</th><th>30~60분 안에 제출할 증거</th></tr>\n<tr><td>IDOR report</td><td>외부 제보자가 다른 tenant의 export URL에 접근 가능하다고 보고</td><td>재현 요청, affected resource query, hotfix diff, audit query, 고객 영향 판단</td></tr>\n<tr><td>Leaked token</td><td>Git history와 CI log에서 오래된 API token 발견</td><td>rotation record, last-used log, 접근 가능 리소스, secret scanning rule</td></tr>\n<tr><td>Refresh reuse</td><td>이미 rotation된 refresh token이 다시 사용됨</td><td>token family 폐기, device timeline, IP/user-agent 비교, 사용자 알림 초안</td></tr>\n<tr><td>Webhook replay</td><td>결제 webhook event id 재사용으로 중복 상태 변경 의심</td><td>event ledger, signature/timestamp 검증, idempotency test, ledger correction plan</td></tr>\n<tr><td>Admin abuse</td><td>CS 계정이 업무 시간 외 대량 PII export 수행</td><td>actor timeline, reason code, approval record, export scope, access review action</td></tr>\n<tr><td>Tenant cache leak</td><td>tenant A dashboard 값이 tenant B에 보인다는 고객 문의</td><td>cache key 분석, purge 범위, affected tenant query, regression test</td></tr>\n</table>\n<h3><span class=\"h3-tag\">12.2</span>진행 규칙</h3>\n<ul>\n<li>처음 10분은 재현과 증거 보존에만 쓴다. 추측으로 고객 영향을 확정하지 않는다.</li>\n<li>임시 통제는 기능 flag, token revoke, origin 차단, export 중지처럼 되돌릴 수 있는 조치를 우선한다.</li>\n<li>영구 수정은 코드 변경, 테스트, 운영 알림, runbook 업데이트까지 포함한다.</li>\n<li>모든 drill은 \"다음에 더 빨리 찾기 위한 alert나 audit query\"를 산출물로 남긴다.</li>\n</ul>\n</section>\n\n<!-- ============ AUTH CH 13 ============ -->\n<section id=\"auth-auth-ch13\">\n<div class=\"ch-head\"><span class=\"ch-code\">SEC-13</span><h2>SECURITY INCIDENT PACKET</h2></div>\n<p class=\"lede\">SECURITY INCIDENT PACKET은 사고가 의심될 때 흩어진 정보를 한곳에 모으는 최소 문서입니다. 목적은 멋진 보고서가 아니라, 대응자가 같은 사실 위에서 빠르게 판단하게 하는 것입니다.</p>\n<h3><span class=\"h3-tag\">13.1</span>SECURITY INCIDENT PACKET</h3>\n<table>\n<tr><th>항목</th><th>채워야 할 내용</th><th>좋은 증거</th></tr>\n<tr><td>Summary</td><td>무슨 일이 의심되며 현재 상태는 open/contained/resolved 중 무엇인가?</td><td>한 문장 요약, 최초 발견 시각, 현재 owner</td></tr>\n<tr><td>Reproduction</td><td>어떤 요청으로 문제가 재현되는가?</td><td>curl, request id, account/tenant fixture, expected vs actual</td></tr>\n<tr><td>Scope</td><td>영향 받은 actor, tenant, resource, 시간 범위는 어디까지인가?</td><td>audit query, DB query, log query, sampling 기준</td></tr>\n<tr><td>Temporary control</td><td>피해 확산을 막기 위해 무엇을 끊었는가?</td><td>feature flag, token revoke, key rotation, CORS/origin 차단, export 중지</td></tr>\n<tr><td>Permanent fix</td><td>근본 원인을 제거하는 코드/설정/운영 변경은 무엇인가?</td><td>patch, migration, policy test, negative test, deployment id</td></tr>\n<tr><td>Audit evidence</td><td>나중에 설명 가능한 원본 증거가 보존됐는가?</td><td>append-only log, ticket, timeline, hash/export of relevant logs</td></tr>\n<tr><td>Comms</td><td>고객, 지원, 법무, 보안, 경영진에게 무엇을 언제 알릴 것인가?</td><td>impact statement, FAQ, legal review, support macro</td></tr>\n<tr><td>Follow-up</td><td>재발 방지를 위해 어떤 owner가 언제까지 무엇을 바꾸는가?</td><td>action item, due date, alert/runbook/test update</td></tr>\n</table>\n<pre class=\"snippet-card\"><code># SECURITY INCIDENT PACKET\nstatus: contained\nowner: security-oncall\ndetected_at: 2026-07-01T09:10:00Z\nsummary: Tenant-scoped export URL accepted another tenant's file id.\ntemporary_control: Disabled export download endpoint behind feature flag.\nscope_query: audit.event = \"export.downloaded\" AND created_at BETWEEN ...\npermanent_fix: Require tenant_id predicate in export lookup and add IDOR regression test.</code></pre>\n<div class=\"callout gm\">\n<span class=\"co-label\">면접 답변 프레임</span>\n<p>사고 대응 질문에는 \"바로 고치겠습니다\"보다 순서가 중요합니다. 먼저 증거를 보존하고 재현을 확보한 뒤, 영향 범위를 산정하고 임시 통제로 확산을 막습니다. 그 다음 영구 수정과 negative test를 배포하고, 고객/법무/지원 커뮤니케이션과 재발 방지 액션을 남깁니다.</p>\n</div>\n</section>\n\n<section id=\"backend-security-review-packet\">\n<div class=\"ch-head\"><span class=\"ch-code\">PACKET</span><h2>BACKEND SECURITY REVIEW PACKET</h2></div>\n<p class=\"lede\">보안 문서를 읽은 뒤 실제 PR과 설계 리뷰에 붙일 증거 묶음입니다. 인증·권한·데이터 경계 변경은 단순 기능 변경처럼 리뷰하지 않습니다.</p>\n<table>\n<tr><th>변경 유형</th><th>필수 증거</th><th>거절 기준</th></tr>\n<tr><td>로그인·세션</td><td>cookie option, session fixation 방어, logout/revoke 테스트</td><td>성공 로그인만 있고 만료·폐기·탈취 시나리오 없음</td></tr>\n<tr><td>JWT·refresh</td><td>token ttl, refresh rotation, replay detection, reuse response</td><td>refresh token을 장기 bearer token처럼 취급</td></tr>\n<tr><td>권한</td><td>RBAC/ABAC decision table, 401/403 negative test</td><td>프론트 route guard만 있고 서버 인가 없음</td></tr>\n<tr><td>테넌트 격리</td><td>tenant id source, query scope, cross-tenant negative fixture</td><td>client가 보낸 tenant id를 신뢰</td></tr>\n<tr><td>입력·외부 호출</td><td>validation schema, SSRF allowlist, SQL parameterization evidence</td><td>문자열 필터만으로 injection을 막는다고 설명</td></tr>\n</table>\n</section>",
};

export default document;
