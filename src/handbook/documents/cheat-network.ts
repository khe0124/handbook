import type { HandbookDocumentContent } from "../types";

const document: HandbookDocumentContent = {
  navHtml: "<div class=\"nav-brand\">CHEAT SHEET</div><div class=\"nav-title\">Network</div>\n  <a href=\"#ch1\"><span class=\"code\">NW-CS-01</span>개요</a>\n  <a href=\"#ch2\"><span class=\"code\">NW-CS-02</span>핵심 명령/개념</a>\n  <a href=\"#ch3\"><span class=\"code\">NW-CS-03</span>판단 기준</a>\n  <a href=\"#ch4\"><span class=\"code\">NW-CS-04</span>자주 나는 실수</a>\n  <a href=\"#ch5\"><span class=\"code\">NW-CS-05</span>면접/리뷰용 한 줄 답변</a>\n  <a href=\"#ch6\"><span class=\"code\">NW-CS-06</span>Red Flags</a>\n  <a href=\"#ch7\"><span class=\"code\">NW-CS-07</span>Review Gate</a>\n  <a href=\"#ch8\"><span class=\"code\">NW-CS-08</span>Failure Playbook</a>",
  mainHtml: "<header class=\"hero\">\n    <div class=\"hero-serial\"><span>DOC : CHEAT-NETWORK-V1</span><span>SCOPE : DNS · TCP · TLS · ROUTING · FIREWALL</span><span>LANG : KO</span></div>\n    <h1>Network<br>Cheat Sheet</h1>\n    <p class=\"hero-sub\">연결 장애를 DNS, 경로, 포트, TLS, HTTP 단계로 좁히기 위한 빠른 참조입니다. 한 번에 추측하지 않고 계층별로 확인합니다.</p>\n    <div class=\"hero-meta\">COMPILED : 2026-06 · FORMAT : QUICK REFERENCE</div>\n  </header>\n  <section id=\"ch1\">\n    <div class=\"ch-head\"><span class=\"ch-code\">NW-CS-01</span><h2>개요</h2></div>\n    <p class=\"lede\">네트워크 디버깅은 source, destination, name, route, policy, protocol 순서로 좁히면 빠릅니다. ping 성공은 서비스 성공을 의미하지 않습니다.</p>\n    <table>\n      <tr><th>증상</th><th>먼저 확인할 것</th><th>분리 기준</th></tr>\n      <tr><td>도메인이 안 열림</td><td>DNS record, TTL, authoritative response</td><td>이름이 틀린 문제인지 IP 이후 문제인지 분리</td></tr>\n      <tr><td>포트 연결 실패</td><td>route table, security group, firewall, listening port</td><td>클라이언트에서 못 나가는지 서버가 안 듣는지 확인</td></tr>\n      <tr><td>TLS 오류</td><td>certificate chain, SNI, hostname, expiration</td><td>인증서 신뢰 문제인지 앱 HTTP 문제인지 분리</td></tr>\n      <tr><td>HTTP 오류</td><td>status code, header, proxy, upstream log</td><td>네트워크는 됐고 앱/프록시 계층 문제로 이동</td></tr>\n      <tr><td>간헐 장애</td><td>DNS cache, connection pool, load balancer target health</td><td>특정 AZ, 특정 upstream, 특정 클라이언트인지 비교</td></tr>\n    </table>\n  </section>\n  <section id=\"ch2\">\n    <div class=\"ch-head\"><span class=\"ch-code\">NW-CS-02</span><h2>핵심 명령/개념</h2></div>\n    <table>\n      <tr><th>명령/개념</th><th>압축 설명</th><th>해석 포인트</th></tr>\n      <tr><td><code>dig example.com</code></td><td>DNS record와 TTL 확인</td><td>권위 응답, CNAME chain, split-horizon DNS 확인</td></tr>\n      <tr><td><code>curl -v https://host</code></td><td>TLS와 HTTP 요청/응답 흐름 확인</td><td>DNS, connect, TLS, status, redirect를 한 번에 관찰</td></tr>\n      <tr><td><code>nc -zv host 443</code></td><td>특정 host:port 연결 가능 여부 확인</td><td>HTTP 이전의 TCP 연결 여부만 빠르게 확인</td></tr>\n      <tr><td><code>traceroute host</code></td><td>패킷이 지나가는 hop 추정</td><td>방화벽/ICMP 정책 때문에 완전한 증거는 아님</td></tr>\n      <tr><td><code>ss -tlnp</code></td><td>서버에서 listening socket 확인</td><td>서비스가 어느 address/port에 bind됐는지 확인</td></tr>\n      <tr><td>CIDR</td><td>IP 범위를 prefix 길이로 표현하는 방식</td><td>route table과 firewall rule의 적용 범위를 결정</td></tr>\n      <tr><td>SNI</td><td>TLS handshake에서 접속하려는 hostname 전달</td><td>여러 인증서를 쓰는 LB/proxy에서 중요</td></tr>\n    </table>\n  </section>\n  <section id=\"ch3\">\n    <div class=\"ch-head\"><span class=\"ch-code\">NW-CS-03</span><h2>판단 기준</h2></div>\n    <table>\n      <tr><th>선택</th><th>기준</th><th>주의점</th></tr>\n      <tr><td>Public subnet</td><td>인터넷에서 직접 들어와야 하고 public route가 필요함</td><td>public IP와 route만으로는 보안 허용이 아님</td></tr>\n      <tr><td>Private subnet</td><td>외부 직접 접근을 막고 NAT나 내부 경로만 허용함</td><td>outbound 필요 시 NAT/egress proxy 확인</td></tr>\n      <tr><td>NAT</td><td>private 자원이 외부로 나가야 하지만 외부에서 들어오면 안 됨</td><td>return path, port exhaustion, 비용 확인</td></tr>\n      <tr><td>Security group</td><td>instance 또는 interface 기준 stateful 정책</td><td>응답 트래픽은 상태 추적으로 허용됨</td></tr>\n      <tr><td>NACL/firewall</td><td>subnet 또는 경계 기준 stateless 정책이 필요함</td><td>inbound/outbound와 ephemeral port를 모두 봐야 함</td></tr>\n      <tr><td>Load balancer</td><td>target health, TLS termination, path routing이 필요함</td><td>client IP 전달, timeout, health check path 확인</td></tr>\n    </table>\n  </section>\n  <section id=\"ch4\">\n    <div class=\"ch-head\"><span class=\"ch-code\">NW-CS-04</span><h2>자주 나는 실수</h2></div>\n    <ul>\n      <li>DNS가 맞는지 확인하지 않고 애플리케이션 문제로 단정한다. 같은 이름이 내부/외부에서 다르게 해석될 수 있다.</li>\n      <li>outbound는 열었지만 return path나 inbound rule을 확인하지 않는다. stateless 장비에서는 양방향 규칙이 필요하다.</li>\n      <li>TLS 인증서의 CN/SAN과 실제 접속 hostname이 다른 문제를 놓친다. IP로 접속하면 인증서 검증이 실패할 수 있다.</li>\n      <li>보안 그룹과 NACL의 stateful/stateless 차이를 혼동한다. ephemeral port 때문에 한쪽만 열어서는 실패할 수 있다.</li>\n      <li>VPN tunnel up을 서비스 통신 성공과 같은 의미로 본다. routing, DNS, firewall, app bind까지 별도 확인해야 한다.</li>\n      <li>proxy나 load balancer timeout을 앱 timeout보다 짧게 둔다. 요청은 처리됐는데 클라이언트는 실패로 보고 재시도할 수 있다.</li>\n    </ul>\n  </section>\n  <section id=\"ch5\">\n    <div class=\"ch-head\"><span class=\"ch-code\">NW-CS-05</span><h2>면접/리뷰용 한 줄 답변</h2></div>\n    <div class=\"glossary\">\n      <div class=\"g-row\"><div class=\"g-term\"><b>요청 흐름</b><span>request path</span></div><div class=\"g-def\">브라우저 요청은 DNS, TCP, TLS, HTTP, proxy, application 순서로 지나가며 각 단계의 실패 신호가 다릅니다.</div></div>\n      <div class=\"g-row\"><div class=\"g-term\"><b>방화벽</b><span>policy</span></div><div class=\"g-def\">네트워크 정책은 출발지, 목적지, 포트, 방향, 상태 추적 여부를 함께 봐야 합니다.</div></div>\n      <div class=\"g-row\"><div class=\"g-term\"><b>디버깅</b><span>debug</span></div><div class=\"g-def\">연결 장애는 추측보다 계층별 증거로 좁혀야 하며, ping 하나로 서비스 정상 여부를 판단할 수 없습니다.</div></div>\n      <div class=\"g-row\"><div class=\"g-term\"><b>CIDR</b><span>addressing</span></div><div class=\"g-def\">CIDR은 IP 범위의 언어라서 subnet, route, firewall rule을 설계할 때 가장 먼저 맞아야 합니다.</div></div>\n      <div class=\"g-row\"><div class=\"g-term\"><b>TLS</b><span>certificate</span></div><div class=\"g-def\">TLS 문제는 인증서 체인, 만료, hostname, SNI를 나눠 보면 애플리케이션 오류와 빠르게 분리됩니다.</div></div>\n    </div>\n  </section>\n  <section id=\"ch6\">\n    <div class=\"ch-head\"><span class=\"ch-code\">NW-CS-06</span><h2>Red Flags</h2></div>\n    <table>\n      <tr><th>신호</th><th>왜 위험한가</th><th>즉시 확인</th></tr>\n      <tr><td>ping 성공만으로 정상 판단</td><td>ICMP와 TCP/TLS/HTTP 성공은 별개</td><td><code>nc -zv</code>, <code>curl -v</code>, 서버 access log</td></tr>\n      <tr><td>VPN tunnel up인데 서비스 실패</td><td>터널 협상과 route/DNS/firewall/app bind는 별개</td><td>route table, return path, 양쪽 firewall</td></tr>\n      <tr><td>간헐 timeout</td><td>특정 target, AZ, connection pool, LB health 문제일 수 있음</td><td>target health, 5xx/timeout 분포, trace</td></tr>\n      <tr><td>TLS hostname mismatch</td><td>사용자는 보안 경고 또는 handshake 실패를 봄</td><td><code>openssl s_client -servername host -connect host:443</code></td></tr>\n      <tr><td>NACL/stateless 장비 변경</td><td>응답 ephemeral port가 막혀 한 방향만 성공할 수 있음</td><td>inbound/outbound 양방향 rule</td></tr>\n    </table>\n  </section>\n  <section id=\"ch7\">\n    <div class=\"ch-head\"><span class=\"ch-code\">NW-CS-07</span><h2>Review Gate</h2></div>\n    <table>\n      <tr><th>변경</th><th>merge/deploy 차단 기준</th><th>필수 증거</th></tr>\n      <tr><td>방화벽/SG/NACL</td><td>source, destination, protocol, port, direction이 불명확함</td><td>규칙 diff, 허용 CIDR, 검증 명령</td></tr>\n      <tr><td>DNS 변경</td><td>TTL, rollback record, 내부/외부 resolver 차이를 모름</td><td><code>dig</code> 결과, TTL, 이전 record</td></tr>\n      <tr><td>LB/proxy 변경</td><td>health check, timeout, header forwarding 검증 없음</td><td>target health, <code>curl -v</code>, upstream log</td></tr>\n      <tr><td>VPN/peering</td><td>CIDR 충돌, return route, 상대 방화벽 담당자 확인 없음</td><td>route table, peer CIDR, 테스트 source</td></tr>\n      <tr><td>TLS 인증서</td><td>SAN, chain, expiry, SNI 검증 없음</td><td><code>openssl s_client</code>, browser/curl 검증</td></tr>\n    </table>\n  </section>\n  <section id=\"ch8\">\n    <div class=\"ch-head\"><span class=\"ch-code\">NW-CS-08</span><h2>Failure Playbook</h2></div>\n    <div class=\"serial-card\"><span class=\"sc-label\">L3-L7 TRIAGE</span>1. <code>dig host</code>로 이름 해석 확인<br>2. <code>nc -zv host port</code>로 TCP 도달성 확인<br>3. <code>curl -vk https://host</code>로 TLS/HTTP 확인<br>4. 서버에서 <code>ss -tlnp</code>로 listen 확인<br>5. LB/proxy/access log와 target health 비교<br>6. route table, firewall, return path를 양쪽에서 대조</div>\n    <div class=\"callout warn\"><span class=\"co-label\">운영 원칙</span><p>네트워크 장애는 \"열었다\"가 아니라 어떤 출발지에서 어떤 목적지의 어떤 포트를 어떤 방향으로 검증했는지로 기록합니다. 증거 없는 네트워크 정상 판단은 장애 시간을 늘립니다.</p></div>\n  </section>",
};

export default document;
