import type { HandbookDocumentContent } from "../types";

const document: HandbookDocumentContent = {
  navHtml: "<div class=\"nav-title\">모델 커스터마이징</div>\n  <a href=\"#ch1\"><span class=\"code\">CUS-01</span>판단 기준</a>\n  <a href=\"#ch2\"><span class=\"code\">CUS-02</span>Fine-tuning</a>\n  <a href=\"#ch3\"><span class=\"code\">CUS-03</span>Distillation</a>\n  <a href=\"#ch4\"><span class=\"code\">CUS-04</span>Routing</a>\n  <a href=\"#ch5\"><span class=\"code\">CUS-05</span>Dataset</a>\n  <a href=\"#ch6\"><span class=\"code\">CUS-06</span>Release</a>\n  <a href=\"#depth\"><span class=\"code\">DEPTH</span>의사결정</a>",
  mainHtml: "<header class=\"hero\">\n  <div class=\"hero-serial\">\n    <span>DOC : LLM-MODEL-CUSTOMIZATION-V1</span>\n    <span>SCOPE : FINE-TUNING · DISTILLATION · ROUTING · DATASET · RELEASE</span>\n    <span>LANG : KO</span>\n  </div>\n  <h1>Fine-tuning·Customization·Model Routing</h1>\n  <p class=\"hero-sub\">모델 커스터마이징은 “더 학습시키면 좋아진다”가 아닙니다. 문제의 원인이 지식 부족인지, 형식 불안정인지, 스타일 불일치인지, 비용·지연인지 구분한 뒤 가장 작은 개입을 선택해야 합니다.</p>\n</header>\n\n<section id=\"ch1\">\n<div class=\"ch-head\"><span class=\"ch-code\">CUS-01</span><h2>CUSTOMIZATION DECISION MATRIX</h2></div>\n<p class=\"lede\">prompt vs RAG vs fine-tuning 선택은 유행이 아니라 실패 원인에 따라 결정합니다. 최신 사내 지식이 없으면 RAG, 출력 형식이 흔들리면 schema와 validator, 반복 스타일이 문제면 fine-tuning 후보입니다.</p>\n<table>\n<tr><th>문제</th><th>먼저 시도</th><th>fine-tuning 후보가 되는 조건</th></tr>\n<tr><td>최신·사내 지식 부족</td><td>RAG, tool, freshness filter</td><td>아님. 지식 주입 목적 fine-tuning은 갱신·삭제가 어렵다</td></tr>\n<tr><td>출력 형식 불안정</td><td>structured output, schema, repair loop</td><td>반복된 형식 작업이 많고 schema만으로 부족할 때</td></tr>\n<tr><td>도메인 문체·분류 기준</td><td>examples, rubric, eval dataset</td><td>충분한 라벨 데이터와 회귀 평가가 있을 때</td></tr>\n<tr><td>비용·지연</td><td>model routing, cache, compression</td><td>큰 모델 출력을 작은 모델로 distillation할 수 있을 때</td></tr>\n<tr><td>복잡한 reasoning</td><td>task decomposition, tools, verifier</td><td>데이터가 reasoning trace보다 결과 라벨 중심이면 효과 제한</td></tr>\n</table>\n<div class=\"serial-card\"><span class=\"sc-label\">CUSTOMIZATION DECISION MATRIX</span>지식 문제는 RAG · 계약 문제는 schema · 반복 스타일 문제는 fine-tuning · 비용 문제는 distillation/routing · 권한 문제는 서버 정책으로 푼다.</div>\n</section>\n\n<section id=\"ch2\">\n<div class=\"ch-head\"><span class=\"ch-code\">CUS-02</span><h2>fine-tuning readiness</h2></div>\n<p class=\"lede\">Fine-tuning은 모델에게 새 정책 문서를 외우게 하는 도구가 아니라, 반복되는 입력-출력 행동을 더 안정적으로 만드는 도구입니다. 데이터 품질과 평가 체계가 없으면 prompt보다 더 고치기 어려운 회귀를 만듭니다.</p>\n<table>\n<tr><th>준비 조건</th><th>필요한 증거</th><th>거절 기준</th></tr>\n<tr><td>데이터</td><td>라벨 품질, 중복 제거, 금지 데이터 제거</td><td>운영 로그를 그대로 학습 데이터로 사용</td></tr>\n<tr><td>평가</td><td>base vs tuned 비교 golden set</td><td>좋아 보이는 예시 몇 개만 확인</td></tr>\n<tr><td>목표</td><td>형식 안정성, 분류 정확도, 스타일 일관성 같은 측정 가능한 목표</td><td>“전반적으로 똑똑하게” 같은 모호한 목표</td></tr>\n<tr><td>운영</td><td>rollback, versioning, safety regression</td><td>tuned model 배포 후 원인 추적 불가</td></tr>\n</table>\n</section>\n\n<section id=\"ch3\">\n<div class=\"ch-head\"><span class=\"ch-code\">CUS-03</span><h2>distillation과 small model strategy</h2></div>\n<p class=\"lede\">Distillation은 큰 모델의 판단을 작은 모델이나 더 싼 경로로 이전해 비용·지연을 줄이는 전략입니다. 단, teacher output을 정답처럼 믿지 말고 사람 라벨과 hard negative를 섞어야 합니다.</p>\n<table>\n<tr><th>전략</th><th>효과</th><th>위험</th></tr>\n<tr><td>teacher-generated labels</td><td>라벨 생산 속도 증가</td><td>teacher bias와 hallucination 복제</td></tr>\n<tr><td>hard negative mining</td><td>애매한 케이스 구분력 개선</td><td>데이터 분포가 실제 운영과 멀어질 수 있음</td></tr>\n<tr><td>specialized small model</td><td>분류·추출 비용 절감</td><td>범위 밖 요청에서 조용히 실패</td></tr>\n<tr><td>fallback to large model</td><td>불확실 케이스 품질 보호</td><td>routing 기준이 없으면 비용 예측 실패</td></tr>\n</table>\n</section>\n\n<section id=\"ch4\">\n<div class=\"ch-head\"><span class=\"ch-code\">CUS-04</span><h2>routing policy와 reasoning effort</h2></div>\n<p class=\"lede\">모든 요청을 가장 강한 모델에 보내는 것은 품질 전략이 아니라 비용 전략 실패입니다. routing policy는 risk, difficulty, latency SLO, context size, tool requirement, reasoning_budget을 기준으로 결정해야 합니다.</p>\n<pre><code>routing policy\nif risk_tier == high: quality_model + verifier + human_review\nif task == simple_classification and confidence >= .9: small_model\nif retrieval_empty: no_answer_or_search_rewrite\nif tool_write: draft_only_until_approval\nif reasoning_required: reasoning effort = high with max_budget</code></pre>\n<table>\n<tr><th>신호</th><th>라우팅</th><th>로그</th></tr>\n<tr><td>쉬운 분류</td><td>작은 모델 또는 rule-first</td><td>route=cheap_path</td></tr>\n<tr><td>긴 문서 분석</td><td>긴 context 모델 + chunk compression</td><td>input_tokens, truncation</td></tr>\n<tr><td>고위험 판단</td><td>품질 모델 + verifier + human approval</td><td>risk_tier, approval_id</td></tr>\n<tr><td>복잡한 추론</td><td>reasoning_budget 상향, tool decomposition</td><td>reasoning_effort, step_count</td></tr>\n</table>\n</section>\n\n<section id=\"ch5\">\n<div class=\"ch-head\"><span class=\"ch-code\">CUS-05</span><h2>training dataset governance</h2></div>\n<p class=\"lede\">커스터마이징 데이터셋은 운영 로그의 복사본이 아닙니다. 개인정보, 비밀, 저작권, 사용자 동의, 라벨 기준, 삭제 요청 대응을 갖춘 제품 자산입니다.</p>\n<table>\n<tr><th>통제</th><th>내용</th><th>감사 질문</th></tr>\n<tr><td>provenance</td><td>데이터 출처와 라이선스 기록</td><td>이 예시를 학습에 쓸 권리가 있는가</td></tr>\n<tr><td>redaction</td><td>PII, secrets, tenant identifiers 제거</td><td>원문 로그가 그대로 들어가지 않았는가</td></tr>\n<tr><td>label policy</td><td>정답 기준과 reviewer agreement</td><td>라벨러마다 다른 기준을 쓰지 않는가</td></tr>\n<tr><td>holdout</td><td>학습에 쓰지 않는 고정 평가 세트</td><td>튜닝 데이터에 평가 케이스가 섞이지 않았는가</td></tr>\n</table>\n</section>\n\n<section id=\"ch6\">\n<div class=\"ch-head\"><span class=\"ch-code\">CUS-06</span><h2>custom model release gate</h2></div>\n<p class=\"lede\">커스텀 모델 배포는 prompt 변경보다 되돌리기 어려울 수 있습니다. offline eval, safety fixture, shadow traffic, canary, rollback을 갖춘 변경 관리로 다뤄야 합니다.</p>\n<pre><code>custom_model_release\nbase_model: quality-model@stable\ntuned_model: classifier-tuned@v3\neval_delta: accuracy +2.1pp, refusal_accuracy -0.2pp\nlatency_delta: -180ms\nsafety_regression: pass\nshadow_traffic: 7d\nrollback: route back to base_model</code></pre>\n</section>\n\n<section id=\"depth\">\n<div class=\"ch-head\"><span class=\"ch-code\">DEPTH</span><h2>커스터마이징 의사결정 방어하기</h2></div>\n<h3>MARKER: concept-depth — fine-tuning은 지식 저장소가 아니다</h3>\n<p>사내 정책, 가격, 계약 조항처럼 바뀌고 삭제되어야 하는 지식은 RAG나 tool로 공급해야 합니다. fine-tuning에 넣으면 업데이트, 권한 분리, 삭제 요청, 출처 추적이 어려워집니다. fine-tuning이 강한 영역은 반복되는 형식, 분류 기준, 문체, 특정 출력 행동입니다. 그래서 커스터마이징 리뷰의 첫 질문은 “모델이 모르는가”가 아니라 “외부 근거를 줬는데도 같은 행동을 반복해서 못 하는가”입니다.</p>\n<h3>MARKER: failure-driven — 튜닝이 평가를 앞지르는 실패</h3>\n<p>데이터 몇 백 건으로 tuned model을 만들고 데모 예시가 좋아졌다고 출시하면, 숨어 있던 안전 회귀와 long-tail 실패가 운영에서 드러납니다. 튜닝 전에는 반드시 base model 대비 평가 기준이 있어야 하고, 좋아진 지표와 나빠진 지표를 함께 봐야 합니다. 특히 refusal accuracy, PII leakage, prompt injection fixture, schema failure rate는 정확도 평균 뒤에 숨기 쉽습니다.</p>\n<h3>MARKER: artifact-ready — Model customization review note</h3>\n<pre><code>problem: support ticket category style inconsistency\nnot_chosen: RAG, because no missing knowledge\nbaseline: prompt+schema accuracy 91.2%, p95 1.8s\nchosen: small classifier fine-tuning + fallback to quality model\nrelease_gate: accuracy >= 93%, safety no regression, cost -30%\nrollback: disable custom route in model_gateway</code></pre>\n<h3>MARKER: failure-driven — routing policy 없이 작은 모델로 비용만 줄이는 실패</h3>\n<p>비용 절감을 위해 모든 요청을 작은 모델로 보내면 평균 비용은 줄어들 수 있지만, long-tail 질문과 고위험 판단에서 품질 실패가 조용히 늘어납니다. routing policy는 task difficulty, confidence, risk tier, context length, tool requirement를 보고 작은 모델, 큰 모델, verifier, human review 중 하나를 선택해야 합니다. 특히 작은 모델이 낮은 confidence를 높은 확신처럼 반환하는 경우가 위험하므로, route decision log와 fallback rate를 함께 봐야 비용 절감이 품질 퇴행을 숨기지 않습니다.</p>\n<h3>MARKER: artifact-ready — Customization dataset card</h3>\n<pre><code>dataset: support-category-v4\nsource: reviewed tickets only\nredaction: pii_removed + tenant_id_hashed\nlabel_policy: category rubric v3\nholdout: 20% human-labeled, never used in training\nblocked_data: secrets, legal privileged text, deleted user records</code></pre>\n</section>",
};

export default document;
