import type { HandbookDocumentContent } from "../types";

const document: HandbookDocumentContent = {
  navHtml: "<div class=\"nav-brand\">ENGINEERING · CS FOUNDATIONS</div>\n  <div class=\"nav-title\">복잡도 · 자료구조 · 증명 · 벤치마크</div>\n  <a href=\"#spine\"><span class=\"code\">CS-01</span>CS FOUNDATION SPINE</a>\n  <a href=\"#complexity\"><span class=\"code\">CS-02</span>복잡도와 비용 모델</a>\n  <a href=\"#structures\"><span class=\"code\">CS-03</span>자료구조 선택</a>\n  <a href=\"#proof\"><span class=\"code\">CS-04</span>INVARIANT PROOF DRILL</a>\n  <a href=\"#production\"><span class=\"code\">CS-05</span>PRODUCTION ALGORITHM DECISION</a>\n  <a href=\"#benchmark\"><span class=\"code\">CS-06</span>BENCHMARK EVIDENCE PACKET</a>\n  <a href=\"#failure\"><span class=\"code\">CS-07</span>실패 모드와 운영 방어선</a>",
  mainHtml: "<header class=\"hero\">\n  <div class=\"hero-serial\"><span>DOC : ENGINEERING-CS-FOUNDATIONS</span><span>SCOPE : COMPLEXITY · DATA STRUCTURES · PROOFS · BENCHMARKS</span><span>LANG : KO</span></div>\n  <h1>CS 기초: 알고리즘을 실무 판단으로 바꾸기</h1>\n  <p class=\"hero-sub\">CS 기초는 면접용 암기가 아니라 <strong>데이터 크기, 접근 패턴, 불변식, 실패 비용을 근거로 구현을 고르는 언어</strong>입니다. 이 문서는 복잡도를 자릿수 계산으로, 자료구조를 실코드로, 증명을 PR 리뷰 문장으로, 벤치마크를 복붙 가능한 harness로 바꿉니다.</p>\n  <div class=\"hero-meta\">AUDIENCE : 제품 엔지니어 · 백엔드/프론트엔드 공통 · UPDATED 2026-07</div>\n</header>\n\n<section id=\"spine\">\n<div class=\"ch-head\"><span class=\"ch-code\">CS-01</span><h2>CS FOUNDATION SPINE</h2></div>\n<p class=\"lede\">알고리즘 선택은 \"가장 빠른 알고리즘\"을 찾는 일이 아닙니다. 실제 입력 분포, 메모리 한계, 캐시 지역성, 구현 위험, 운영 관측성을 함께 보며 충분히 좋은 선택을 고정하는 일입니다.</p>\n<div class=\"semantic-card process-card\"><span class=\"sc-label\">CS FOUNDATION SPINE</span><br>problem contract → input scale → operation mix → data structure invariant<br>→ asymptotic bound → constant factor → memory layout<br>→ correctness sketch → benchmark evidence → failure playbook</div>\n<table>\n<tr><th>질문</th><th>확인할 것</th><th>산출물</th></tr>\n<tr><td>입력은 얼마나 커지는가</td><td>현재 p50/p99 크기, 1년 성장률, 최악 요청 크기</td><td>크기 표와 제한값 (예: \"태그 룰은 현재 120개, 연 2배 성장, 상한 5,000\")</td></tr>\n<tr><td>무슨 연산이 지배적인가</td><td>읽기/쓰기/탐색/정렬/삭제 비율</td><td>operation mix 표 (예: 조회 10⁴/일 vs 갱신 10/일 → 읽기 최적화)</td></tr>\n<tr><td>정확성의 핵심은 무엇인가</td><td>정렬 순서, 중복 금지, 연결성, 도달성, 단조성</td><td>불변식 한 문장</td></tr>\n<tr><td>성능 병목은 어디인가</td><td>CPU, 메모리, 할당, I/O, lock 경합</td><td>프로파일링 기록</td></tr>\n</table>\n<h3><span class=\"h3-tag\">RULE</span>판단 부등식 세 개</h3>\n<p>이 문서 전체를 관통하는 판단은 부등식 세 개로 요약됩니다. 감이 아니라 이 부등식의 양변에 숫자를 넣을 수 있어야 결정입니다.</p>\n<ul>\n<li><strong>인덱스 구축</strong>: 조회 횟수를 q라 할 때 <code>n + q·1</code> (구축 + hash 조회) <code>&lt;</code> <code>q·n</code> (매번 스캔) — <em>q ≥ 2면 거의 항상 인덱스가 이긴다</em>. q = 1이면 스캔이 이긴다.</li>\n<li><strong>정렬 후 이분탐색</strong>: <code>n log n + q log n</code> <code>&lt;</code> <code>q·n</code> — q가 log n보다 크면 정렬이 회수된다.</li>\n<li><strong>캐시 도입</strong>: <code>hit율 × 절감 시간</code> <code>&gt;</code> <code>miss 비용 + 무효화 버그 위험 + 메모리 상한 관리 비용</code> — 우변을 0으로 치는 순간 stale 장애가 시작된다.</li>\n</ul>\n<div class=\"callout\"><span class=\"co-label\">실무 기준</span><p>복잡도는 출발점이고 벤치마크는 결론입니다. <code>O(n log n)</code>이 <code>O(n)</code>보다 느리다는 말은 같은 상수와 같은 메모리 접근 조건에서만 대략 맞습니다. n이 수백이면 연속 메모리를 도는 단순 배열 스캔이 포인터를 쫓는 \"이론적 최적해\"를 이기는 경우가 흔합니다. 교차점은 CS-06의 harness로 직접 잽니다.</p></div>\n</section>\n\n<section id=\"complexity\">\n<div class=\"ch-head\"><span class=\"ch-code\">CS-02</span><h2>복잡도와 비용 모델</h2></div>\n<p class=\"lede\">Big-O는 증가율의 언어입니다. 실무에서 쓸모 있으려면 두 가지 환산이 필요합니다: 표기를 <strong>연산 수 자릿수</strong>로, 연산 수를 <strong>시간 예산</strong>으로. 이 절의 모든 숫자는 \"단순 연산 1회 ≈ 10ns (10⁸ ops/s)\"라는 보수적 비용 모델 기준의 자릿수 감각이며, 실제 상수는 연산 종류에 따라 10~100배 달라집니다.</p>\n<table>\n<tr><th>표기</th><th>n = 1,000</th><th>n = 1,000,000</th><th>10ms 예산에서 허용되는 n</th><th>실무 예</th></tr>\n<tr><td><code>O(1)</code></td><td>1</td><td>1</td><td>n과 무관 (단, 원격 호출이면 상수가 10⁵배)</td><td>hash map lookup, 배열 인덱스</td></tr>\n<tr><td><code>O(log n)</code></td><td>~10</td><td>~20</td><td>사실상 무제한</td><td>binary search, B-tree index</td></tr>\n<tr><td><code>O(n)</code></td><td>10³</td><td>10⁶ (≈ 10ms)</td><td>~10⁶</td><td>filter, validation pass</td></tr>\n<tr><td><code>O(n log n)</code></td><td>~10⁴</td><td>~2×10⁷ (≈ 200ms)</td><td>~5×10⁴</td><td>sort, merge</td></tr>\n<tr><td><code>O(n²)</code></td><td>10⁶ (≈ 10ms)</td><td>10¹² (<strong>≈ 3시간</strong>)</td><td>~1,000</td><td>중첩 루프 join, 거리 행렬</td></tr>\n</table>\n<div class=\"semantic-card evidence-card\"><span class=\"sc-label\">예산 계산 절차</span><br>1. 시간 예산을 정한다 — 예: API p99 100ms 중 이 함수 몫 10ms<br>2. 예산을 연산 수로 바꾼다 — 10ms ÷ 10ns = 10⁶ 회<br>3. 복잡도 식을 뒤집어 허용 n을 구한다 — O(n²)이면 n ≤ √10⁶ = 1,000<br>4. p99 입력 크기와 비교한다 — p99 n = 5,000이면 이 설계는 예산을 25배 초과<br>5. 초과하면 알고리즘을 바꾸거나(CS-05), 입력을 제한한다(CS-07)</div>\n<h3><span class=\"h3-tag\">MODEL</span>숨은 비용: 연산은 평등하지 않다</h3>\n<p>복잡도 계산에서 \"연산 1회\"는 보통 CPU 명령 몇 개를 가정합니다. 하지만 루프 본문이 원격 호출이면 상수가 자릿수로 다섯 개 뛰고, Big-O가 같아도 성능은 완전히 다른 문제가 됩니다.</p>\n<table>\n<tr><th>연산</th><th>대략 비용</th><th>단순 연산(10ns) 환산</th></tr>\n<tr><td>단순 연산 / L1 cache 참조</td><td>~1–10ns</td><td>기준 ×1</td></tr>\n<tr><td>main memory 접근 (cache miss)</td><td>~100ns</td><td>×10</td></tr>\n<tr><td>SSD random read</td><td>~100µs</td><td>×10⁴</td></tr>\n<tr><td>같은 데이터센터 round trip + 인덱스 hit DB 쿼리</td><td>~0.5–2ms</td><td><strong>×10⁵ — 루프 안 DB 호출 1회 ≈ 로컬 연산 수십만 회</strong></td></tr>\n<tr><td>리전 간 round trip</td><td>~100ms</td><td>×10⁷</td></tr>\n</table>\n<pre class=\"snippet-card\"><span class=\"sc-label\">TS · N+1 — Big-O가 아니라 상수가 죽이는 경우</span>\n// BAD — N+1 query: 복잡도는 O(n)이지만 상수가 1ms.\n// n = 200이면 네트워크 왕복만 200 × 1ms = 200ms. p99 예산 100ms 초과.\nfor (const order of orders) {\n  const customer = await db.customer.findUnique({\n    where: { id: order.customerId },\n  });\n  enrich(order, customer);\n}\n\n// GOOD — 2 query + 메모리 join: 왕복 2회 ≈ 2ms, 나머지는 로컬 O(n).\nconst customerIds = [...new Set(orders.map((o) =&gt; o.customerId))];\nconst customers = await db.customer.findMany({\n  where: { id: { in: customerIds } },\n});\nconst customersById = new Map(customers.map((c) =&gt; [c.id, c]));\nfor (const order of orders) {\n  enrich(order, customersById.get(order.customerId));\n}</pre>\n<div class=\"callout warn\"><span class=\"co-label\">숨은 비용 리뷰 규칙</span><p>코드 리뷰에서 Big-O보다 먼저 표시할 것: 루프 안 <code>await</code>·원격 호출, 루프 안 정렬, 요청별 전체 스캔, 큰 자료구조 복사(spread로 매 반복 새 배열), 무제한 재귀. 이 다섯은 표기상 복잡도가 낮아도 상수 또는 실효 복잡도가 폭발하는 지점입니다. 예: 루프 안 <code>[...acc, item]</code> 누적은 O(n)처럼 보이지만 복사 때문에 O(n²)입니다.</p></div>\n<div class=\"checklist\"><ul>\n<li>상한 입력 크기를 API, DB, UI 경계에서 모두 제한한다 (CS-07의 guard 코드 참조).</li>\n<li>평균이 아니라 p95/p99 입력 크기로 시간 예산을 계산한다 — 예산 계산 절차 2~4단계.</li>\n<li>중첩 루프는 한쪽 크기 상한이 코드나 스키마로 강제될 때만 허용한다 (\"보통 작다\"는 근거가 아니다).</li>\n<li>캐시를 추가할 때 무효화 비용과 메모리 상한을 같이 쓴다 (CS-01 캐시 부등식).</li>\n</ul></div>\n</section>\n\n<section id=\"structures\">\n<div class=\"ch-head\"><span class=\"ch-code\">CS-03</span><h2>자료구조 선택</h2></div>\n<p class=\"lede\">자료구조는 불변식을 저장하는 방식입니다. 어떤 연산을 빠르게 만들면 보통 다른 연산, 메모리, 구현 복잡도가 비용으로 따라옵니다. 선택 기준은 \"무슨 연산이 몇 번 일어나는가\"이지 익숙함이 아닙니다.</p>\n<table>\n<tr><th>자료구조</th><th>핵심 연산 비용</th><th>불변식</th><th>실패 모드</th></tr>\n<tr><td>Array / Vector</td><td>인덱스 O(1), 끝 삽입 amortized O(1), 중간 삽입/탐색 O(n)</td><td>연속 메모리, 순서 유지</td><td><code>includes</code>/<code>indexOf</code>를 루프 안에서 호출해 O(n²) (아래 코드)</td></tr>\n<tr><td>Hash Map / Set</td><td>조회·삽입·삭제 평균 O(1), 최악 O(n)</td><td>키 해시와 동등성이 안정적</td><td>mutable key, 나쁜 <code>hashCode</code>, 무제한 성장(→ LRU로 상한)</td></tr>\n<tr><td>Tree / B-tree</td><td>조회·삽입 O(log n), 범위 검색 O(log n + k)</td><td>정렬 순서, 균형</td><td>비교자 불일치, 포인터 추적으로 cache miss 누적</td></tr>\n<tr><td>Heap / Priority Queue</td><td>peek O(1), push/pop O(log n)</td><td>부모가 자식보다 우선</td><td>임의 삭제 O(n), stale entry (lazy deletion 필요)</td></tr>\n<tr><td>Graph</td><td>BFS/DFS O(V + E)</td><td>노드와 엣지 관계</td><td>방문 표시 누락 → cycle에서 무한 탐색</td></tr>\n<tr><td>Bloom Filter</td><td>조회·삽입 O(k), 공간 원소당 ~10bit</td><td>false positive 허용, false negative 없음</td><td>\"없다고 하면 정말 없다\"를 뒤집어 기대하면 설계 오류</td></tr>\n</table>\n<h3><span class=\"h3-tag\">CODE</span>중복 제거 — 루프 안 <code>includes</code>는 O(n²)</h3>\n<p>가장 흔한 실무 O(n²)는 중첩 <code>for</code>가 아니라 <em>루프 안에 숨은 선형 탐색</em>입니다. <code>includes</code>, <code>indexOf</code>, <code>find</code>, Java의 <code>List.contains</code>가 모두 O(n)이라는 사실이 코드 모양에 드러나지 않습니다.</p>\n<pre class=\"snippet-card\"><span class=\"sc-label\">TS · dedupe — O(n²) 함정과 O(n) 교정</span>\n// BAD — out.includes가 매번 out을 처음부터 스캔한다 → 전체 O(n²).\n// n = 50,000이면 최악 비교 횟수 ≈ n²/2 = 1.25 × 10⁹ 회 → 초 단위.\nfunction dedupeSlow(ids: string[]): string[] {\n  const out: string[] = [];\n  for (const id of ids) {\n    if (!out.includes(id)) out.push(id); // 루프 안 O(n) 선형 탐색\n  }\n  return out;\n}\n\n// GOOD — Set 조회는 평균 O(1) → 전체 O(n).\n// 불변식: k번째 반복이 끝나면 seen === ids[0..k)의 고유값 집합이고\n//         out은 그 고유값을 첫 등장 순서로 담는다.\nfunction dedupe(ids: string[]): string[] {\n  const seen = new Set&lt;string&gt;();\n  const out: string[] = [];\n  for (const id of ids) {\n    if (seen.has(id)) continue;\n    seen.add(id);\n    out.push(id);\n  }\n  return out;\n}</pre>\n<pre class=\"snippet-card\"><span class=\"sc-label\">JAVA · 같은 함정, 같은 교정 + 반복 조회용 인덱스</span>\n// BAD — List.contains는 O(n) → 전체 O(n²)\nList&lt;String&gt; out = new ArrayList&lt;&gt;();\nfor (String id : ids) {\n    if (!out.contains(id)) out.add(id);\n}\n\n// GOOD — LinkedHashSet: 평균 O(1) 조회 + 입력 순서 유지 → 전체 O(n)\n// 불변식: unique는 ids의 고유값을 첫 등장 순서로 담는다\nSet&lt;String&gt; unique = new LinkedHashSet&lt;&gt;(ids);\n\n// 반복 조회가 있다면 스캔하지 말고 인덱스를 한 번 구축한다.\n// 구축 O(n), 이후 조회 평균 O(1) — CS-01 부등식: 조회 2회 이상이면 이득.\n// 불변식: index.get(customerId)는 orders 중 해당 고객의 주문 전체를\n//         원래 순서대로 담는다 (groupingBy는 encounter order 유지).\nMap&lt;Long, List&lt;Order&gt;&gt; index = orders.stream()\n    .collect(Collectors.groupingBy(Order::getCustomerId));</pre>\n<h3><span class=\"h3-tag\">CODE</span>LRU cache — 자료구조 성질을 그대로 쓰는 20줄</h3>\n<p>LRU는 \"hash 조회 O(1) + 사용 순서 유지\"라는 두 요구의 결합입니다. JS <code>Map</code>과 Java <code>LinkedHashMap</code>은 둘 다 이 결합을 언어 차원에서 제공하므로, 직접 doubly linked list를 짤 이유가 없습니다.</p>\n<pre class=\"snippet-card\"><span class=\"sc-label\">TS · Map 기반 LRU — 삽입 순서가 곧 사용 순서</span>\n// Map은 삽입 순서를 유지한다 — 이 성질이 구현의 전부다.\n// 불변식 1: map의 key 순서는 \"가장 오래 안 씀 → 가장 최근 씀\".\n// 불변식 2: map.size ≤ capacity (set이 반환되는 시점마다 성립).\nclass LruCache&lt;K, V&gt; {\n  private readonly map = new Map&lt;K, V&gt;();\n  constructor(private readonly capacity: number) {}\n\n  get(key: K): V | undefined {\n    if (!this.map.has(key)) return undefined;\n    const value = this.map.get(key)!;\n    this.map.delete(key);     // 삭제 후 재삽입 = 맨 뒤(가장 최근)로 이동\n    this.map.set(key, value); // → 불변식 1 유지\n    return value;\n  }\n\n  set(key: K, value: V): void {\n    if (this.map.has(key)) this.map.delete(key);\n    this.map.set(key, value);\n    if (this.map.size &gt; this.capacity) {\n      // 불변식 1에 의해 keys()의 첫 원소가 eviction 대상\n      this.map.delete(this.map.keys().next().value!);\n    }\n  }\n}</pre>\n<pre class=\"snippet-card\"><span class=\"sc-label\">JAVA · LinkedHashMap 기반 LRU</span>\n// accessOrder = true: get/put마다 entry를 순서 맨 뒤로 보낸다.\n// removeEldestEntry가 true를 반환하면 맨 앞(가장 오래 안 쓴) entry 제거.\n// 불변식: size() ≤ capacity, iteration 순서 = LRU → MRU.\n// 주의: 동기화 없음 — 동시 접근 경로면 Caffeine 같은 검증된 라이브러리로.\nclass LruCache&lt;K, V&gt; extends LinkedHashMap&lt;K, V&gt; {\n    private final int capacity;\n\n    LruCache(int capacity) {\n        super(16, 0.75f, true); // 세 번째 인자가 accessOrder\n        this.capacity = capacity;\n    }\n\n    @Override\n    protected boolean removeEldestEntry(Map.Entry&lt;K, V&gt; eldest) {\n        return size() &gt; capacity;\n    }\n}</pre>\n<h3><span class=\"h3-tag\">RULE</span>운영 데이터 구조</h3>\n<p>생산 코드에서 자료구조는 성능만 담당하지 않습니다. 중복 요청 방지용 set, 위의 LRU cache, rate limit counter, 작업 큐, topology graph처럼 <em>운영 정책을 코드로 고정</em>합니다. 이때 선택 문서에는 용량 상한(위 코드의 <code>capacity</code>), TTL, eviction 정책, 동시성 정책이 함께 있어야 합니다. \"cache를 쓴다\"는 결정이 아니고 \"capacity 10,000, LRU eviction, 무효화는 write-through\"가 결정입니다.</p>\n</section>\n\n<section id=\"proof\">\n<div class=\"ch-head\"><span class=\"ch-code\">CS-04</span><h2>INVARIANT PROOF DRILL</h2></div>\n<p class=\"lede\">증명은 형식 논문이 아니라 \"왜 이 코드가 깨지지 않는지\"를 짧게 설명하는 훈련입니다. 실무에서는 불변식, 초기화, 보존, 종료를 PR 설명에 넣을 수 있으면 충분합니다. 아래 binary search는 이 네 단계가 코드 주석으로 그대로 들어간 예입니다.</p>\n<div class=\"semantic-card process-card\"><span class=\"sc-label\">INVARIANT PROOF DRILL</span><br>1. invariant: 루프/상태 전이 전후로 항상 참인 문장을 쓴다<br>2. initialization: 시작 전에 invariant가 참임을 보인다<br>3. preservation: 한 단계 실행 후에도 참임을 보인다<br>4. termination: 끝났을 때 invariant가 원하는 결과를 의미함을 보인다<br>5. counterexample: 깨지는 입력을 하나 만들어 테스트로 고정한다</div>\n<h3><span class=\"h3-tag\">CODE</span>binary search — 반열림 <code>[lo, hi)</code> 불변식</h3>\n<pre class=\"snippet-card\"><span class=\"sc-label\">TS · lowerBound — 불변식이 주석으로 증명되는 코드</span>\n// lowerBound: target 이상인 값이 처음 나오는 인덱스 (없으면 arr.length).\n// 전제: arr는 오름차순 정렬.\n// 불변식 (루프 전후 항상 참):\n//   (a) arr[0..lo)  의 모든 값 &lt; target   — lo 왼쪽은 전부 탈락 확정\n//   (b) arr[hi..n)  의 모든 값 ≥ target   — hi부터 오른쪽은 전부 정답 후보 이상\n// 초기화: lo=0, hi=n이면 두 구간 모두 빈 구간이므로 (a)(b) 자명하게 참.\nfunction lowerBound(arr: number[], target: number): number {\n  let lo = 0;\n  let hi = arr.length; // 반열림: hi 자체는 후보 구간에 포함되지 않는다\n  while (lo &lt; hi) {\n    const mid = lo + ((hi - lo) &gt;&gt; 1); // (lo+hi)/2의 overflow 방어 습관\n    if (arr[mid] &lt; target) {\n      lo = mid + 1; // 보존: arr[mid] &lt; target이므로 (a)가 mid까지 확장\n    } else {\n      hi = mid;     // 보존: arr[mid] ≥ target이므로 (b)가 mid까지 확장\n    }\n    // 종료 보장: mid ∈ [lo, hi-1]이므로 두 분기 모두 구간을 최소 1 줄인다\n  }\n  // 종료: lo === hi. (a)와 (b)가 그 지점에서 만나므로\n  // lo는 \"target 이상이 시작되는 첫 인덱스\" — 곧 삽입 위치다.\n  return lo;\n}</pre>\n<pre class=\"snippet-card\"><span class=\"sc-label\">TS · 나쁜 예 — 닫힌 구간 초기화 + 반열림 축소의 오프바이원</span>\n// BAD — 구간 규약이 섞였다: 초기화는 닫힌 구간 [lo, hi],\n// 축소는 반열림 규칙(hi = mid). 불변식 (b) \"arr[hi..n) ≥ target\"이\n// 초기화 시점부터 거짓이다 — arr[n-1]이 target 미만일 수 있으므로.\nfunction lowerBoundBroken(arr: number[], target: number): number {\n  let lo = 0;\n  let hi = arr.length - 1; // 닫힌 구간으로 시작해 놓고\n  while (lo &lt; hi) {\n    const mid = (lo + hi) &gt;&gt; 1;\n    if (arr[mid] &lt; target) lo = mid + 1;\n    else hi = mid;         // 반열림 규칙으로 줄인다 → 규약 불일치\n  }\n  return lo;\n}\n// counterexample: lowerBoundBroken([1, 3], 5)\n//   기대값 2 (\"없음\" = arr.length) → 실제 1을 반환.\n//   hi = length - 1 초기화가 \"마지막 원소 뒤\"라는 정답 후보를\n//   시작부터 배제했다. 오프바이원 버그의 뿌리는 ±1이 아니라\n//   구간 규약(닫힘/반열림)을 한 함수 안에서 섞은 것이다.</pre>\n<div class=\"callout risk\"><span class=\"co-label\">오프바이원의 진짜 원인</span><p>binary search 버그는 <code>mid + 1</code>이냐 <code>mid</code>냐를 외워서 고치는 게 아닙니다. <em>구간 규약을 하나 정하고(반열림 권장), 초기화·축소·종료가 모두 그 규약을 따르는지</em> 확인하면 끝납니다. 테스트는 counterexample 계열을 고정합니다: 빈 배열, 한 원소, 전부 target 미만, 전부 target 이상, 중복값 경계.</p></div>\n<h3><span class=\"h3-tag\">CODE</span>two pointers — \"탈락은 안전하다\"를 증명하기</h3>\n<pre class=\"snippet-card\"><span class=\"sc-label\">TS · 정렬 배열에서 합이 target인 쌍 — O(n)</span>\n// 전제: arr는 오름차순 정렬.\n// 불변식: 합이 target인 쌍이 존재한다면, 그 쌍의 두 인덱스는\n//         항상 현재 구간 [lo, hi] 안에 있다.\n// lo 탈락의 근거: arr[lo] + arr[hi] &lt; target일 때 hi는 lo가 만날 수 있는\n//   최대 파트너다. 최대와도 부족하면 arr[lo]는 어떤 쌍도 못 만든다 → 안전.\nfunction findPairSum(arr: number[], target: number): [number, number] | null {\n  let lo = 0;\n  let hi = arr.length - 1;\n  while (lo &lt; hi) {\n    const sum = arr[lo] + arr[hi];\n    if (sum === target) return [lo, hi];\n    if (sum &lt; target) lo++; // arr[lo] 탈락 — 위 근거로 불변식 보존\n    else hi--;              // 대칭 논리: 최소 파트너와도 초과 → arr[hi] 탈락\n  }\n  return null; // 종료: 후보 구간 소진 = \"쌍이 없다\"가 증명된 상태\n}\n// 이중 루프 O(n²) 대비: n = 100,000이면 5×10⁹ 회 → 10⁵ 회. 자릿수 4개 차이.</pre>\n<table>\n<tr><th>패턴</th><th>불변식 예</th><th>검증 테스트 (counterexample 계열)</th></tr>\n<tr><td>binary search</td><td>정답은 항상 반열림 <code>[lo, hi)</code> 안에 있다</td><td>빈 배열, 한 원소, 전부 미만/이상, 중복 경계</td></tr>\n<tr><td>two pointers</td><td>탈락한 원소는 어떤 정답에도 참여할 수 없다</td><td>모두 통과, 모두 실패, 포인터 교차 직전</td></tr>\n<tr><td>BFS</td><td>큐에서 꺼낸 노드의 거리는 최단 거리로 확정됐다</td><td>cycle, disconnected, 여러 최단 경로</td></tr>\n<tr><td>transaction state machine</td><td>저장된 상태는 허용된 전이의 결과뿐이다</td><td>중복 요청, 역전이, 동시 전이</td></tr>\n</table>\n<div class=\"callout gm\"><span class=\"co-label\">리뷰 문장</span><p>\"이 함수는 정렬된 배열을 전제로 하며, 각 반복에서 정답 후보 구간을 절반으로 줄입니다. 구간은 반열림 <code>[lo, hi)</code>로 유지하고, 종료 시 <code>lo == hi</code>이므로 삽입 위치가 됩니다.\" — PR 설명에 이 세 문장이 있으면 리뷰어는 구현 의도, 전제, 경계 조건을 한 번에 검증할 수 있습니다.</p></div>\n</section>\n\n<section id=\"production\">\n<div class=\"ch-head\"><span class=\"ch-code\">CS-05</span><h2>PRODUCTION ALGORITHM DECISION</h2></div>\n<p class=\"lede\">프로덕션 알고리즘 선택은 \"교과서상 최적\"보다 \"충분히 빠르고, 설명 가능하며, 장애 시 완화 가능한가\"가 중요합니다. 그리고 대부분의 회귀는 새 알고리즘이 아니라 <em>데이터가 자라서 예전 결정이 무효가 되는 것</em>에서 옵니다.</p>\n<h3><span class=\"h3-tag\">CASE</span>태그 매칭 — 이중 루프가 데이터 성장에 깔리는 시나리오</h3>\n<div class=\"semantic-card evidence-card\"><span class=\"sc-label\">CASE · 사례 시나리오 (비용 모델 추정, 실측 아님)</span><br><strong>증상</strong>: 주문-태그 룰 매칭 API의 p99가 배포 없이 몇 달에 걸쳐 30ms → 400ms로 상승. p50은 거의 그대로.<br><strong>증거</strong>: 트래픽은 정체. 주문 배치 크기 n과 태그 룰 수 m이 각각 3배 성장. profiler에서 <code>matchOrders</code>가 CPU의 85%.<br><strong>원인</strong>: O(n·m) 이중 루프. n = 10,000, m = 100이면 내부 반복 10⁶회 — 10ns/op 모델로 수십 ms. 3배 성장 후 n·m = 9×10⁶ → 9배인 수백 ms. <em>p99만 튀는 이유: 가장 큰 배치가 꼬리를 만든다.</em><br><strong>조치</strong>: 룰을 tagId로 인덱싱해 O(n·k + m)로 (k = 주문당 태그 수 ≈ 5). 연산 수 ~5×10⁴ → ms 미만 규모. 자릿수 두 개 감소.</div>\n<pre class=\"snippet-card\"><span class=\"sc-label\">TS · Before — O(n·m·k): 주문마다 전체 룰 스캔</span>\n// n = 주문 수, m = 룰 수, k = 주문당 태그 수.\n// includes까지 세면 n·m·k. 출시 시점(n=1,000, m=30)엔 3×10⁴로 무해했다.\n// 문제는 코드가 아니라 \"n과 m이 함께 자란다\"는 사실이 결정에 없던 것.\nfunction matchOrders(orders: Order[], rules: TagRule[]): Match[] {\n  const matches: Match[] = [];\n  for (const order of orders) {          // n\n    for (const rule of rules) {          // × m\n      if (order.tagIds.includes(rule.tagId)) { // × k\n        matches.push({ orderId: order.id, ruleId: rule.id });\n      }\n    }\n  }\n  return matches;\n}</pre>\n<pre class=\"snippet-card\"><span class=\"sc-label\">TS · After — O(n·k + m): 룰을 tagId로 한 번 인덱싱</span>\n// 불변식: rulesByTag.get(tagId)는 해당 tagId를 갖는 룰 전체, 입력 순서 유지.\n// CS-01 인덱스 부등식: 구축 m회 + 조회 n·k회 ≪ 스캔 n·m·k회 (m ≫ 1이면 항상).\nfunction matchOrders(orders: Order[], rules: TagRule[]): Match[] {\n  const rulesByTag = new Map&lt;string, TagRule[]&gt;();\n  for (const rule of rules) {\n    const bucket = rulesByTag.get(rule.tagId) ?? [];\n    bucket.push(rule);\n    rulesByTag.set(rule.tagId, bucket);\n  }\n  const matches: Match[] = [];\n  for (const order of orders) {\n    for (const tagId of order.tagIds) {          // n × k\n      for (const rule of rulesByTag.get(tagId) ?? []) {\n        matches.push({ orderId: order.id, ruleId: rule.id });\n      }\n    }\n  }\n  return matches;\n}</pre>\n<div class=\"callout warn\"><span class=\"co-label\">수치의 지위</span><p>위 ms 수치는 10ns/op 비용 모델로 계산한 <em>자릿수 추정</em>이지 실측이 아닙니다. 결정 기록에 넣을 최종 수치는 CS-06의 harness로 실제 입력 분포에서 잰 p50/p95여야 합니다. 추정은 \"재볼 가치가 있는가\"를 정하는 필터입니다.</p></div>\n<table>\n<tr><th>선택지</th><th>고르는 상황</th><th>피해야 할 상황</th><th>운영 증거</th></tr>\n<tr><td>단순 스캔</td><td>n 상한이 강제되고(예: ≤ 500) 코드 경로가 드묾</td><td>요청마다 큰 컬렉션 스캔</td><td>입력 상한, p99 시간</td></tr>\n<tr><td>정렬 후 처리</td><td>조회 q &gt; log n (CS-01 부등식)</td><td>매 요청마다 같은 정렬 반복</td><td>정렬 키, 안정성 필요 여부</td></tr>\n<tr><td>인덱스/맵 구성</td><td>반복 lookup ≥ 2회 — 위 CASE</td><td>한 번만 조회하고 버림</td><td>메모리 사용량, 갱신 정책</td></tr>\n<tr><td>근사 알고리즘</td><td>정확도보다 latency와 비용이 중요 (검색 추천, count 추정)</td><td>정산, 권한, 보안 판정</td><td>오차 범위, 재현 가능한 평가셋</td></tr>\n<tr><td>오프라인 전처리</td><td>읽기 경로 latency가 중요하고 stale 수 분 허용</td><td>신선도 요구가 매우 높음</td><td>빌드 주기, stale 허용 시간</td></tr>\n</table>\n<div class=\"semantic-card decision-card\"><span class=\"sc-label\">PRODUCTION ALGORITHM DECISION</span><br>Decision record fields<br>- problem and correctness invariant<br>- input distribution and hard limit — <em>그리고 성장률: 위 CASE의 누락 항목</em><br>- candidate algorithms and rejected alternatives<br>- measured latency, allocation, CPU, memory (CS-06 harness 출력 첨부)<br>- fallback when input exceeds budget (CS-07 guard와 연결)<br>- monitoring metric and rollback trigger — 예: \"p99 &gt; 100ms 5분 지속 시 알림\"</div>\n<div class=\"callout risk\"><span class=\"co-label\">면접식 최적화의 함정</span><p>작은 팀 서비스에서 복잡한 lock-free 구조나 직접 구현한 balanced tree를 넣는 것은 성능보다 유지보수 리스크가 큽니다. 표준 라이브러리, DB 인덱스, 검증된 라이브러리를 먼저 쓰고, 직접 구현은 <em>측정된 병목 + 대안 부재</em> 두 조건이 모두 확인됐을 때만 선택합니다. 위 CASE의 해법도 <code>Map</code> 하나였지 자료구조 발명이 아니었습니다.</p></div>\n</section>\n\n<section id=\"benchmark\">\n<div class=\"ch-head\"><span class=\"ch-code\">CS-06</span><h2>BENCHMARK EVIDENCE PACKET</h2></div>\n<p class=\"lede\">벤치마크는 숫자를 만드는 절차가 아니라 결정을 방어하는 증거 패킷입니다. 재현 가능하고, 비교 조건이 공정하며, 실제 입력을 닮아야 합니다. 아래 harness는 그대로 파일에 붙여 <code>node bench.mjs</code>로 실행됩니다.</p>\n<div class=\"semantic-card evidence-card\"><span class=\"sc-label\">BENCHMARK EVIDENCE PACKET</span><br>hypothesis → fixture data → warmup → measured run<br>→ p50/p95/max → allocation/GC → profiler snapshot<br>→ regression threshold → decision note</div>\n<h3><span class=\"h3-tag\">CODE</span>최소 harness — warmup + 반복 + percentile</h3>\n<pre class=\"snippet-card\"><span class=\"sc-label\">JS · bench.mjs — node bench.mjs 로 실행</span>\nimport { performance } from 'node:perf_hooks';\n\nfunction bench(name, fn, { warmup = 50, runs = 200 } = {}) {\n  for (let i = 0; i &lt; warmup; i++) fn(); // JIT/캐시 예열 — 측정에서 제외\n  const samples = [];\n  for (let i = 0; i &lt; runs; i++) {\n    const t0 = performance.now();\n    fn();\n    samples.push(performance.now() - t0);\n  }\n  samples.sort((a, b) =&gt; a - b);\n  const at = (q) =&gt; samples[Math.min(samples.length - 1, Math.floor(samples.length * q))];\n  console.log(\n    `${name.padEnd(20)} p50=${at(0.5).toFixed(3)}ms` +\n    `  p95=${at(0.95).toFixed(3)}ms  max=${samples[samples.length - 1].toFixed(3)}ms`\n  );\n}\n\n// fixture: 실제 분포를 닮게 — 여기선 고유값 1,000종이 반복되는 50,000건\nconst input = Array.from({ length: 50_000 }, (_, i) =&gt; `id-${i % 1_000}`);\nlet sink = 0; // 결과를 소비해 dead code elimination 방지\n\nbench('includes-dedupe', () =&gt; {\n  const out = []; for (const v of input) if (!out.includes(v)) out.push(v);\n  sink += out.length;\n});\nbench('set-dedupe', () =&gt; { sink += new Set(input).size; });\nconsole.log('sink =', sink); // sink를 출력해야 최적화로 증발하지 않는다</pre>\n<h3><span class=\"h3-tag\">READ</span>결과 해석 — 숫자에서 결정으로</h3>\n<table>\n<tr><th>관찰 (예시 수치)</th><th>해석</th><th>다음 행동</th></tr>\n<tr><td>includes p50 = 38ms vs set p50 = 0.6ms</td><td>~60배 — 알고리즘 등급 차이(O(n²) vs O(n)). 상수 튜닝으로 못 좁힌다</td><td>set 채택. Decision record에 두 수치와 fixture 조건 기록</td></tr>\n<tr><td>p95/p50 비율 ≤ 1.3</td><td>측정 노이즈 적정 — 분포가 안정적</td><td>그대로 판정에 사용</td></tr>\n<tr><td>p95/p50 비율 &gt; 2</td><td>GC, 절전 모드, 백그라운드 프로세스 개입 의심</td><td>runs 증가, 전원 연결·부하 없는 상태에서 재측정</td></tr>\n<tr><td>max가 p95의 5배 이상</td><td>warmup 부족(첫 실행 JIT) 또는 간헐 GC pause</td><td>warmup 증가 후 재측정. 그래도 남으면 allocation 프로파일 확인</td></tr>\n<tr><td>두 후보 차이가 p50 기준 20% 미만</td><td>이 fixture에서는 사실상 동급</td><td>단순한 쪽 채택 — 복잡도는 그 자체가 비용</td></tr>\n</table>\n<div class=\"callout warn\"><span class=\"co-label\">MICROBENCHMARK 함정</span><p>결과를 소비하지 않으면(위 코드의 <code>sink</code>) 엔진이 계산 자체를 제거해 \"0.001ms의 기적\"이 나옵니다. 그 외 필수 조건: 같은 프로세스에서 후보 순서를 바꿔도 결과가 유지되는지 확인, 노트북 전원 연결, fixture는 랜덤 균등 분포가 아니라 실제 로그에서 샘플. Java라면 이 수제 harness 대신 JMH를 씁니다 — JVM warmup과 dead code 문제를 도구가 처리합니다.</p></div>\n<table>\n<tr><th>항목</th><th>필수 내용</th><th>흔한 오류</th></tr>\n<tr><td>데이터셋</td><td>작은/보통/p99/최악 입력을 분리해 각각 측정</td><td>랜덤 균등 분포만 사용 — hash 충돌·중복률·정렬도가 실제와 다름</td></tr>\n<tr><td>환경</td><td>CPU, 메모리, 런타임 버전, 플래그 기록</td><td>노트북 절전 모드에서 단발 측정</td></tr>\n<tr><td>측정</td><td>warmup 후 반복, p50/p95/max, outlier 기록</td><td>한 번 실행 후 평균만 보고</td></tr>\n<tr><td>관찰</td><td>latency, allocation, GC, CPU flame</td><td>wall time만 비교 — allocation 10배 차이를 놓침</td></tr>\n<tr><td>판정</td><td>회귀 임계값과 롤백 조건 (예: p95 +20% 시 revert)</td><td>\"빨라 보임\"으로 종료</td></tr>\n</table>\n<div class=\"checklist\"><ul>\n<li>벤치마크 입력이 실제 로그나 샘플에서 온 것인지 표시한다.</li>\n<li>성능 개선 PR에는 이전/이후 p50·p95와 실행 명령(<code>node bench.mjs</code>)을 남긴다.</li>\n<li>최적화 후 코드 복잡도가 증가했다면 유지보수 비용도 적는다.</li>\n<li>threshold를 CI나 성능 대시보드에 연결할 수 있는지 검토한다.</li>\n</ul></div>\n</section>\n\n<section id=\"failure\">\n<div class=\"ch-head\"><span class=\"ch-code\">CS-07</span><h2>실패 모드와 운영 방어선</h2></div>\n<p class=\"lede\">알고리즘 장애는 대부분 큰 입력, 나쁜 분포, 동시성, 메모리 증가에서 터집니다. \"정답을 계산한다\"와 \"서비스가 계속 산다\"는 별개의 요구입니다. CS-02에서 계산한 \"허용 n\"은 문서가 아니라 코드 경계에서 강제돼야 방어선이 됩니다.</p>\n<table>\n<tr><th>실패 모드</th><th>증상</th><th>방어선</th></tr>\n<tr><td>입력 폭탄</td><td>CPU spike, request timeout — <code>?limit=1000000</code> 한 방</td><td>크기 제한(아래 코드), pagination, streaming, admission control</td></tr>\n<tr><td>메모리 폭증</td><td>GC pause, OOM — 무제한 cache·버퍼 누적</td><td>bounded cache(CS-03 LRU의 <code>capacity</code>), chunking, backpressure</td></tr>\n<tr><td>해시/정렬 전제 깨짐</td><td>중복 누락, 순서 오류 — mutable key, 비교자 비대칭</td><td>immutable key, comparator test, invariant assertion</td></tr>\n<tr><td>동시 갱신</td><td>lost update, stale cache</td><td>atomic operation, lock, version, idempotency key</td></tr>\n<tr><td>근사 오판</td><td>false positive/negative 비용 발생</td><td>오차 예산, shadow validation, human review queue</td></tr>\n</table>\n<h3><span class=\"h3-tag\">CODE</span>입력 폭탄 방어 — \"무제한 n\"을 경계에서 끊기</h3>\n<p>CS-02의 예산 계산이 \"O(n) 경로는 n ≤ 10⁶까지 허용\"이라고 말했다면, 그 상한을 신뢰 경계에서 코드로 강제합니다. 클라이언트가 보내는 값은 입력이지 계약이 아닙니다.</p>\n<pre class=\"snippet-card\"><span class=\"sc-label\">TS · pagination guard — 통과한 값의 범위를 불변식으로 보장</span>\n// 불변식: 이 함수가 반환한 값은 항상\n//   1 ≤ limit ≤ MAX_PAGE_SIZE, 0 ≤ offset ≤ MAX_OFFSET 를 만족한다.\n// 이후 코드(쿼리, 루프, 직렬화)는 이 범위를 전제로 예산을 계산할 수 있다.\nconst MAX_PAGE_SIZE = 100;\nconst DEFAULT_PAGE_SIZE = 20;\nconst MAX_OFFSET = 10_000; // deep offset은 DB가 그만큼 읽고 버린다 — 아래 callout\n\nfunction parsePagination(query: Record&lt;string, unknown&gt;): {\n  limit: number;\n  offset: number;\n} {\n  const rawLimit = Number(query.limit);\n  const rawOffset = Number(query.offset);\n  // 초과 요청은 400 대신 clamp — 에러냐 clamp냐는 API 정책으로 명시해 합의\n  const limit =\n    Number.isInteger(rawLimit) &amp;&amp; rawLimit &gt; 0\n      ? Math.min(rawLimit, MAX_PAGE_SIZE)\n      : DEFAULT_PAGE_SIZE;\n  const offset =\n    Number.isInteger(rawOffset) &amp;&amp; rawOffset &gt;= 0\n      ? Math.min(rawOffset, MAX_OFFSET)\n      : 0;\n  return { limit, offset };\n}\n\n// body 폭탄은 파싱 전에 끊는다 — 100MB JSON은 parse 자체가 CPU·메모리 공격\napp.use(express.json({ limit: '1mb' })); // 초과 시 413 Payload Too Large</pre>\n<div class=\"callout warn\"><span class=\"co-label\">DEEP OFFSET</span><p><code>OFFSET 1000000 LIMIT 20</code>은 결과가 20행이어도 DB가 100만 행을 읽고 버립니다 — 복잡도가 페이지 번호에 비례해 자라는 O(offset) 경로입니다. offset 상한을 두고, 그 너머는 cursor 기반 pagination(<code>WHERE id &gt; :lastId ORDER BY id LIMIT :n</code>)으로 전환합니다. cursor는 인덱스를 타므로 페이지 깊이와 무관하게 O(log n + k)입니다.</p></div>\n<div class=\"checklist\"><ul>\n<li>모든 리스트 API에 limit 상한이 코드로 강제되는가 (기본값 존재 포함).</li>\n<li>모든 cache·큐·버퍼에 용량 상한과 eviction 정책이 있는가.</li>\n<li>hash key로 쓰는 객체가 immutable인가, comparator가 대칭·추이적인가 테스트했는가.</li>\n<li>재귀에 깊이 상한이 있는가 — 입력이 만든 cycle이 stack overflow가 되지 않는가.</li>\n</ul></div>\n<div class=\"glossary\">\n<div class=\"g-row\"><div class=\"g-term\"><b>Amortized</b><span>평균화 비용</span></div><div class=\"g-def\">개별 연산은 비싸도 긴 연산열 전체로 나누면 낮은 비용을 갖는다는 분석입니다. 동적 배열의 끝 삽입이 대표 예 — 가끔 O(n) 재할당이 있어도 평균 O(1)입니다.</div></div>\n<div class=\"g-row\"><div class=\"g-term\"><b>Invariant</b><span>항상 참</span></div><div class=\"g-def\">상태 전이 전후에 유지되어야 하는 조건입니다. CS-04의 <code>[lo, hi)</code>처럼 코드 주석과 PR 설명에 넣을 수 있는 한 문장이 목표입니다.</div></div>\n<div class=\"g-row\"><div class=\"g-term\"><b>Tail Latency</b><span>p95/p99</span></div><div class=\"g-def\">소수의 느린 요청이 사용자 경험과 장애 예산을 지배하는 현상입니다. CS-05 CASE처럼 \"가장 큰 입력\"이 꼬리를 만들므로, 평균이 아니라 p99 입력으로 예산을 계산합니다.</div></div>\n<div class=\"g-row\"><div class=\"g-term\"><b>N+1 Query</b><span>루프 안 원격 호출</span></div><div class=\"g-def\">목록 1회 조회 후 각 항목마다 추가 쿼리를 날리는 패턴입니다. 복잡도는 O(n)이지만 상수가 로컬 연산의 10⁵배라 CS-02 비용 모델에서 최우선 리뷰 대상입니다.</div></div>\n</div>\n</section>\n<footer>ENGINEERING CS FOUNDATIONS HANDBOOK · V2 · 2026-07</footer>",
};

export default document;
