import type { HandbookDocumentContent } from "../types";

const document: HandbookDocumentContent = {
  navHtml: "<div class=\"nav-title\">LLM 운영</div>\n  <a href=\"#ch1\"><span class=\"code\">OPS-01</span>Architecture</a>\n  <a href=\"#ch2\"><span class=\"code\">OPS-02</span>Lifecycle</a>\n  <a href=\"#ch3\"><span class=\"code\">OPS-03</span>Gateway</a>\n  <a href=\"#ch4\"><span class=\"code\">OPS-04</span>Queue·Cache</a>\n  <a href=\"#ch5\"><span class=\"code\">OPS-05</span>Observability</a>\n  <a href=\"#ch6\"><span class=\"code\">OPS-06</span>Release·Runbook</a>\n  <a href=\"#ch7\"><span class=\"code\">OPS-07</span>Vendor·Data</a>\n  <a href=\"#depth\"><span class=\"code\">DEPTH</span>장애 운영</a>",
  mainHtml: "<header class=\"hero\">\n  <div class=\"hero-serial\">\n    <span>DOC : LLM-APP-OPS-V2</span>\n    <span>SCOPE : API · QUEUE · RATE LIMIT · OBSERVABILITY · COST</span>\n    <span>LANG : KO</span>\n  </div>\n  <h1>LLM 앱 아키텍처와<br>운영</h1>\n  <p class=\"hero-sub\">LLM 앱은 일반 API보다 지연과 비용 변동이 크고, 품질 실패가 500 에러로 드러나지 않습니다. 그래서 관측과 평가가 운영의 일부가 됩니다.</p>\n</header>\n\n<section id=\"ch1\">\n<div class=\"ch-head\"><span class=\"ch-code\">OPS-01</span><h2>운영 가능한 LLM 앱의 정의</h2></div>\n<p class=\"lede\">LLM 앱의 성공 기준은 응답 성공 여부가 아니라 latency, cost, quality, safety, reliability를 동시에 만족하는 것입니다. “실패는 500만이 아니다”가 운영의 출발점입니다.</p>\n<div class=\"serial-card\">\n<span class=\"sc-label\">LLM APP ARCHITECTURE</span>\nONLINE PATH: client → API gateway → auth/tenant check → request validator → prompt builder → retrieval/tool layer → model_gateway → output validator → response store<br>\nASYNC PATH: client → job create → queue → worker → model_gateway → artifact store → progress update → webhook<br>\nCONTROL PATH: prompt registry → eval runner → eval_gate → canary_rollout → observability pipeline → alert → runbook → prompt_rollback\n</div>\n<table>\n<tr><th>컴포넌트</th><th>책임</th><th>운영 지표</th></tr>\n<tr><td>Model gateway</td><td>provider 추상화, retry, timeout, fallback_routing</td><td>provider error rate, p95 latency</td></tr>\n<tr><td>Prompt registry</td><td>prompt_version, owner, changelog</td><td>release success, rollback count</td></tr>\n<tr><td>Retrieval service</td><td>권한 filter, reranking, citation</td><td>retrieval_empty_rate, recall@k</td></tr>\n<tr><td>Output validator</td><td>schema parse, policy check</td><td>schema_failure_rate</td></tr>\n<tr><td>Eval pipeline</td><td>golden set, regression, online feedback</td><td>groundedness, judge score</td></tr>\n</table>\n</section>\n\n<section id=\"ch2\">\n<div class=\"ch-head\"><span class=\"ch-code\">OPS-02</span><h2>Request Lifecycle Envelope</h2></div>\n<p class=\"lede\">LLM 요청은 추적 가능해야 합니다. 답변이 틀렸을 때 어떤 prompt, model, retrieval index, tool result가 사용됐는지 재구성할 수 있어야 합니다.</p>\n<pre><code>{\n  \"request_id\": \"req_...\",\n  \"trace_id\": \"trace_...\",\n  \"tenant_id\": \"org_...\",\n  \"user_id\": \"usr_...\",\n  \"feature\": \"contract_review\",\n  \"prompt_version\": \"contract-review@2026-06-30\",\n  \"model_version\": \"quality-model@stable\",\n  \"model_policy\": \"quality_first\",\n  \"retrieval_policy\": {\n    \"index_version\": \"legal-docs-v42\",\n    \"document_acl_version\": \"acl-v9\",\n    \"acl_required\": true,\n    \"top_k\": 8\n  },\n  \"idempotency_key\": \"sha256(...)\"\n}</code></pre>\n<table>\n<tr><th>계약</th><th>왜 필요한가</th></tr>\n<tr><td>idempotency_key</td><td>긴 작업 재시도 시 중복 분석·중복 결제 방지</td></tr>\n<tr><td>tenant_id</td><td>검색, cache, 로그, quota의 격리 기준</td></tr>\n<tr><td>prompt_version/model_version</td><td>품질 회귀와 장애 원인 추적</td></tr>\n<tr><td>document_acl_version</td><td>권한 변경 후 cache와 retrieval 결과 무효화</td></tr>\n</table>\n</section>\n\n<section id=\"ch3\">\n<div class=\"ch-head\"><span class=\"ch-code\">OPS-03</span><h2>model_gateway와 Resilience</h2></div>\n<p class=\"lede\">모델 제공자 장애, 지연, rate limit은 정상적으로 발생합니다. gateway는 timeout, retry_budget, circuit_breaker, fallback_routing을 한곳에서 관리해야 합니다.</p>\n<table>\n<tr><th>정책</th><th>예시</th><th>주의</th></tr>\n<tr><td>timeout</td><td>interactive 20s, async 180s</td><td>UI 경로와 배치 경로를 분리</td></tr>\n<tr><td>retry_budget</td><td>feature별 전체 요청의 5% 이하</td><td>schema 실패와 provider 실패를 구분</td></tr>\n<tr><td>circuit_breaker</td><td>5분 error rate 또는 p95 초과 시 open</td><td>무한 fallback 루프 방지</td></tr>\n<tr><td>fallback_routing</td><td>primary 장애 시 latency_first 모델로 전환</td><td>품질 차이와 risk tier 표시</td></tr>\n<tr><td>degradation</td><td>RAG 검색만 제공, 초안 저장 보류</td><td>사용자에게 상태를 명확히 표시</td></tr>\n</table>\n</section>\n\n<section id=\"ch4\">\n<div class=\"ch-head\"><span class=\"ch-code\">OPS-04</span><h2>Queue·Cache·RATE LIMIT·quota</h2></div>\n<table>\n<tr><th>문제</th><th>설계</th><th>주의</th></tr>\n<tr><td>긴 문서 분석</td><td>background job, progress state, retry policy</td><td>중복 작업 idempotency</td></tr>\n<tr><td>반복 질문</td><td>exact cache 또는 semantic_cache</td><td>권한·문서 버전별 cache_key</td></tr>\n<tr><td>비용 폭주</td><td>user/org별 quota와 token_budget</td><td>retry storm 방지</td></tr>\n<tr><td>provider 장애</td><td>fallback model, graceful degradation</td><td>품질 차이 표시</td></tr>\n</table>\n<div class=\"serial-card\">\n<span class=\"sc-label\">cache_key</span>\ntenant_id + user_permission_hash + normalized_query_hash + prompt_version + model_family + retrieval_index_version + document_acl_version\n</div>\n</section>\n\n<section id=\"ch5\">\n<div class=\"ch-head\"><span class=\"ch-code\">OPS-05</span><h2>LLM OBSERVABILITY</h2></div>\n<p class=\"lede\">LLM 운영은 logs, metrics, traces, evals를 구분해서 봐야 합니다. 단순 API 성공률로는 품질 실패를 볼 수 없습니다.</p>\n<table>\n<tr><th>trace span</th><th>required attributes</th></tr>\n<tr><td>request.validate</td><td>feature, tenant_id_hash, input_size</td></tr>\n<tr><td>prompt.build</td><td>prompt_version, template_hash, input_tokens</td></tr>\n<tr><td>retrieval.search</td><td>top_k, retrieval_empty_rate, index_version</td></tr>\n<tr><td>tool.call</td><td>tool_name, approval_required, blocked</td></tr>\n<tr><td>model.generate</td><td>model, provider, output_tokens, cost_usd, fallback_used</td></tr>\n<tr><td>output.validate</td><td>schema_valid, schema_failure_rate, policy_decision</td></tr>\n<tr><td>eval.log</td><td>groundedness, citation_precision, judge_score</td></tr>\n</table>\n<div class=\"serial-card\">\n<span class=\"sc-label\">COST DASHBOARD</span>\nrequests by feature · model mix · input/output token · cache hit rate · retry count · cost_per_successful_answer · p95 latency · schema_failure_rate · retrieval_empty_rate\n</div>\n</section>\n\n<section id=\"ch6\">\n<div class=\"ch-head\"><span class=\"ch-code\">OPS-06</span><h2>Release, Runbook, production_readiness_checklist</h2></div>\n<p class=\"lede\">prompt, model, retrieval index, schema 변경은 코드 배포처럼 다뤄야 합니다. eval_gate를 통과하고 canary_rollout 후 문제가 있으면 prompt_rollback이 가능해야 합니다.</p>\n<table>\n<tr><th>증상</th><th>먼저 볼 것</th><th>완화</th></tr>\n<tr><td>응답 느림</td><td>provider latency, token length, queue depth</td><td>작은 모델 routing, streaming, async</td></tr>\n<tr><td>비용 급증</td><td>feature별 token, retry, cache miss</td><td>quota 하향, prompt compression, expensive model 제한</td></tr>\n<tr><td>환각 증가</td><td>retrieval_empty_rate, prompt_version, document freshness</td><td>prompt_rollback, citation 강화, no-answer 기준 강화</td></tr>\n<tr><td>JSON 실패</td><td>schema change, model change, max output</td><td>repair loop, fallback, schema evolution</td></tr>\n<tr><td>provider 장애</td><td>gateway error, circuit_breaker 상태</td><td>fallback_routing, degradation mode</td></tr>\n</table>\n<div class=\"serial-card\">\n<span class=\"sc-label\">production_readiness_checklist</span>\n□ 모든 요청에 request_id/trace_id가 있다 · □ prompt_version과 model_version이 로그에 남는다 · □ user/org quota가 있다 · □ cache key에 tenant/ACL/document version이 포함된다 · □ provider 장애 시 fallback/degradation 경로가 있다 · □ prompt 변경은 eval_gate를 통과해야 배포된다\n</div>\n</section>\n<section id=\"ch7\">\n<div class=\"ch-head\"><span class=\"ch-code\">OPS-07</span><h2>Vendor governance · DATA RESIDENCY · routing policy</h2></div>\n<p class=\"lede\">프로덕션 LLM 운영은 모델 gateway 내부 문제만이 아닙니다. provider due diligence, 데이터 국외 이전, 보존 기간, 학습 사용 여부, SLA, 장애 공지, 비용 계약까지 운영 설계에 포함해야 합니다.</p>\n<table>\n<tr><th>검토 축</th><th>필수 질문</th><th>증거</th></tr>\n<tr><td>provider due diligence</td><td>입력·출력 데이터가 학습에 쓰이는가, retention은 얼마인가</td><td>DPA, security whitepaper, vendor policy snapshot</td></tr>\n<tr><td>DATA RESIDENCY</td><td>tenant나 산업 규제로 region 제한이 필요한가</td><td>region policy, routing denylist, audit export</td></tr>\n<tr><td>SLA와 장애</td><td>provider 장애 시 어떤 기능을 degrade할 것인가</td><td>fallback matrix, incident runbook</td></tr>\n<tr><td>routing policy</td><td>risk, cost, latency, context, reasoning_budget에 따라 모델을 고르는가</td><td>route decision log, model mix dashboard</td></tr>\n</table>\n<pre><code>model_route_decision\nrisk_tier: high\nregion_policy: kr_only\nreasoning_budget: high\nprimary: quality_model_regional\nfallback: human_review_queue\nblocked: providers_without_data_residency_commitment</code></pre>\n</section>\n<section id=\"depth\">\n<div class=\"ch-head\"><span class=\"ch-code\">DEPTH</span><h2>운영 가능한 LLM 앱의 증거 설계</h2></div>\n<h3>MARKER: concept-depth — Request lifecycle as reconstructable evidence</h3>\n<p>request envelope는 로그 장식이 아닙니다. 답변이 틀렸을 때 같은 입력, 같은 prompt_version, 같은 model_version, 같은 retrieval index로 사고를 재현하기 위한 최소 증거입니다. 비용이 급증했을 때 어떤 feature와 tenant에서 token이 늘었는지 추적하고, 권한 사고가 났을 때 어떤 document_acl_version과 cache_key가 쓰였는지 확인하며, prompt 변경 후 품질이 떨어졌을 때 어떤 release가 원인인지 찾는 기반입니다.</p>\n<p>Online path, async path, control path는 상태 소유권도 달라야 합니다. Online path는 사용자의 대기 시간과 partial UI 상태를 관리하고, async path는 job state와 idempotency를 관리하며, control path는 prompt registry, eval gate, canary, rollback을 관리합니다. 이 경계를 섞으면 사용자는 완료되지 않은 결과를 완료로 보거나, worker 재시도로 같은 tool action이 두 번 실행되거나, prompt 변경이 평가 없이 배포될 수 있습니다.</p>\n<h3>MARKER: failure-driven — LLM failure taxonomy</h3>\n<p>LLM 운영에서 모든 실패를 retry로 해결하려 하면 비용 폭주가 납니다. transport failure나 일시적 provider failure는 제한된 retry가 가능하지만, schema failure는 targeted repair가 필요하고, grounding failure는 retrieval/context/prompt를 봐야 하며, safety failure는 즉시 차단하고 regression case를 추가해야 합니다. cost failure는 quota와 routing 문제이고, latency failure는 provider 시간·queue depth·token length를 분해해야 합니다.</p>\n<h3>MARKER: artifact-ready — Failure class decision table</h3>\n<pre><code>transport_failure: retry=yes, fallback=maybe, alert=error_rate\nprovider_failure: retry=limited, fallback=yes, alert=circuit_breaker\nschema_failure: retry=repair_once, fallback=review_queue, alert=schema_failure_rate\ngrounding_failure: retry=no, fallback=no_answer, alert=groundedness_drop\nsafety_failure: retry=no, fallback=block, alert=policy_violation\ncost_failure: retry=no, fallback=cheaper_model, alert=cost_per_successful_answer\nlatency_failure: retry=no, fallback=async_or_streaming, alert=p95_latency</code></pre>\n<p>Observability는 지표를 많이 모으는 일이 아니라 의사결정으로 연결하는 일입니다. `retrieval_empty_rate`가 오르고 `groundedness`가 내려가면 모델보다 인덱스 freshness나 권한 필터를 먼저 의심해야 합니다. `schema_failure_rate`가 오르면 max output, schema evolution, repair prompt를 봐야 합니다. `retry_count`와 `cache_hit_rate`가 동시에 나빠지면 비용 알림이 오기 전에 quota와 cache key를 확인해야 합니다.</p>\n<h3>MARKER: artifact-ready — Incident runbook: hallucination increase</h3>\n<pre><code>symptom: groundedness 0.94 -> 0.86, unsupported_claim_count doubled\ninspect: prompt_version, retrieval_empty_rate, index_version, recent chunker changes\nmitigate: tighten no-answer policy, reduce top_k noise, rollback prompt if recent\nrecover: add failed cases to golden dataset, rerun regression gate\nowner: ai-platform-oncall</code></pre>\n<h3>MARKER: artifact-ready — Prompt release change request</h3>\n<pre><code>prompt_version: rag-answer@44\nmodel_version: quality-model@stable\nrisk_tier: medium\neval_report: eval-report-2026-06-30\nexpected_delta: groundedness +1.5pp, latency +100ms\ncanary: 10% internal tenants for 24h\nrollback_plan: restore rag-answer@43</code></pre>\n<h3>MARKER: failure-driven — 비용 최적화가 품질 퇴행을 숨기는 실패</h3>\n<p>운영 단계에서 비용을 줄이려고 retrieved chunk 수, context compression, model tier, completion budget을 동시에 낮추면 겉으로는 latency와 cost가 좋아질 수 있습니다. 하지만 citation coverage가 줄고 no-answer가 늘거나, 반대로 근거 없는 짧은 답변이 많아지면 제품 품질은 퇴행합니다. 이 실패는 비용 대시보드만 보면 개선처럼 보이는 점이 위험합니다. LLM 앱 운영에서는 cost, latency, quality, safety를 같은 release gate에서 봐야 합니다. 비용 절감 변경은 반드시 golden dataset, groundedness, refusal accuracy, 사용자 fallback 비율과 함께 비교되어야 하며, p95 latency 개선만으로 릴리스하면 안 됩니다.</p>\n</section>",
};

export default document;
