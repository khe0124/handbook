import type { HandbookDocumentContent } from "../types";

const document: HandbookDocumentContent = {
  navHtml: "<div class=\"nav-brand\">OPERATIONS · 서비스 요청 경로</div>\n  <div class=\"nav-title\">서비스 요청 경로</div>\n  <a href=\"#chindex\"><span class=\"code\">INDEX</span>문서 구조</a>\n  <a href=\"#ch1\"><span class=\"code\">REQ-00</span>기준 경로</a>\n  <a href=\"#ch2\"><span class=\"code\">REQ-01</span>DNS와 TLS</a>\n  <a href=\"#ch3\"><span class=\"code\">REQ-02</span>Load Balancer와 health check</a>\n  <a href=\"#ch4\"><span class=\"code\">REQ-03</span>앱과 DB 경계</a>\n  <a href=\"#ch5\"><span class=\"code\">MODEL</span>개념 모델</a>\n  <a href=\"#ch6\"><span class=\"code\">CHECK</span>실무 체크리스트</a>\n  <a href=\"#ch7\"><span class=\"code\">PLAYBOOK</span>실무 플레이북</a>\n  <a href=\"#ch8\"><span class=\"code\">PRACTICE</span>출력 판독 훈련</a>\n  <a href=\"#ch9\"><span class=\"code\">TERM</span>용어사전</a>\n  <a href=\"#ch10\"><span class=\"code\">CASE</span>장애/운영 시나리오</a>\n  <a href=\"#ch11\"><span class=\"code\">RISK</span>자주 틀리는 판단</a>\n  <a href=\"#ch12\"><span class=\"code\">Q&A</span>면접 답변 템플릿</a>",
  mainHtml: "<header class=\"hero\">\n  <div class=\"hero-serial\">\n    <span>DOC : OPERATIONS-REQUEST-PATH</span>\n    <span>SCOPE : OPERATIONS · RELIABILITY · INCIDENT RESPONSE</span>\n    <span>LANG : KO</span>\n  </div>\n  <h1>서비스 요청 경로</h1>\n  <p class=\"hero-sub\">브라우저에서 애플리케이션과 데이터 저장소까지 요청이 지나가는 hop을 분해한다</p>\n  <div class=\"hero-meta\">FORMAT : 개념 → 증거 → 플레이북 → 장애 시나리오 → 면접 답변 · UPDATED 2026-06</div>\n</header>\n\n<section id=\"chindex\">\n<div class=\"ch-head\"><span class=\"ch-code\">INDEX</span><h2>문서 구조</h2></div>\n<p class=\"lede\">이 문서는 개념 요약이 아니라 운영 판단 연습용 플레이북입니다. 각 항목은 “무엇을 아는가”보다 “장애 때 어떤 증거를 보고 어떤 가설을 버리는가”를 기준으로 읽습니다.</p>\n<ul><li>핵심 개념</li><li>주제별 운영 증거</li><li>실무 플레이북</li><li>용어사전</li><li>Incident packet</li><li>주제별 답변 구조</li></ul>\n\n</section>\n\n      <section id=\"ch1\">\n        <div class=\"ch-head\"><span class=\"ch-code\">REQ-00</span><h2>기준 경로</h2></div>\n        <p class=\"lede\">대표 경로는 DNS → CDN/WAF → Load Balancer → App → DB다. 장애가 나면 사용자의 증상, DNS 해석, TLS handshake, CDN cache, WAF 차단, Load Balancer target health, 애플리케이션 로그, DB connection pool을 같은 시간축에 올린다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>사용자 요청이 어느 hop까지 도달했는지 어떻게 확인하는가?</td><td>DNS answer, TLS handshake, CDN/WAF request id, LB target health, app access log, DB pool metric</td><td>요청이 멈춘 가장 앞 계층을 찾고, 그 뒤 계층의 로그 공백을 원인으로 오해하지 않는다.</td></tr>\n          <tr><td>기준 경로를 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>dig, curl -v, openssl s_client, WAF log, LB target health, app log, DB pool dashboard</td><td>DNS, edge, LB, app, DB 증거를 같은 timestamp와 traceId로 연결한다.</td></tr>\n          <tr><td>기준 경로 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>특정 지역 사용자의 로그인 실패와 일부 계층 로그 공백</td><td>가장 앞단에서 막힌 증거를 찾기 전에는 app 또는 DB 문제로 단정하지 않는다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch2\">\n        <div class=\"ch-head\"><span class=\"ch-code\">REQ-01</span><h2>DNS와 TLS</h2></div>\n        <p class=\"lede\">DNS가 잘못되면 애플리케이션 로그가 비어 있을 수 있다. TLS 인증서 만료, SNI 불일치, intermediate chain 누락은 네트워크 연결은 되지만 HTTPS에서 실패하는 전형적인 원인이다. curl -v, openssl s_client, dig 결과를 함께 남긴다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>DNS/TLS 문제가 애플리케이션 장애처럼 보이는지 어떻게 구분하는가?</td><td>authoritative answer, resolver 차이, TTL, CNAME/ALIAS chain, SNI, SAN, expiry, intermediate chain</td><td>앱 로그가 비어 있거나 TLS handshake에서 실패하면 edge 이전 문제를 먼저 본다.</td></tr>\n          <tr><td>DNS와 TLS를 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>dig +trace app.example.com, dig app.example.com @8.8.8.8, curl -v, openssl s_client -servername app.example.com -connect app.example.com:443</td><td>권한 DNS와 public resolver 결과, 인증서 SAN/SNI/chain을 분리해 확인한다.</td></tr>\n          <tr><td>DNS와 TLS 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>일부 사용자만 인증서 오류 또는 이전 IP 접속</td><td>TTL, resolver cache, CDN binding, certificate chain을 복구 절차에 포함한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch3\">\n        <div class=\"ch-head\"><span class=\"ch-code\">REQ-02</span><h2>Load Balancer와 health check</h2></div>\n        <p class=\"lede\">Load Balancer는 단순 분산 장치가 아니라 장애 격리 장치다. health check path가 실제 의존성을 너무 깊게 보거나 너무 얕게 보면 정상 pod가 빠지거나 죽은 앱이 살아 있는 것처럼 보인다. 502는 upstream 연결 실패, 504는 timeout으로 우선 분리한다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>Load Balancer가 장애를 격리하는지, 오히려 정상 트래픽을 막는지 어떻게 확인하는가?</td><td>target health, health check status, 502/503/504, deregistration delay, idle timeout, app readiness</td><td>502는 upstream 연결, 503은 capacity/health, 504는 timeout 후보로 먼저 나눈다.</td></tr>\n          <tr><td>Load Balancer와 health check를 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>LB target health, listener rule, target group metric, WAF log, app access log, trace waterfall</td><td>target이 unhealthy인지, 연결은 되지만 응답이 늦는지, health check path가 과도한지 분리한다.</td></tr>\n          <tr><td>Load Balancer와 health check 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>배포 직후 502/503 증가와 일부 target unhealthy</td><td>bad target drain, rollout pause, health check rollback을 임시 완화로 검토한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch4\">\n        <div class=\"ch-head\"><span class=\"ch-code\">REQ-03</span><h2>앱과 DB 경계</h2></div>\n        <p class=\"lede\">App → DB 구간은 connection pool, timeout, transaction lock, migration, secret rotation이 얽힌다. 프론트 증상만 보고 API 문제로 단정하지 말고, traceId로 request log, slow query, DB wait event를 이어 본다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>App → DB 구간의 병목이 사용자 요청 실패로 이어졌는지 어떻게 확인하는가?</td><td>connection pool pending, DB wait event, lock wait, slow query, request timeout, secret rotation event</td><td>trace waterfall에서 가장 먼저 늘어난 구간과 DB wait event 시간이 맞아야 DB 경계 문제로 본다.</td></tr>\n          <tr><td>앱과 DB 경계를 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>APM trace waterfall, app access log, DB slow query log, pg_stat_activity 또는 provider DB performance insight</td><td>pool pending과 lock wait가 늘고 app timeout이 뒤따르면 DB 경계 병목 후보가 강하다.</td></tr>\n          <tr><td>앱과 DB 경계 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>로그인 API 504와 동시에 DB connection pool 대기가 급증</td><td>pool 보호, slow query 차단, migration lock 해소를 임시 완화와 영구 수정으로 나눠 기록한다.</td></tr>\n        </table>\n        <table>\n          <tr><th>예시 신호</th><th>관찰값</th><th>해석</th></tr>\n          <tr><td>정상</td><td>pool pending 낮음, slow query 없음, lock wait 없음</td><td>DB보다 upstream 또는 app 로직을 다시 본다.</td></tr>\n          <tr><td>비정상</td><td>pool pending 급증, lock wait 증가, API timeout 동시 발생</td><td>DB 병목 또는 connection pool 설정 문제로 우선 좁힌다.</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch5\">\n        <div class=\"ch-head\"><span class=\"ch-code\">MODEL</span><h2>개념 모델</h2></div>\n        <p class=\"lede\">서비스 요청 경로는 단일 지식 항목이 아니라 운영 판단의 일부다. 아래 표처럼 개념을 책임, 확인 지표, 실패 신호로 나누면 면접 답변과 실제 장애 대응이 모두 선명해진다.</p>\n        <table>\n          <tr><th>구성 요소</th><th>운영 책임</th><th>확인할 증거</th></tr>\n          <tr><td>REQ-00 · 기준 경로</td><td>대표 경로는 DNS → CDN/WAF → Load Balancer → App → DB다. 장애가 나면 사용자의 증상, DNS 해석, TLS handshake, CDN cache, WAF 차단, Load Balancer target health, 애플리케이션 로그, DB connection pool을 같은 시간축에 올린다.</td><td>DNS answer, TLS handshake, CDN/WAF request id, LB target health, app access log, DB pool metric</td></tr>\n          <tr><td>REQ-01 · DNS와 TLS</td><td>DNS가 잘못되면 애플리케이션 로그가 비어 있을 수 있다. TLS 인증서 만료, SNI 불일치, intermediate chain 누락은 네트워크 연결은 되지만 HTTPS에서 실패하는 전형적인 원인이다. curl -v, openssl s_client, dig 결과를 함께 남긴다.</td><td>authoritative answer, resolver 차이, TTL, CNAME/ALIAS chain, SNI, SAN, expiry, intermediate chain</td></tr>\n          <tr><td>REQ-02 · Load Balancer와 health check</td><td>Load Balancer는 단순 분산 장치가 아니라 장애 격리 장치다. health check path가 실제 의존성을 너무 깊게 보거나 너무 얕게 보면 정상 pod가 빠지거나 죽은 앱이 살아 있는 것처럼 보인다. 502는 upstream 연결 실패, 504는 timeout으로 우선 분리한다.</td><td>target health, health check status, 502/503/504, deregistration delay, idle timeout, app readiness</td></tr>\n          <tr><td>REQ-03 · 앱과 DB 경계</td><td>App → DB 구간은 connection pool, timeout, transaction lock, migration, secret rotation이 얽힌다. 프론트 증상만 보고 API 문제로 단정하지 말고, traceId로 request log, slow query, DB wait event를 이어 본다.</td><td>connection pool pending, DB wait event, lock wait, slow query, request timeout, secret rotation event</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch6\">\n        <div class=\"ch-head\"><span class=\"ch-code\">CHECK</span><h2>실무 체크리스트</h2></div>\n        <p class=\"lede\">서비스 요청 경로 체크리스트는 기준 경로, DNS와 TLS, Load Balancer와 health check, 앱과 DB 경계의 변경 위험을 사전에 드러내기 위한 검토표다. 각 항목은 리뷰, 배포 승인, 운영 인수에서 같은 증거로 다시 확인되어야 한다.</p>\n        <ul>\n          <li>사용자 증상, 지역, 시간, 브라우저 조건을 먼저 기록한다.</li>\n          <li>DNS, TLS, CDN/WAF, Load Balancer, App, DB 로그를 같은 시간축에 맞춘다.</li>\n          <li>502, 503, 504, timeout, TLS error를 분리한다.</li>\n          <li>traceId로 프론트, API, DB 관측 신호를 연결한다.</li>\n        </ul>\n        <table>\n          <tr><th>작업 단계</th><th>필수 산출물</th><th>차단 기준</th></tr>\n          <tr><td>설계</td><td>목적, 영향 범위, 비목표, owner</td><td>영향 범위가 사용자·시스템·비용 기준으로 설명되지 않음</td></tr>\n          <tr><td>변경</td><td>diff, 승인 기록, 검증 명령</td><td>production 변경과 staging 검증의 차이가 설명되지 않음</td></tr>\n          <tr><td>복구</td><td>rollback 또는 forward fix, 담당자, 예상 시간</td><td>실패 시 되돌릴 수 없는데 사전 승인과 공지가 없음</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch7\">\n        <div class=\"ch-head\"><span class=\"ch-code\">PLAYBOOK</span><h2>실무 플레이북</h2></div>\n        <p class=\"lede\">서비스 요청 경로 플레이북은 <strong>REQUEST PATH TRIAGE PLAYBOOK</strong> 기준으로 증상, 범위, 최근 변경, 계층별 증거를 연결한다. 목표는 명령어 암기가 아니라 어떤 가설을 버리고 어떤 완화책을 실행할지 결정하는 것이다.</p>\n        <div class=\"serial-card\">\n          <span class=\"sc-label\">REQUEST PATH TRIAGE PLAYBOOK</span>\n          symptom → scope → recent change → evidence<br>\n          → mitigation → verification → runbook diff\n        </div>\n        <table>\n          <tr><th>운영 표면</th><th>볼 증거</th><th>판단 기준</th></tr>\n          <tr><td>DNS</td><td>authoritative answer, resolver 차이, TTL, CNAME/ALIAS chain</td><td>앱 로그가 비어 있으면 먼저 이름 해석과 edge 진입을 본다.</td></tr>\n          <tr><td>TLS / Edge</td><td>SNI, SAN, expiry, intermediate chain, WAF/CDN request id</td><td>HTTPS 실패와 WAF 차단을 앱 장애로 오판하지 않는다.</td></tr>\n          <tr><td>LB / App / DB</td><td>target health, 502/503/504, traceId, pool pending, slow query</td><td>timeout이 어느 hop에서 시작됐는지 시간축으로 연결한다.</td></tr>\n        </table>\n\n        <table>\n          <tr><th>확인 단계</th><th>명령·확인 위치</th><th>해석 기준</th></tr>\n          <tr><td>DNS 확인</td><td>dig +trace app.example.com, dig app.example.com @8.8.8.8</td><td>권한 서버 응답과 public resolver cache 차이를 구분한다.</td></tr>\n          <tr><td>TLS 확인</td><td>curl -v https://app.example.com/health, openssl s_client -servername app.example.com -connect app.example.com:443</td><td>SNI, certificate chain, expiry, ALPN, handshake error를 본다.</td></tr>\n          <tr><td>경로 확인</td><td>LB target health, WAF log, app access log, trace waterfall, DB pool metric</td><td>502는 upstream 연결, 504는 timeout, 503은 capacity/health 후보로 나눈다.</td></tr>\n        </table>\n        <h3>MARKER: artifact-ready — operations command evidence packet</h3>\n        <pre class=\"snippet-card\"><code>ops_command_evidence_packet:\n  document: operations-request-path-handbook.html\n  playbook: REQUEST PATH TRIAGE PLAYBOOK\n  hypothesis: \"DNS 계층이 사용자 영향과 연결되어 있는가\"\n  command_or_probe: \"dig +trace app.example.com, dig app.example.com @8.8.8.8\"\n  normal_signal: \"권한 서버 응답과 public resolver cache 차이를 구분한다.\"\n  abnormal_signal: \"특정 지역 로그인 504, app log 일부 누락\"\n  mitigation_then_permanent_fix:\n    mitigation: \"DNS/edge/LB/app 중 어디까지 요청이 도달했는지 먼저 확인한다.\"\n    permanent_fix: \"다음 장애에서 같은 탐색을 반복하지 않게 한다.\"\n  evidence_to_attach:\n    - timestamp\n    - environment\n    - owner\n    - trace_or_request_id\n    - dashboard_snapshot\n    - runbook_diff</code></pre>\n        <table>\n          <tr><th>심화 장애 패턴</th><th>관찰 신호</th><th>판정 기준</th></tr>\n          <tr><td>DNS fail</td><td>NXDOMAIN, wrong CNAME, resolver cache, split-horizon mismatch</td><td>app log가 비어 있으면 DNS/edge에서 요청이 멈췄을 가능성이 높다.</td></tr>\n          <tr><td>TLS fail</td><td>certificate expired, SAN mismatch, missing intermediate, SNI mismatch</td><td>curl과 openssl 결과를 같이 보고 브라우저 오류 문구만 믿지 않는다.</td></tr>\n          <tr><td>LB fail</td><td>unhealthy target, no healthy upstream, idle timeout, deregistration delay</td><td>502/503/504를 같은 서버 오류로 묶지 않는다.</td></tr>\n          <tr><td>App/DB fail</td><td>pool pending, lock wait, slow query, downstream timeout</td><td>trace waterfall에서 가장 먼저 늘어난 구간을 찾는다.</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch8\">\n        <div class=\"ch-head\"><span class=\"ch-code\">PRACTICE</span><h2>출력 판독 훈련</h2></div>\n        <p class=\"lede\">서비스 요청 경로는 개념을 아는 것보다 실제 출력에서 정상과 비정상을 구분하는 훈련이 중요하다. 아래 lab은 명령, 출력, 판단, 완화, 영구 수정을 한 번에 연결한다.</p>\n        <div class=\"practice-lab\">\n          <span class=\"sc-label\">PRACTICE LAB 01 · ALB target health 502 판독</span>\n          <p>배포 직후 로그인 API에서 502가 증가한다. 앱 로그는 일부 요청만 남고 ALB target health가 흔들린다.</p>\n          <table>\n            <tr><th>Command / Query</th><th>목적</th></tr>\n            <tr><td><pre><code>aws elbv2 describe-target-health --target-group-arn arn:aws:elasticloadbalancing:region:acct:targetgroup/app-login/abc</code></pre></td><td>ALB가 upstream target을 정상으로 보고 있는지 확인한다.</td></tr>\n          </table>\n          <table>\n            <tr><th>정상 출력</th><th>비정상 출력</th><th>판단 훈련</th></tr>\n            <tr><td><pre><code>TargetHealth.State: healthy\nHealthCheckPort: traffic-port\nTarget.ResponseCode: 200</code></pre></td><td><pre><code>TargetHealth.State: unhealthy\nTargetHealth.Reason: Target.ResponseCodeMismatch\nDescription: Health checks failed with these codes: [500]</code></pre></td><td>ALB 502가 앱 코드 전체 문제가 아니라 target health check path 또는 새 버전 readiness 문제일 수 있다.</td></tr>\n          </table>\n          <table>\n            <tr><th>즉시 완화</th><th>영구 수정</th><th>검증 기준</th></tr>\n            <tr><td>신규 target drain, rollout pause, 이전 artifact rollback.</td><td>health check path를 실제 readiness와 분리하고 배포 smoke test에 target health gate를 추가한다.</td><td>unhealthy target 0, ALB 5xx 정상화, app access log와 traceId가 다시 연결됨.</td></tr>\n          </table>\n        </div>\n        <div class=\"practice-lab\">\n          <span class=\"sc-label\">PRACTICE LAB 02 · DNS NXDOMAIN vs cache 판독</span>\n          <p>일부 지역 사용자만 app.example.com 접속 실패를 보고한다.</p>\n          <table>\n            <tr><th>Command / Query</th><th>목적</th></tr>\n            <tr><td><pre><code>dig +trace app.example.com\n\ndig app.example.com @8.8.8.8</code></pre></td><td>권한 DNS와 public resolver cache 차이를 분리한다.</td></tr>\n          </table>\n          <table>\n            <tr><th>정상 출력</th><th>비정상 출력</th><th>판단 훈련</th></tr>\n            <tr><td><pre><code>app.example.com. 60 IN A 203.0.113.10\nstatus: NOERROR</code></pre></td><td><pre><code>status: NXDOMAIN\n;; no servers could be reached from delegated zone</code></pre></td><td>앱 장애가 아니라 record 삭제, zone delegation, resolver cache 문제일 수 있다.</td></tr>\n          </table>\n          <table>\n            <tr><th>즉시 완화</th><th>영구 수정</th><th>검증 기준</th></tr>\n            <tr><td>이전 record 복구, TTL 전파 모니터링, CDN origin fallback 확인.</td><td>DNS change ticket에 TTL 사전 조정, rollback record, 권한 DNS 검증을 필수화한다.</td><td>authoritative resolver와 public resolver 모두 NOERROR, CDN/WAF request id 재등장.</td></tr>\n          </table>\n        </div>\n      </section>\n\n      <section id=\"ch9\">\n        <div class=\"ch-head\"><span class=\"ch-code\">TERM</span><h2>용어사전</h2></div>\n        <p class=\"lede\">서비스 요청 경로에서 반복적으로 등장하는 운영 용어를 정의, 증거, 오해, 관련 항목 기준으로 정리한다. 장애 중에는 용어 자체보다 해당 용어가 어떤 로그, 지표, 설정, 책임 경계와 연결되는지를 확인한다.</p>\n        <table>\n          <tr><th>용어</th><th>정의</th><th>운영 증거</th><th>자주 하는 오해</th><th>관련 항목</th></tr>\n          <tr><td>DNS</td><td>도메인이 실제로 어떤 IP나 CNAME으로 해석되는지 확인하는 이름 해석 체계.</td><td>dig +trace, public resolver result, TTL</td><td>앱 로그가 비면 DNS는 정상이다.</td><td>resolver cache, authoritative answer, TTL</td></tr>\n          <tr><td>TLS</td><td>HTTPS 연결 전에 인증서와 암호화 조건을 합의하는 단계.</td><td>curl -v, openssl s_client, SAN/SNI/chain</td><td>인증서 발급 성공은 사용자 접속 성공과 같다.</td><td>SNI, SAN, intermediate chain</td></tr>\n          <tr><td>CDN/WAF request id</td><td>edge 계층에서 요청을 추적하는 식별자.</td><td>CDN log, WAF log, edge 403</td><td>403은 항상 앱 권한 문제다.</td><td>edge, WAF, origin</td></tr>\n          <tr><td>target health</td><td>Load Balancer가 backend를 받을 수 있는 상태로 보는지의 판단.</td><td>LB target health, health check status, 502/503/504</td><td>pod가 Running이면 LB도 정상이다.</td><td>health check, readiness, drain</td></tr>\n          <tr><td>connection pool</td><td>App이 DB 연결을 재사용하기 위해 관리하는 연결 집합.</td><td>pool pending, DB wait event, request timeout</td><td>DB CPU가 낮으면 DB 병목이 아니다.</td><td>slow query, lock wait, timeout</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch10\">\n        <div class=\"ch-head\"><span class=\"ch-code\">CASE</span><h2>장애/운영 시나리오</h2></div>\n        <p class=\"lede\">특정 시간대에 로그인 API가 간헐적으로 504를 반환한다. 먼저 DNS와 TLS가 정상인지 배제하고, CDN/WAF 차단 로그와 Load Balancer target health를 본다. 그 다음 App의 request latency와 DB connection pool 대기를 확인해 timeout이 어디에서 시작됐는지 좁힌다.</p>\n        <div class=\"callout\">\n          <span class=\"co-label\">Triage frame</span>\n          <p>서비스 요청 경로 장애는 증상 → 영향 범위 → 최근 변경 → DNS 증거 → 완화책 → 복구 검증 → 재발 방지 순서로 기록한다. 이 흐름이 있어야 담당자가 바뀌어도 같은 결론에 도달한다.</p>\n        </div>\n        <table>\n          <tr><th>처음 10분</th><th>다음 30분</th><th>종료 조건</th></tr>\n          <tr><td>authoritative answer, resolver 차이, TTL, CNAME/ALIAS chain 기준으로 영향 범위를 고정한다.</td><td>dig +trace app.example.com, dig app.example.com @8.8.8.8 항목에서 증거를 모아 완화책을 실행한다.</td><td>다음 장애에서 같은 탐색을 반복하지 않게 한다.</td></tr>\n        </table>\n        <table>\n          <tr><th>Incident packet</th><th>기록할 내용</th><th>판정 기준</th></tr>\n          <tr><td>증상</td><td>특정 지역 로그인 504, app log 일부 누락</td><td>DNS/edge/LB/app 중 어디까지 요청이 도달했는지 먼저 확인한다.</td></tr>\n          <tr><td>완화</td><td>WAF rule rollback, bad target drain, feature flag off, DB pool 보호</td><td>사용자 영향이 줄어드는 조치를 원인 분석과 병행한다.</td></tr>\n          <tr><td>사후</td><td>missing traceId, weak health check, timeout mismatch를 runbook에 반영</td><td>다음 장애에서 같은 탐색을 반복하지 않게 한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch11\">\n        <div class=\"ch-head\"><span class=\"ch-code\">RISK</span><h2>자주 틀리는 판단</h2></div>\n        <p class=\"lede\">서비스 요청 경로에서 자주 틀리는 판단은 기준 경로와 앱과 DB 경계의 책임 경계를 흐리는 데서 시작된다. 아래 신호가 보이면 설계, runbook, 답변 구조를 다시 점검한다.</p>\n        <ul>\n          <li>프론트 화면 오류만 보고 API 코드부터 수정한다.</li>\n          <li>Load Balancer health check가 통과하면 애플리케이션도 정상이라고 단정한다.</li>\n          <li>TLS 인증서 문제를 네트워크 장애로 오해한다.</li>\n          <li>DB lock wait와 API timeout을 별개 사건으로 본다.</li>\n        </ul>\n      </section>\n\n      <section id=\"ch12\">\n        <div class=\"ch-head\"><span class=\"ch-code\">Q&A</span><h2>면접 답변 템플릿</h2></div>\n        <p class=\"lede\">요청 장애는 DNS → CDN/WAF → Load Balancer → App → DB 순서로 좁힌다고 답합니다. 각 hop마다 볼 증거가 다르기 때문에 dig, curl -v, LB target health, 애플리케이션 로그, DB slow query를 같은 시간축에 놓고 판단합니다.</p>\n        <div class=\"serial-card\">\n          <span class=\"sc-label\">OPERATIONS ANSWER FRAME</span>\n          1. DNS부터 DB까지 hop별 증거를 순서대로 본다.<br>\n          2. 502/503/504/TLS error를 같은 장애로 묶지 않는다.<br>\n          3. traceId로 edge, app, DB 시간을 연결해 원인 후보를 줄인다.<br>\n        </div>\n\n      </section>\n<footer>\nOPERATIONS HANDBOOK · 2026-06<br>\nPRINCIPLE : 운영 지식은 경로, 증거, 복구 절차로 검증한다.\n</footer>",
};

export default document;
