import type { HandbookDocumentContent } from "../types";

const document: HandbookDocumentContent = {
  navHtml: "<div class=\"nav-brand\">OPERATIONS · 보안 경계</div>\n  <div class=\"nav-title\">보안 경계</div>\n  <a href=\"#chindex\"><span class=\"code\">INDEX</span>문서 구조</a>\n  <a href=\"#ch1\"><span class=\"code\">SEC-00</span>공개면 줄이기</a>\n  <a href=\"#ch2\"><span class=\"code\">SEC-01</span>WAF와 방화벽</a>\n  <a href=\"#ch3\"><span class=\"code\">SEC-02</span>IAM과 secret</a>\n  <a href=\"#ch4\"><span class=\"code\">SEC-03</span>검증 질문</a>\n  <a href=\"#ch5\"><span class=\"code\">MODEL</span>개념 모델</a>\n  <a href=\"#ch6\"><span class=\"code\">CHECK</span>실무 체크리스트</a>\n  <a href=\"#ch7\"><span class=\"code\">PLAYBOOK</span>실무 플레이북</a>\n  <a href=\"#ch8\"><span class=\"code\">PRACTICE</span>출력 판독 훈련</a>\n  <a href=\"#ch9\"><span class=\"code\">TERM</span>용어사전</a>\n  <a href=\"#ch10\"><span class=\"code\">CASE</span>장애/운영 시나리오</a>\n  <a href=\"#ch11\"><span class=\"code\">RISK</span>자주 틀리는 판단</a>\n  <a href=\"#ch12\"><span class=\"code\">Q&A</span>면접 답변 템플릿</a>",
  mainHtml: "<header class=\"hero\">\n  <div class=\"hero-serial\">\n    <span>DOC : OPERATIONS-SECURITY-BOUNDARY</span>\n    <span>SCOPE : OPERATIONS · RELIABILITY · INCIDENT RESPONSE</span>\n    <span>LANG : KO</span>\n  </div>\n  <h1>보안 경계</h1>\n  <p class=\"hero-sub\">외부 공개면, 내부 신뢰 경계, 권한, 비밀 정보를 계층별로 통제한다</p>\n  <div class=\"hero-meta\">FORMAT : 개념 → 증거 → 플레이북 → 장애 시나리오 → 면접 답변 · UPDATED 2026-06</div>\n</header>\n\n<section id=\"chindex\">\n<div class=\"ch-head\"><span class=\"ch-code\">INDEX</span><h2>문서 구조</h2></div>\n<p class=\"lede\">이 문서는 개념 요약이 아니라 운영 판단 연습용 플레이북입니다. 각 항목은 “무엇을 아는가”보다 “장애 때 어떤 증거를 보고 어떤 가설을 버리는가”를 기준으로 읽습니다.</p>\n<ul><li>핵심 개념</li><li>주제별 운영 증거</li><li>실무 플레이북</li><li>용어사전</li><li>Incident packet</li><li>주제별 답변 구조</li></ul>\n\n</section>\n\n      <section id=\"ch1\">\n        <div class=\"ch-head\"><span class=\"ch-code\">SEC-00</span><h2>공개면 줄이기</h2></div>\n        <p class=\"lede\">인터넷에 공개되는 것은 DNS record, CDN, WAF, Load Balancer 정도로 제한한다. App, DB, cache, queue는 private subnet에 두고 관리 접근은 VPN, bastion, SSO, short-lived credential로 제한한다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>인터넷 공개면이 의도한 edge 계층으로 제한되어 있는가?</td><td>public DNS record, CDN/WAF distribution, LB listener, security group ingress, admin access path</td><td>App/DB/cache/queue가 public endpoint 없이 private 경계 안에 있어야 한다.</td></tr>\n          <tr><td>공개면 줄이기를 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>external scan, DNS inventory, LB listener list, SG ingress, bastion/VPN access log</td><td>외부에서 접근 가능한 표면을 실제로 열거해야 한다.</td></tr>\n          <tr><td>공개면 줄이기 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>관리 포트 또는 내부 앱이 인터넷에 노출됨</td><td>public ingress를 닫고 edge/WAF/LB 경로로만 접근하게 복구한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch2\">\n        <div class=\"ch-head\"><span class=\"ch-code\">SEC-01</span><h2>WAF와 방화벽</h2></div>\n        <p class=\"lede\">WAF는 L7의 악성 요청 패턴을 줄이고, security group은 stateful L4 허용 목록을 만든다. NACL은 stateless subnet 경계라 응답 포트까지 고려해야 한다. 차단 로그가 남지 않는 규칙은 운영 중 원인 분석을 어렵게 만든다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>WAF, security group, NACL이 각 계층 책임에 맞게 동작하는가?</td><td>WAF rule hit, SG stateful rule, NACL stateless rule, firewall deny log, false positive rate</td><td>L7 차단과 L4 허용 목록, subnet stateless 경계를 혼동하지 않아야 한다.</td></tr>\n          <tr><td>WAF와 방화벽을 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>WAF log, SG rule diff, NACL ephemeral port, firewall decision log</td><td>차단 로그 없이 규칙만 있으면 운영 중 원인 분석이 어렵다.</td></tr>\n          <tr><td>WAF와 방화벽 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>정상 고객 요청이 WAF rule에 차단됨</td><td>rule rollback, allow exception expiry, false positive 재튜닝을 기록한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch3\">\n        <div class=\"ch-head\"><span class=\"ch-code\">SEC-02</span><h2>IAM과 secret</h2></div>\n        <p class=\"lede\">IAM은 사람, 서비스, pipeline, workload identity를 분리한다. secret은 repo, image, log, frontend bundle에 들어가면 안 된다. rotation 절차에는 revoke, 재발급, 배포, 영향 범위 확인이 포함된다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>IAM과 secret이 사람·서비스·pipeline 경계별로 분리되어 있는가?</td><td>principal, role assumption, policy diff, secret version, rotation date, last-used</td><td>장기 key와 wildcard 권한은 예외 승인과 만료 조건이 필요하다.</td></tr>\n          <tr><td>IAM과 secret를 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>IAM access analyzer, policy diff, secret manager version, audit log, deploy consumer list</td><td>secret이 repo/image/log/frontend bundle에 남지 않아야 한다.</td></tr>\n          <tr><td>IAM과 secret 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>secret rotation 후 일부 pod 인증 실패</td><td>revoke, 재발급, consumer 재시작, 영향 범위 확인을 순서대로 수행한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch4\">\n        <div class=\"ch-head\"><span class=\"ch-code\">SEC-03</span><h2>검증 질문</h2></div>\n        <p class=\"lede\">위협 모델링은 public ingress, 인증 우회, 권한 상승, secret 노출, egress 악용, 로그 공백을 먼저 본다. 이 서비스의 public ingress는 어디인가? egress는 어떤 도메인과 포트까지 허용되는가? 관리자 권한은 누가 승인하고 얼마나 유지되는가? 침해 시 어떤 로그로 행위를 추적할 수 있는가?</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>위협 모델이 public ingress, auth, privilege, egress, log gap을 다루는가?</td><td>public ingress list, auth boundary, admin action log, egress allowlist, audit coverage</td><td>공격 경로와 탐지 증거가 함께 설명되어야 한다.</td></tr>\n          <tr><td>검증 질문을 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>threat model review, access review, WAF/auth/admin/egress log sample</td><td>통제는 차단뿐 아니라 조사 가능한 로그로 증명되어야 한다.</td></tr>\n          <tr><td>검증 질문 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>비정상 admin export 후 행위 추적 로그 부족</td><td>권한 축소, egress 차단, audit alert, runbook diff를 남긴다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch5\">\n        <div class=\"ch-head\"><span class=\"ch-code\">MODEL</span><h2>개념 모델</h2></div>\n        <p class=\"lede\">보안 경계는 단일 지식 항목이 아니라 운영 판단의 일부다. 아래 표처럼 개념을 책임, 확인 지표, 실패 신호로 나누면 면접 답변과 실제 장애 대응이 모두 선명해진다.</p>\n        <table>\n          <tr><th>구성 요소</th><th>운영 책임</th><th>확인할 증거</th></tr>\n          <tr><td>SEC-00 · 공개면 줄이기</td><td>인터넷에 공개되는 것은 DNS record, CDN, WAF, Load Balancer 정도로 제한한다. App, DB, cache, queue는 private subnet에 두고 관리 접근은 VPN, bastion, SSO, short-lived credential로 제한한다.</td><td>public DNS record, CDN/WAF distribution, LB listener, security group ingress, admin access path</td></tr>\n          <tr><td>SEC-01 · WAF와 방화벽</td><td>WAF는 L7의 악성 요청 패턴을 줄이고, security group은 stateful L4 허용 목록을 만든다. NACL은 stateless subnet 경계라 응답 포트까지 고려해야 한다. 차단 로그가 남지 않는 규칙은 운영 중 원인 분석을 어렵게 만든다.</td><td>WAF rule hit, SG stateful rule, NACL stateless rule, firewall deny log, false positive rate</td></tr>\n          <tr><td>SEC-02 · IAM과 secret</td><td>IAM은 사람, 서비스, pipeline, workload identity를 분리한다. secret은 repo, image, log, frontend bundle에 들어가면 안 된다. rotation 절차에는 revoke, 재발급, 배포, 영향 범위 확인이 포함된다.</td><td>principal, role assumption, policy diff, secret version, rotation date, last-used</td></tr>\n          <tr><td>SEC-03 · 검증 질문</td><td>위협 모델링은 public ingress, 인증 우회, 권한 상승, secret 노출, egress 악용, 로그 공백을 먼저 본다. 이 서비스의 public ingress는 어디인가? egress는 어떤 도메인과 포트까지 허용되는가? 관리자 권한은 누가 승인하고 얼마나 유지되는가? 침해 시 어떤 로그로 행위를 추적할 수 있는가?</td><td>public ingress list, auth boundary, admin action log, egress allowlist, audit coverage</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch6\">\n        <div class=\"ch-head\"><span class=\"ch-code\">CHECK</span><h2>실무 체크리스트</h2></div>\n        <p class=\"lede\">보안 경계 체크리스트는 공개면 줄이기, WAF와 방화벽, IAM과 secret, 검증 질문의 변경 위험을 사전에 드러내기 위한 검토표다. 각 항목은 리뷰, 배포 승인, 운영 인수에서 같은 증거로 다시 확인되어야 한다.</p>\n        <ul>\n          <li>public ingress를 DNS, CDN/WAF, Load Balancer로 제한한다.</li>\n          <li>IAM은 사람, workload, pipeline 권한을 분리한다.</li>\n          <li>secret은 repo, image, log, frontend bundle에 남지 않게 한다.</li>\n          <li>위협 모델링으로 인증 우회, 권한 상승, egress 악용을 점검한다.</li>\n        </ul>\n        <table>\n          <tr><th>작업 단계</th><th>필수 산출물</th><th>차단 기준</th></tr>\n          <tr><td>설계</td><td>목적, 영향 범위, 비목표, owner</td><td>영향 범위가 사용자·시스템·비용 기준으로 설명되지 않음</td></tr>\n          <tr><td>변경</td><td>diff, 승인 기록, 검증 명령</td><td>production 변경과 staging 검증의 차이가 설명되지 않음</td></tr>\n          <tr><td>복구</td><td>rollback 또는 forward fix, 담당자, 예상 시간</td><td>실패 시 되돌릴 수 없는데 사전 승인과 공지가 없음</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch7\">\n        <div class=\"ch-head\"><span class=\"ch-code\">PLAYBOOK</span><h2>실무 플레이북</h2></div>\n        <p class=\"lede\">보안 경계 플레이북은 <strong>SECURITY BOUNDARY OPERATIONS PLAYBOOK</strong> 기준으로 증상, 범위, 최근 변경, 계층별 증거를 연결한다. 목표는 명령어 암기가 아니라 어떤 가설을 버리고 어떤 완화책을 실행할지 결정하는 것이다.</p>\n        <div class=\"serial-card\">\n          <span class=\"sc-label\">SECURITY BOUNDARY OPERATIONS PLAYBOOK</span>\n          symptom → scope → recent change → evidence<br>\n          → mitigation → verification → runbook diff\n        </div>\n        <table>\n          <tr><th>운영 표면</th><th>볼 증거</th><th>판단 기준</th></tr>\n          <tr><td>Public ingress</td><td>DNS, CDN/WAF, LB listener, allowed path, rate limit</td><td>공개면은 의도한 edge 계층으로 제한한다.</td></tr>\n          <tr><td>Identity / Secret</td><td>IAM principal, role assumption, secret location, rotation record</td><td>장기 key와 wildcard 권한은 예외로 관리한다.</td></tr>\n          <tr><td>Auditability</td><td>WAF log, auth failure, admin action, egress log, exception expiry</td><td>차단과 허용의 근거가 나중에 조사 가능해야 한다.</td></tr>\n        </table>\n\n        <table>\n          <tr><th>확인 단계</th><th>명령·확인 위치</th><th>해석 기준</th></tr>\n          <tr><td>공개면 점검</td><td>DNS record, LB listener, WAF rule, security group ingress 목록화</td><td>인터넷에서 접근 가능한 표면을 실제로 열거한다.</td></tr>\n          <tr><td>권한 점검</td><td>IAM policy diff, last-used, access review, break-glass ticket</td><td>필요 권한과 사용된 권한의 차이를 본다.</td></tr>\n          <tr><td>비밀 점검</td><td>secret manager version, rotation date, deploy consumer, log redaction</td><td>secret이 repo/image/log/frontend bundle에 남지 않아야 한다.</td></tr>\n        </table>\n        <h3>MARKER: artifact-ready — operations command evidence packet</h3>\n        <pre class=\"snippet-card\"><code>ops_command_evidence_packet:\n  document: operations-security-boundary-handbook.html\n  playbook: SECURITY BOUNDARY OPERATIONS PLAYBOOK\n  hypothesis: \"Public ingress 계층이 사용자 영향과 연결되어 있는가\"\n  command_or_probe: \"DNS record, LB listener, WAF rule, security group ingress 목록화\"\n  normal_signal: \"인터넷에서 접근 가능한 표면을 실제로 열거한다.\"\n  abnormal_signal: \"비정상 admin export 또는 외부 스캔 급증\"\n  mitigation_then_permanent_fix:\n    mitigation: \"actor, source IP, action, resource, WAF/auth log를 묶는다.\"\n    permanent_fix: \"보안 조치가 운영 절차로 남아야 한다.\"\n  evidence_to_attach:\n    - timestamp\n    - environment\n    - owner\n    - trace_or_request_id\n    - dashboard_snapshot\n    - runbook_diff</code></pre>\n\n      </section>\n\n      <section id=\"ch8\">\n        <div class=\"ch-head\"><span class=\"ch-code\">PRACTICE</span><h2>출력 판독 훈련</h2></div>\n        <p class=\"lede\">보안 경계는 개념을 아는 것보다 실제 출력에서 정상과 비정상을 구분하는 훈련이 중요하다. 아래 lab은 명령, 출력, 판단, 완화, 영구 수정을 한 번에 연결한다.</p>\n        <div class=\"practice-lab\">\n          <span class=\"sc-label\">PRACTICE LAB 01 · IAM 권한 확대 diff 판독</span>\n          <p>배포 pipeline role에 임시 권한을 추가한 뒤 보안 리뷰가 들어왔다.</p>\n          <table>\n            <tr><th>Command / Query</th><th>목적</th></tr>\n            <tr><td><pre><code>iam policy diff\n- s3:GetObject arn:aws:s3:::app-config/*\n+ s3:*\n+ kms:Decrypt *\nlastUsed=never\nexceptionExpiry=missing</code></pre></td><td>권한 확대가 최소 권한 원칙과 예외 만료 기준을 만족하는지 확인한다.</td></tr>\n          </table>\n          <table>\n            <tr><th>정상 출력</th><th>비정상 출력</th><th>판단 훈련</th></tr>\n            <tr><td><pre><code>Action: s3:GetObject\nResource: arn:aws:s3:::app-config/prod/*\nCondition: aws:PrincipalArn pipeline-role\nexceptionExpiry=2026-07-05</code></pre></td><td><pre><code>Action: s3:*\nResource: *\nAction: kms:Decrypt\nResource: *\nexceptionExpiry=missing</code></pre></td><td>pipeline 권한이 배포 범위를 넘어섰고 만료 없는 예외라 침해 반경이 커진다.</td></tr>\n          </table>\n          <table>\n            <tr><th>즉시 완화</th><th>영구 수정</th><th>검증 기준</th></tr>\n            <tr><td>wildcard 권한을 즉시 되돌리고 필요한 read 권한만 한정한다.</td><td>IAM diff review gate, last-used review, exception expiry alert를 추가한다.</td><td>wildcard action/resource 0, break-glass ticket 만료일 존재, audit log에서 사용 권한 확인.</td></tr>\n          </table>\n        </div>\n      </section>\n\n      <section id=\"ch9\">\n        <div class=\"ch-head\"><span class=\"ch-code\">TERM</span><h2>용어사전</h2></div>\n        <p class=\"lede\">보안 경계에서 반복적으로 등장하는 운영 용어를 정의, 증거, 오해, 관련 항목 기준으로 정리한다. 장애 중에는 용어 자체보다 해당 용어가 어떤 로그, 지표, 설정, 책임 경계와 연결되는지를 확인한다.</p>\n        <table>\n          <tr><th>용어</th><th>정의</th><th>운영 증거</th><th>자주 하는 오해</th><th>관련 항목</th></tr>\n          <tr><td>public ingress</td><td>인터넷에서 접근 가능한 진입 표면.</td><td>public DNS, CDN/WAF distribution, LB listener</td><td>WAF가 있으면 내부 앱 노출도 괜찮다.</td><td>edge, security group, external scan</td></tr>\n          <tr><td>WAF</td><td>L7 요청 패턴을 기준으로 악성 또는 비정상 요청을 줄이는 통제.</td><td>WAF rule hit, false positive rate, deny log</td><td>WAF는 네트워크 방화벽과 같은 역할이다.</td><td>firewall, rate limit, CDN</td></tr>\n          <tr><td>security group</td><td>stateful L4 허용 목록.</td><td>SG inbound/outbound diff, source, port</td><td>NACL과 동작 방식이 같다.</td><td>NACL, L4, ingress</td></tr>\n          <tr><td>IAM principal</td><td>권한을 부여받는 사람, 서비스, pipeline, workload identity.</td><td>principal, role assumption, last-used, policy diff</td><td>하나의 admin key로 운영해도 편하면 된다.</td><td>role, least privilege, audit log</td></tr>\n          <tr><td>secret rotation</td><td>비밀값을 revoke, 재발급, 배포, 영향 확인까지 교체하는 절차.</td><td>secret version, rotation date, consumer list</td><td>새 secret을 만들면 rotation이 끝난다.</td><td>revoke, runtime injection, audit</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch10\">\n        <div class=\"ch-head\"><span class=\"ch-code\">CASE</span><h2>장애/운영 시나리오</h2></div>\n        <p class=\"lede\">외부 공개 API를 신규 오픈한다. WAF rule과 rate limit을 먼저 적용하고, Load Balancer 뒤의 App은 private subnet에 둔다. IAM은 배포 권한과 운영 조회 권한을 분리하고, 인증 실패·차단·권한 거부 로그가 관측 시스템으로 들어오는지 확인한다.</p>\n        <div class=\"callout\">\n          <span class=\"co-label\">Triage frame</span>\n          <p>보안 경계 장애는 증상 → 영향 범위 → 최근 변경 → Public ingress 증거 → 완화책 → 복구 검증 → 재발 방지 순서로 기록한다. 이 흐름이 있어야 담당자가 바뀌어도 같은 결론에 도달한다.</p>\n        </div>\n        <table>\n          <tr><th>처음 10분</th><th>다음 30분</th><th>종료 조건</th></tr>\n          <tr><td>DNS, CDN/WAF, LB listener, allowed path, rate limit 기준으로 영향 범위를 고정한다.</td><td>DNS record, LB listener, WAF rule, security group ingress 목록화 항목에서 증거를 모아 완화책을 실행한다.</td><td>보안 조치가 운영 절차로 남아야 한다.</td></tr>\n        </table>\n        <table>\n          <tr><th>Incident packet</th><th>기록할 내용</th><th>판정 기준</th></tr>\n          <tr><td>증상</td><td>비정상 admin export 또는 외부 스캔 급증</td><td>actor, source IP, action, resource, WAF/auth log를 묶는다.</td></tr>\n          <tr><td>완화</td><td>token revoke, WAF rule tighten, role disable, egress block</td><td>임시 차단은 고객 영향과 만료 조건을 같이 기록한다.</td></tr>\n          <tr><td>사후</td><td>least privilege diff, audit alert, rotation drill 추가</td><td>보안 조치가 운영 절차로 남아야 한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch11\">\n        <div class=\"ch-head\"><span class=\"ch-code\">RISK</span><h2>자주 틀리는 판단</h2></div>\n        <p class=\"lede\">보안 경계에서 자주 틀리는 판단은 공개면 줄이기와 검증 질문의 책임 경계를 흐리는 데서 시작된다. 아래 신호가 보이면 설계, runbook, 답변 구조를 다시 점검한다.</p>\n        <ul>\n          <li>보안 그룹을 0.0.0.0/0으로 열고 WAF만 믿는다.</li>\n          <li>admin 권한을 장기 access key로 운영한다.</li>\n          <li>secret rotation을 배포 절차와 연결하지 않는다.</li>\n          <li>차단 로그가 없어 false positive와 공격을 구분하지 못한다.</li>\n        </ul>\n      </section>\n\n      <section id=\"ch12\">\n        <div class=\"ch-head\"><span class=\"ch-code\">Q&A</span><h2>면접 답변 템플릿</h2></div>\n        <p class=\"lede\">보안 경계는 공개면 최소화, 네트워크 통제, IAM 최소 권한, secret 관리, 감사 로그로 나눠 답합니다. WAF와 방화벽은 역할이 다르고, 침해를 완전히 막는 것보다 탐지와 피해 제한까지 설계하는 것이 중요합니다.</p>\n        <div class=\"serial-card\">\n          <span class=\"sc-label\">OPERATIONS ANSWER FRAME</span>\n          1. 공개면, 권한, 비밀, 감사 로그로 나눠 설명한다.<br>\n          2. WAF와 SG/NACL/IAM은 서로 대체하지 않는 통제라고 말한다.<br>\n          3. 침해를 막는 것뿐 아니라 탐지와 피해 제한까지 설계한다.<br>\n        </div>\n\n      </section>\n<footer>\nOPERATIONS HANDBOOK · 2026-06<br>\nPRINCIPLE : 운영 지식은 경로, 증거, 복구 절차로 검증한다.\n</footer>",
};

export default document;
