import type { HandbookDocumentContent } from "../types";

const document: HandbookDocumentContent = {
  navHtml: "<div class=\"nav-brand\">JAVA</div>\n  <div class=\"nav-title\">Java 예시 사례</div>\n  <a href=\"#ch1\"><span class=\"code\">JAVA-01</span>변수·타입·var</a>\n  <a href=\"#ch2\"><span class=\"code\">JAVA-02</span>컬렉션</a>\n  <a href=\"#ch3\"><span class=\"code\">JAVA-03</span>스트림 API</a>\n  <a href=\"#ch4\"><span class=\"code\">JAVA-04</span>Optional</a>\n  <a href=\"#ch5\"><span class=\"code\">JAVA-05</span>record와 불변 객체</a>\n  <a href=\"#ch6\"><span class=\"code\">JAVA-06</span>예외 처리</a>\n  <a href=\"#ch7\"><span class=\"code\">JAVA-07</span>제네릭</a>\n  <a href=\"#ch8\"><span class=\"code\">JAVA-08</span>동시성 기초</a>\n  <a href=\"#ch9\"><span class=\"code\">JAVA-09</span>날짜·시간</a>\n  <a href=\"#ch10\"><span class=\"code\">JAVA-10</span>자주 쓰는 이디엄</a>\n  <a href=\"#ch11\"><span class=\"code\">JAVA-11</span>치트시트</a>",
  mainHtml: "<header class=\"hero\">\n  <div class=\"hero-serial\">\n    <span>DOC : JAVA-EXAMPLES-V1</span>\n    <span>STACK : JAVA 17+</span>\n    <span>LANG : KO</span>\n  </div>\n  <h1>Java<br>예시 사례</h1>\n  <p class=\"hero-sub\">개념이 아니라 바로 쓰는 코드 — JS/TS 하던 손에 Java 문법을 붙이는 레시피. 각 섹션은 \"언제·왜\" 한두 줄과 복붙해서 고쳐 쓸 코드 카드로 구성됩니다. Java 17+ 기준.</p>\n  <div class=\"hero-meta\">NOTE : JS/TS와 대비되는 지점은 본문에 한 줄로 표시<br>COMPILED : 2026-06</div>\n</header>\n\n<!-- ============ CH 1 ============ -->\n<section id=\"ch1\">\n<div class=\"ch-head\"><span class=\"ch-code\">JAVA-01</span><h2>변수·타입·var</h2></div>\n<p class=\"lede\">Java는 정적 타입 언어 — 변수마다 타입이 박힙니다. 핵심 구분 하나: <em>기본형(primitive)</em>은 값 자체를, <em>참조형(reference)</em>은 객체 주소를 담습니다. JS의 <code>let</code>/<code>const</code>와 대비하면 감이 빨리 잡힙니다.</p>\n<div class=\"serial-card\">\n<span class=\"sc-label\">PRIMITIVE vs REFERENCE</span>\n<span class=\"dim\">// 기본형 — 값을 직접 담음 (소문자로 시작)</span><br>\n<span class=\"seg2\">int</span> count = <span class=\"seg\">10</span>;<br>\n<span class=\"seg2\">long</span> big = <span class=\"seg\">9_000_000_000L</span>;   <span class=\"dim\">// L 접미사, _ 가독성</span><br>\n<span class=\"seg2\">double</span> price = <span class=\"seg\">19.99</span>;<br>\n<span class=\"seg2\">boolean</span> active = <span class=\"seg\">true</span>;<br>\n<span class=\"seg2\">char</span> grade = <span class=\"seg\">'A'</span>;<br>\n<br>\n<span class=\"dim\">// 참조형 — 객체 (대문자로 시작, null 가능)</span><br>\n<span class=\"seg2\">String</span> name = <span class=\"seg\">\"haeun\"</span>;<br>\n<span class=\"seg2\">Integer</span> boxed = <span class=\"seg\">42</span>;          <span class=\"dim\">// int의 객체 wrapper, null 가능</span><br>\n<span class=\"seg2\">int[]</span> nums = {<span class=\"seg\">1</span>, <span class=\"seg\">2</span>, <span class=\"seg\">3</span>};\n</div>\n<p><code>int</code>은 절대 <code>null</code>이 될 수 없지만 <code>Integer</code>(wrapper)는 가능합니다. DB 컬럼이 nullable이면 entity 필드는 <code>Integer</code>/<code>Long</code>을 씁니다 — 여기서 JS 출신이 자주 헷갈립니다.</p>\n<h3><span class=\"h3-tag\">1.1</span>var 타입 추론 · final</h3>\n<p>Java 10+의 <code>var</code>는 TS의 타입 추론과 비슷하지만, <em>지역 변수에서만</em> 쓸 수 있고 우변으로 타입이 즉시 정해집니다(런타임 동적 타입이 아님). <code>final</code>은 JS의 <code>const</code>에 해당 — 재할당 금지.</p>\n<div class=\"serial-card\">\n<span class=\"sc-label\">var &amp; final</span>\n<span class=\"seg2\">var</span> users = <span class=\"seg2\">new</span> ArrayList&lt;String&gt;();  <span class=\"dim\">// ArrayList&lt;String&gt;로 추론</span><br>\n<span class=\"seg2\">var</span> total = <span class=\"seg\">0</span>;                       <span class=\"dim\">// int로 추론</span><br>\n<br>\n<span class=\"seg2\">final</span> <span class=\"seg2\">int</span> MAX = <span class=\"seg\">100</span>;                  <span class=\"dim\">// 재할당 불가 (= JS const)</span><br>\n<span class=\"seg2\">final</span> <span class=\"seg2\">var</span> config = loadConfig();      <span class=\"dim\">// final + var 조합 가능</span><br>\n<span class=\"dim\">// MAX = 200;  →  컴파일 에러</span>\n</div>\n<div class=\"callout warn\">\n<span class=\"co-label\">var 주의</span>\n<p><code>var x;</code>처럼 초기값 없이 선언하거나, 필드(클래스 멤버)·메서드 파라미터·리턴 타입에는 <code>var</code>를 못 씁니다. 우변이 타입을 명확히 드러내지 않으면(예: <code>var x = null;</code>) 컴파일 에러입니다. JS의 <code>let x;</code> 감각으로 쓰면 막힙니다.</p>\n</div>\n</section>\n\n<!-- ============ CH 2 ============ -->\n<section id=\"ch2\">\n<div class=\"ch-head\"><span class=\"ch-code\">JAVA-02</span><h2>컬렉션</h2></div>\n<p class=\"lede\">JS의 배열·Map·Set이 Java에선 <code>List</code>·<code>Map</code>·<code>Set</code> 인터페이스로 나뉩니다. 인터페이스(<code>List</code>)로 선언하고 구현체(<code>ArrayList</code>)로 생성하는 게 관례입니다.</p>\n<div class=\"serial-card\">\n<span class=\"sc-label\">생성 — 수정 가능 vs 불변</span>\n<span class=\"dim\">// 수정 가능한 빈 리스트</span><br>\nList&lt;String&gt; names = <span class=\"seg2\">new</span> ArrayList&lt;&gt;();<br>\nnames.add(<span class=\"seg\">\"kim\"</span>);<br>\nnames.add(<span class=\"seg\">\"lee\"</span>);<br>\n<br>\n<span class=\"dim\">// 불변 리스트 — add 하면 UnsupportedOperationException</span><br>\nList&lt;String&gt; fixed = List.of(<span class=\"seg\">\"a\"</span>, <span class=\"seg\">\"b\"</span>, <span class=\"seg\">\"c\"</span>);<br>\n<br>\n<span class=\"dim\">// Map / Set</span><br>\nMap&lt;String, Integer&gt; scores = <span class=\"seg2\">new</span> HashMap&lt;&gt;();<br>\nscores.put(<span class=\"seg\">\"haeun\"</span>, <span class=\"seg\">95</span>);<br>\nMap&lt;String, Integer&gt; m = Map.of(<span class=\"seg\">\"x\"</span>, <span class=\"seg\">1</span>, <span class=\"seg\">\"y\"</span>, <span class=\"seg\">2</span>);  <span class=\"dim\">// 불변</span><br>\nSet&lt;String&gt; tags = <span class=\"seg2\">new</span> HashSet&lt;&gt;(List.of(<span class=\"seg\">\"vcm\"</span>, <span class=\"seg\">\"ccm\"</span>));\n</div>\n<h3><span class=\"h3-tag\">2.1</span>순회</h3>\n<div class=\"serial-card\">\n<span class=\"sc-label\">ITERATION</span>\n<span class=\"dim\">// List 순회 — for-each (JS for...of)</span><br>\n<span class=\"seg2\">for</span> (String name : names) {<br>\n&nbsp;&nbsp;System.out.println(name);<br>\n}<br>\nnames.forEach(System.out::println);  <span class=\"dim\">// 메서드 참조</span><br>\n<br>\n<span class=\"dim\">// Map 순회 — entrySet (JS for...of Object.entries)</span><br>\n<span class=\"seg2\">for</span> (Map.Entry&lt;String, Integer&gt; e : scores.entrySet()) {<br>\n&nbsp;&nbsp;System.out.println(e.getKey() + <span class=\"seg\">\" = \"</span> + e.getValue());<br>\n}<br>\nscores.forEach((key, value) -&gt; System.out.println(key + value));\n</div>\n<table>\n<tr><th>자주 쓰는 메서드</th><th>의미 / JS 대응</th></tr>\n<tr><td><code>list.size()</code></td><td>크기 (JS <code>arr.length</code>)</td></tr>\n<tr><td><code>list.get(0)</code> / <code>list.set(0, v)</code></td><td>인덱스 접근/수정 (<code>arr[0]</code>)</td></tr>\n<tr><td><code>list.contains(v)</code></td><td>포함 여부 (<code>arr.includes</code>)</td></tr>\n<tr><td><code>list.isEmpty()</code></td><td>비었는지 (<code>arr.length === 0</code>)</td></tr>\n<tr><td><code>map.getOrDefault(k, def)</code></td><td>없으면 기본값 (<code>map.get(k) ?? def</code>)</td></tr>\n<tr><td><code>map.computeIfAbsent(k, x -&gt; ...)</code></td><td>없을 때만 계산해 넣고 반환</td></tr>\n<tr><td><code>map.containsKey(k)</code></td><td>키 존재 여부 (<code>k in obj</code>)</td></tr>\n</table>\n<div class=\"callout\">\n<span class=\"co-label\">선택 기준</span>\n<p>순서 유지가 필요하면 <code>ArrayList</code>, 중복 제거는 <code>HashSet</code>, 키-값은 <code>HashMap</code>. 입력 순서를 보존하는 Map이 필요하면 <code>LinkedHashMap</code>. <code>List.of()</code>/<code>Map.of()</code>로 만든 건 <strong>불변</strong>이라 나중에 수정할 거면 <code>new ArrayList&lt;&gt;(List.of(...))</code>로 감싸세요.</p>\n</div>\n</section>\n\n<!-- ============ CH 3 ============ -->\n<section id=\"ch3\">\n<div class=\"ch-head\"><span class=\"ch-code\">JAVA-03</span><h2>스트림 API</h2></div>\n<p class=\"lede\">JS의 <code>.filter().map().reduce()</code> 체이닝이 Java에선 Stream입니다. 차이 하나: 컬렉션에 <code>.stream()</code>으로 진입하고, <code>.collect()</code>로 빠져나와야 결과가 나옵니다(lazy).</p>\n<div class=\"serial-card\">\n<span class=\"sc-label\">filter · map · collect</span>\n<span class=\"dim\">// JS: users.filter(u =&gt; u.age &gt;= 18).map(u =&gt; u.name)</span><br>\nList&lt;String&gt; adults = users.stream()<br>\n&nbsp;&nbsp;.filter(u -&gt; u.age() &gt;= <span class=\"seg\">18</span>)<br>\n&nbsp;&nbsp;.map(User::name)<br>\n&nbsp;&nbsp;.collect(Collectors.toList());<br>\n<br>\n<span class=\"dim\">// Java 16+ : .toList() 로 더 짧게 (불변 리스트 반환)</span><br>\nList&lt;String&gt; names = users.stream().map(User::name).toList();\n</div>\n<h3><span class=\"h3-tag\">3.1</span>reduce · groupingBy</h3>\n<div class=\"serial-card\">\n<span class=\"sc-label\">집계</span>\n<span class=\"dim\">// 합계 — JS reduce((a, b) =&gt; a + b, 0)</span><br>\n<span class=\"seg2\">int</span> totalAge = users.stream()<br>\n&nbsp;&nbsp;.mapToInt(User::age)<br>\n&nbsp;&nbsp;.sum();<br>\n<br>\n<span class=\"seg2\">int</span> product = nums.stream().reduce(<span class=\"seg\">1</span>, (a, b) -&gt; a * b);<br>\n<br>\n<span class=\"dim\">// 그룹핑 — Map&lt;도시, List&lt;User&gt;&gt; (JS엔 기본 내장 없음)</span><br>\nMap&lt;String, List&lt;User&gt;&gt; byCity = users.stream()<br>\n&nbsp;&nbsp;.collect(Collectors.groupingBy(User::city));<br>\n<br>\n<span class=\"dim\">// 그룹별 개수 — Map&lt;도시, Long&gt;</span><br>\nMap&lt;String, Long&gt; countByCity = users.stream()<br>\n&nbsp;&nbsp;.collect(Collectors.groupingBy(User::city, Collectors.counting()));\n</div>\n<table>\n<tr><th>Stream 연산</th><th>JS 배열 대응</th></tr>\n<tr><td><code>.filter(p)</code></td><td><code>.filter()</code></td></tr>\n<tr><td><code>.map(f)</code></td><td><code>.map()</code></td></tr>\n<tr><td><code>.sorted()</code> / <code>.sorted(comparator)</code></td><td><code>.sort()</code></td></tr>\n<tr><td><code>.distinct()</code></td><td><code>[...new Set(arr)]</code></td></tr>\n<tr><td><code>.limit(n)</code> / <code>.skip(n)</code></td><td><code>.slice()</code></td></tr>\n<tr><td><code>.anyMatch(p)</code> / <code>.allMatch(p)</code></td><td><code>.some()</code> / <code>.every()</code></td></tr>\n<tr><td><code>.findFirst()</code></td><td><code>.find()</code> (Optional 반환)</td></tr>\n<tr><td><code>.count()</code></td><td><code>.length</code> (필터 후)</td></tr>\n</table>\n<div class=\"callout warn\">\n<span class=\"co-label\">스트림은 일회용</span>\n<p>한 번 소비한 Stream은 재사용 못 합니다 — 두 번 <code>.collect()</code>하면 <code>IllegalStateException</code>. 또 <code>.stream()</code>만 호출하고 terminal 연산(<code>collect</code>/<code>forEach</code>/<code>count</code> 등)을 안 붙이면 <em>아무 일도 일어나지 않습니다</em>(lazy). JS 배열 메서드처럼 매번 새 배열을 즉시 반환하는 게 아닙니다.</p>\n</div>\n</section>\n\n<!-- ============ CH 4 ============ -->\n<section id=\"ch4\">\n<div class=\"ch-head\"><span class=\"ch-code\">JAVA-04</span><h2>Optional</h2></div>\n<p class=\"lede\">\"값이 있을 수도, 없을 수도\" 를 타입으로 표현 — JS의 <code>?.</code>/<code>??</code>가 메서드로 들어온 형태입니다. <code>null</code> 체크를 강제해 NPE를 줄입니다.</p>\n<div class=\"serial-card\">\n<span class=\"sc-label\">생성 &amp; 꺼내기</span>\n<span class=\"dim\">// 생성</span><br>\nOptional&lt;User&gt; found = repo.findById(<span class=\"seg\">1L</span>);  <span class=\"dim\">// 보통 라이브러리가 반환</span><br>\nOptional&lt;String&gt; opt = Optional.ofNullable(maybeNull);<br>\n<br>\n<span class=\"dim\">// 기본값 — JS: maybeNull ?? \"guest\"</span><br>\nString name = opt.orElse(<span class=\"seg\">\"guest\"</span>);<br>\nString lazy = opt.orElseGet(() -&gt; computeDefault());  <span class=\"dim\">// 필요할 때만 계산</span><br>\n<br>\n<span class=\"dim\">// 없으면 예외 던지기</span><br>\nUser user = found.orElseThrow(() -&gt; <span class=\"seg2\">new</span> NotFoundException(<span class=\"seg\">\"없음\"</span>));\n</div>\n<div class=\"serial-card\">\n<span class=\"sc-label\">map · ifPresent — chaining</span>\n<span class=\"dim\">// JS: user?.profile?.email ?? \"no-email\"</span><br>\nString email = found<br>\n&nbsp;&nbsp;.map(User::profile)<br>\n&nbsp;&nbsp;.map(Profile::email)<br>\n&nbsp;&nbsp;.orElse(<span class=\"seg\">\"no-email\"</span>);<br>\n<br>\n<span class=\"dim\">// 값이 있을 때만 실행 — JS: if (x != null) {...}</span><br>\nfound.ifPresent(u -&gt; System.out.println(u.name()));<br>\nfound.ifPresentOrElse(<br>\n&nbsp;&nbsp;u -&gt; log.info(<span class=\"seg\">\"찾음: \"</span> + u.name()),<br>\n&nbsp;&nbsp;() -&gt; log.warn(<span class=\"seg\">\"못 찾음\"</span>)<br>\n);\n</div>\n<div class=\"callout risk\">\n<span class=\"co-label\">안티패턴 — Optional.get()</span>\n<p><code>opt.get()</code>은 값이 없으면 <code>NoSuchElementException</code>을 던집니다 — <code>opt.isPresent()</code>로 먼저 확인하지 않고 <code>get()</code>을 부르는 건 NPE를 다른 예외로 바꾼 것뿐입니다. <code>orElse</code>/<code>orElseThrow</code>/<code>map</code>으로 처리하세요. 그리고 Optional은 <em>리턴 타입용</em>입니다 — 필드나 메서드 파라미터에 쓰지 마세요.</p>\n</div>\n</section>\n\n<!-- ============ CH 5 ============ -->\n<section id=\"ch5\">\n<div class=\"ch-head\"><span class=\"ch-code\">JAVA-05</span><h2>record와 불변 객체</h2></div>\n<p class=\"lede\">데이터만 담는 클래스(DTO, value object)는 <code>record</code> 한 줄이면 끝 — 생성자·getter·<code>equals</code>·<code>hashCode</code>·<code>toString</code>이 자동 생성됩니다. TS의 <code>readonly</code> 객체 타입에 가깝습니다.</p>\n<div class=\"serial-card\">\n<span class=\"sc-label\">RECORD — 한 줄 불변 객체</span>\n<span class=\"dim\">// 이게 전부. 필드는 자동 final, getter는 name() 형태</span><br>\n<span class=\"seg2\">public record</span> User(String name, <span class=\"seg2\">int</span> age, String city) {}<br>\n<br>\n<span class=\"dim\">// 사용</span><br>\n<span class=\"seg2\">var</span> u = <span class=\"seg2\">new</span> User(<span class=\"seg\">\"haeun\"</span>, <span class=\"seg\">31</span>, <span class=\"seg\">\"gwangmyeong\"</span>);<br>\nu.name();   <span class=\"dim\">// \"haeun\"  (getName 아님 — name())</span><br>\nu.age();    <span class=\"dim\">// 31</span><br>\n<span class=\"dim\">// u.age = 32;  →  불가능 (불변)</span><br>\n<br>\n<span class=\"dim\">// 검증·정규화는 compact 생성자에</span><br>\n<span class=\"seg2\">public record</span> Credit(String id, <span class=\"seg2\">int</span> amount) {<br>\n&nbsp;&nbsp;<span class=\"seg2\">public</span> Credit {<br>\n&nbsp;&nbsp;&nbsp;&nbsp;<span class=\"seg2\">if</span> (amount &lt; <span class=\"seg\">0</span>) <span class=\"seg2\">throw new</span> IllegalArgumentException(<span class=\"seg\">\"음수 불가\"</span>);<br>\n&nbsp;&nbsp;}<br>\n}\n</div>\n<h3><span class=\"h3-tag\">5.1</span>빌더 패턴</h3>\n<p>필드가 많거나 선택적 값이 섞이면 생성자 인자 순서가 헷갈립니다. 이때 빌더로 \"이름 붙은 인자\"처럼 씁니다(JS의 옵션 객체 <code>{name, age}</code> 감각).</p>\n<div class=\"serial-card\">\n<span class=\"sc-label\">BUILDER</span>\n<span class=\"seg2\">public class</span> Order {<br>\n&nbsp;&nbsp;<span class=\"seg2\">private final</span> String id;<br>\n&nbsp;&nbsp;<span class=\"seg2\">private final</span> <span class=\"seg2\">int</span> qty;<br>\n&nbsp;&nbsp;<span class=\"seg2\">private final</span> String memo;<br>\n<br>\n&nbsp;&nbsp;<span class=\"seg2\">private</span> Order(Builder b) {<br>\n&nbsp;&nbsp;&nbsp;&nbsp;<span class=\"seg2\">this</span>.id = b.id; <span class=\"seg2\">this</span>.qty = b.qty; <span class=\"seg2\">this</span>.memo = b.memo;<br>\n&nbsp;&nbsp;}<br>\n<br>\n&nbsp;&nbsp;<span class=\"seg2\">public static class</span> Builder {<br>\n&nbsp;&nbsp;&nbsp;&nbsp;<span class=\"seg2\">private</span> String id;<br>\n&nbsp;&nbsp;&nbsp;&nbsp;<span class=\"seg2\">private</span> <span class=\"seg2\">int</span> qty = <span class=\"seg\">1</span>;<br>\n&nbsp;&nbsp;&nbsp;&nbsp;<span class=\"seg2\">private</span> String memo = <span class=\"seg\">\"\"</span>;<br>\n&nbsp;&nbsp;&nbsp;&nbsp;<span class=\"seg2\">public</span> Builder id(String v){ <span class=\"seg2\">this</span>.id = v; <span class=\"seg2\">return this</span>; }<br>\n&nbsp;&nbsp;&nbsp;&nbsp;<span class=\"seg2\">public</span> Builder qty(<span class=\"seg2\">int</span> v){ <span class=\"seg2\">this</span>.qty = v; <span class=\"seg2\">return this</span>; }<br>\n&nbsp;&nbsp;&nbsp;&nbsp;<span class=\"seg2\">public</span> Builder memo(String v){ <span class=\"seg2\">this</span>.memo = v; <span class=\"seg2\">return this</span>; }<br>\n&nbsp;&nbsp;&nbsp;&nbsp;<span class=\"seg2\">public</span> Order build(){ <span class=\"seg2\">return new</span> Order(<span class=\"seg2\">this</span>); }<br>\n&nbsp;&nbsp;}<br>\n}<br>\n<br>\n<span class=\"dim\">// 사용 — 순서 무관, 읽기 쉬움</span><br>\n<span class=\"seg2\">var</span> order = <span class=\"seg2\">new</span> Order.Builder().id(<span class=\"seg\">\"A1\"</span>).qty(<span class=\"seg\">5</span>).build();\n</div>\n<div class=\"callout\">\n<span class=\"co-label\">실무 팁</span>\n<p>빌더를 손으로 다 짜는 건 보일러플레이트입니다. 실무에선 Lombok의 <code>@Builder</code>/<code>@Value</code> 애너테이션으로 자동 생성하거나, 단순 DTO는 <code>record</code>로 끝내는 경우가 많습니다. record는 빌더가 필요 없을 만큼 필드가 적을 때, 빌더는 선택 필드가 많을 때.</p>\n</div>\n</section>\n\n<!-- ============ CH 6 ============ -->\n<section id=\"ch6\">\n<div class=\"ch-head\"><span class=\"ch-code\">JAVA-06</span><h2>예외 처리</h2></div>\n<p class=\"lede\">Java 예외는 두 갈래 — <em>checked</em>(컴파일러가 처리를 강제)와 <em>unchecked</em>(런타임, 강제 안 함). JS에는 이 구분이 없어서 처음엔 낯섭니다.</p>\n<table>\n<tr><th>구분</th><th>예시 / 처리</th></tr>\n<tr><td><b>checked</b></td><td><code>IOException</code>, <code>SQLException</code> — <code>try-catch</code> 또는 <code>throws</code>로 선언 강제</td></tr>\n<tr><td><b>unchecked</b></td><td><code>NullPointerException</code>, <code>IllegalArgumentException</code> — 처리 강제 없음(보통 버그)</td></tr>\n</table>\n<div class=\"serial-card\">\n<span class=\"sc-label\">try-with-resources — 자동 close</span>\n<span class=\"dim\">// 괄호 안 자원은 끝나면 자동 close (JS엔 없는 패턴)</span><br>\n<span class=\"seg2\">try</span> (<span class=\"seg2\">var</span> reader = Files.newBufferedReader(path)) {<br>\n&nbsp;&nbsp;<span class=\"seg2\">return</span> reader.readLine();<br>\n} <span class=\"seg2\">catch</span> (IOException e) {<br>\n&nbsp;&nbsp;<span class=\"dim\">// expected failure — 회복 가능한 곳에서 처리</span><br>\n&nbsp;&nbsp;log.error(<span class=\"seg\">\"파일 읽기 실패: \"</span> + path, e);<br>\n&nbsp;&nbsp;<span class=\"seg2\">throw new</span> ConfigLoadException(<span class=\"seg\">\"설정 로드 실패\"</span>, e);<br>\n}\n</div>\n<div class=\"serial-card\">\n<span class=\"sc-label\">커스텀 예외 클래스</span>\n<span class=\"dim\">// RuntimeException 상속 = unchecked (요즘 도메인 예외 관례)</span><br>\n<span class=\"seg2\">public class</span> CreditNotFoundException <span class=\"seg2\">extends</span> RuntimeException {<br>\n&nbsp;&nbsp;<span class=\"seg2\">public</span> CreditNotFoundException(String id) {<br>\n&nbsp;&nbsp;&nbsp;&nbsp;<span class=\"seg2\">super</span>(<span class=\"seg\">\"크레딧 없음: \"</span> + id);  <span class=\"dim\">// 메시지를 부모로</span><br>\n&nbsp;&nbsp;}<br>\n}<br>\n<br>\n<span class=\"dim\">// 던지기</span><br>\n<span class=\"seg2\">if</span> (credit == <span class=\"seg2\">null</span>) <span class=\"seg2\">throw new</span> CreditNotFoundException(id);\n</div>\n<div class=\"callout warn\">\n<span class=\"co-label\">예외를 삼키지 마라</span>\n<p>빈 <code>catch {}</code>는 버그를 숨기는 최악의 패턴입니다. 잡았으면 ① 회복하거나 ② 로그를 남기고 의미 있는 예외로 다시 던지세요(<code>throw new ...(msg, e)</code> — 원인 <code>e</code>를 꼭 전달해 stack trace 보존). 회복 못 할 거면 애초에 잡지 말고 위로 흘려보내는 게 낫습니다.</p>\n</div>\n</section>\n\n<!-- ============ CH 7 ============ -->\n<section id=\"ch7\">\n<div class=\"ch-head\"><span class=\"ch-code\">JAVA-07</span><h2>제네릭</h2></div>\n<p class=\"lede\">TS의 <code>&lt;T&gt;</code>와 같은 개념 — 타입을 파라미터로 받아 재사용 가능한 코드를 만듭니다. 차이: Java 제네릭은 컴파일 후 타입 정보가 지워집니다(type erasure).</p>\n<div class=\"serial-card\">\n<span class=\"sc-label\">제네릭 메서드 &amp; 클래스</span>\n<span class=\"dim\">// 제네릭 메서드 — &lt;T&gt;를 리턴 타입 앞에</span><br>\n<span class=\"seg2\">public static</span> &lt;T&gt; T firstOrNull(List&lt;T&gt; list) {<br>\n&nbsp;&nbsp;<span class=\"seg2\">return</span> list.isEmpty() ? <span class=\"seg2\">null</span> : list.get(<span class=\"seg\">0</span>);<br>\n}<br>\n<br>\n<span class=\"dim\">// 제네릭 클래스 — 결과 래퍼 같은 것</span><br>\n<span class=\"seg2\">public class</span> ApiResult&lt;T&gt; {<br>\n&nbsp;&nbsp;<span class=\"seg2\">private final</span> T data;<br>\n&nbsp;&nbsp;<span class=\"seg2\">private final</span> String error;<br>\n&nbsp;&nbsp;<span class=\"seg2\">public</span> ApiResult(T data, String error) {<br>\n&nbsp;&nbsp;&nbsp;&nbsp;<span class=\"seg2\">this</span>.data = data; <span class=\"seg2\">this</span>.error = error;<br>\n&nbsp;&nbsp;}<br>\n&nbsp;&nbsp;<span class=\"seg2\">public</span> T data() { <span class=\"seg2\">return</span> data; }<br>\n}<br>\n<br>\nApiResult&lt;User&gt; res = <span class=\"seg2\">new</span> ApiResult&lt;&gt;(user, <span class=\"seg2\">null</span>);\n</div>\n<h3><span class=\"h3-tag\">7.1</span>와일드카드 ? extends</h3>\n<p>\"User든 그 하위 타입이든 다 받겠다\"를 표현 — 읽기 전용으로 여러 하위 타입을 함께 다룰 때 씁니다.</p>\n<div class=\"serial-card\">\n<span class=\"sc-label\">WILDCARD</span>\n<span class=\"dim\">// ? extends Number — Number의 하위 타입 리스트 모두 허용</span><br>\n<span class=\"seg2\">public</span> <span class=\"seg2\">double</span> sum(List&lt;? <span class=\"seg2\">extends</span> Number&gt; nums) {<br>\n&nbsp;&nbsp;<span class=\"seg2\">double</span> total = <span class=\"seg\">0</span>;<br>\n&nbsp;&nbsp;<span class=\"seg2\">for</span> (Number n : nums) total += n.doubleValue();<br>\n&nbsp;&nbsp;<span class=\"seg2\">return</span> total;<br>\n}<br>\n<br>\n<span class=\"dim\">// 둘 다 호출 가능</span><br>\nsum(List.of(<span class=\"seg\">1</span>, <span class=\"seg\">2</span>, <span class=\"seg\">3</span>));          <span class=\"dim\">// List&lt;Integer&gt;</span><br>\nsum(List.of(<span class=\"seg\">1.5</span>, <span class=\"seg\">2.5</span>));          <span class=\"dim\">// List&lt;Double&gt;</span>\n</div>\n<div class=\"callout\">\n<span class=\"co-label\">PECS 기억법</span>\n<p>\"Producer Extends, Consumer Super\" — 값을 <em>꺼내 읽기만</em> 하면 <code>? extends T</code>, 값을 <em>넣기만</em> 하면 <code>? super T</code>. foundational 단계에선 <code>? extends</code>(읽기) 하나만 확실히 알아도 충분합니다. erasure 때문에 런타임에 <code>List&lt;String&gt;</code>인지 <code>List&lt;Integer&gt;</code>인지 구분 못 한다는 것만 기억하세요.</p>\n</div>\n</section>\n\n<!-- ============ CH 8 ============ -->\n<section id=\"ch8\">\n<div class=\"ch-head\"><span class=\"ch-code\">JAVA-08</span><h2>동시성 기초</h2></div>\n<p class=\"lede\">JS는 단일 스레드 + 이벤트 루프지만, Java는 진짜 멀티스레드입니다. 스레드를 직접 만들기보다 <code>ExecutorService</code>(스레드 풀)와 <code>CompletableFuture</code>(JS Promise에 해당)를 씁니다.</p>\n<div class=\"serial-card\">\n<span class=\"sc-label\">ExecutorService — 스레드 풀</span>\n<span class=\"seg2\">var</span> pool = Executors.newFixedThreadPool(<span class=\"seg\">4</span>);<br>\n<br>\n<span class=\"dim\">// 작업 제출 → Future로 결과 받기</span><br>\nFuture&lt;Integer&gt; future = pool.submit(() -&gt; heavyCompute());<br>\nInteger result = future.get();  <span class=\"dim\">// 끝날 때까지 블로킹</span><br>\n<br>\npool.shutdown();  <span class=\"dim\">// 반드시 종료 (안 하면 JVM 안 끝남)</span>\n</div>\n<div class=\"serial-card\">\n<span class=\"sc-label\">CompletableFuture — JS Promise 대응</span>\n<span class=\"dim\">// supplyAsync = Promise 시작, thenApply = .then()</span><br>\nCompletableFuture&lt;String&gt; cf = CompletableFuture<br>\n&nbsp;&nbsp;.supplyAsync(() -&gt; fetchUser(id))      <span class=\"dim\">// 비동기 실행</span><br>\n&nbsp;&nbsp;.thenApply(user -&gt; user.name())       <span class=\"dim\">// .then(u =&gt; u.name)</span><br>\n&nbsp;&nbsp;.thenApply(String::toUpperCase)<br>\n&nbsp;&nbsp;.exceptionally(e -&gt; <span class=\"seg\">\"기본값\"</span>);      <span class=\"dim\">// .catch()</span><br>\n<br>\nString name = cf.join();  <span class=\"dim\">// 결과 대기 (await 비슷)</span><br>\n<br>\n<span class=\"dim\">// 둘을 합치기 — Promise.all 비슷</span><br>\nCompletableFuture&lt;String&gt; combined = cf1<br>\n&nbsp;&nbsp;.thenCombine(cf2, (a, b) -&gt; a + b);\n</div>\n<div class=\"callout warn\">\n<span class=\"co-label\">JS와 다른 점</span>\n<p>JS의 <code>await</code>는 이벤트 루프에 양보하지만, Java <code>future.get()</code>/<code>join()</code>은 <em>실제로 스레드를 블로킹</em>합니다. 공유 상태를 여러 스레드가 동시에 건드리면 race condition이 생기니 — foundational 단계에선 가능하면 불변 객체를 넘기고, 공유 가변 상태를 피하세요. 스레드 풀은 <code>shutdown()</code> 누락이 단골 버그입니다.</p>\n</div>\n</section>\n\n<!-- ============ CH 9 ============ -->\n<section id=\"ch9\">\n<div class=\"ch-head\"><span class=\"ch-code\">JAVA-09</span><h2>날짜·시간</h2></div>\n<p class=\"lede\">옛날 <code>Date</code>/<code>Calendar</code>는 잊으세요 — Java 8+의 <code>java.time</code>이 표준입니다. 모두 <strong>불변</strong>이라 JS의 가변 <code>Date</code>보다 안전합니다.</p>\n<div class=\"serial-card\">\n<span class=\"sc-label\">java.time — 핵심 타입</span>\nLocalDate today = LocalDate.now();              <span class=\"dim\">// 날짜만 2026-06-23</span><br>\nLocalDate d = LocalDate.of(<span class=\"seg\">2026</span>, <span class=\"seg\">6</span>, <span class=\"seg\">23</span>);<br>\nLocalDateTime now = LocalDateTime.now();        <span class=\"dim\">// 날짜+시간</span><br>\nInstant ts = Instant.now();                     <span class=\"dim\">// UTC 타임스탬프</span><br>\n<br>\n<span class=\"dim\">// 연산 — 불변이라 새 객체 반환</span><br>\nLocalDate next = today.plusDays(<span class=\"seg\">7</span>);<br>\nLocalDate prev = today.minusMonths(<span class=\"seg\">1</span>);<br>\n<span class=\"seg2\">boolean</span> before = d.isBefore(today);\n</div>\n<div class=\"serial-card\">\n<span class=\"sc-label\">Duration · Period · 포맷</span>\n<span class=\"dim\">// 두 시각 사이 간격</span><br>\nDuration dur = Duration.between(start, end);<br>\n<span class=\"seg2\">long</span> mins = dur.toMinutes();<br>\n<br>\n<span class=\"dim\">// 두 날짜 사이 일수</span><br>\n<span class=\"seg2\">long</span> days = ChronoUnit.DAYS.between(d1, d2);<br>\n<br>\n<span class=\"dim\">// 포맷팅 — JS의 toLocaleDateString 대응</span><br>\n<span class=\"seg2\">var</span> fmt = DateTimeFormatter.ofPattern(<span class=\"seg\">\"yyyy-MM-dd HH:mm\"</span>);<br>\nString text = now.format(fmt);<br>\n<br>\n<span class=\"dim\">// 파싱 — 문자열 → 날짜</span><br>\nLocalDate parsed = LocalDate.parse(<span class=\"seg\">\"2026-06-23\"</span>);\n</div>\n<div class=\"callout\">\n<span class=\"co-label\">타임존</span>\n<p>저장·전송은 <code>Instant</code>(UTC)로, 사용자 표시할 때만 <code>ZonedDateTime</code>으로 타임존을 입히는 게 안전합니다 — <code>instant.atZone(ZoneId.of(\"Asia/Seoul\"))</code>. <code>LocalDateTime</code>은 타임존 정보가 없으니, \"한국 시각 그대로\" 같은 의미가 필요할 때만 쓰세요.</p>\n</div>\n</section>\n\n<!-- ============ CH 10 ============ -->\n<section id=\"ch10\">\n<div class=\"ch-head\"><span class=\"ch-code\">JAVA-10</span><h2>자주 쓰는 이디엄</h2></div>\n<p class=\"lede\">매일 마주치는 작은 패턴들. equals/hashCode, enum, switch 표현식, 문자열 처리 — 여기서 JS 습관과 가장 자주 충돌합니다.</p>\n<h3><span class=\"h3-tag\">10.1</span>equals / hashCode</h3>\n<div class=\"serial-card\">\n<span class=\"sc-label\">동등성 — == 아님</span>\n<span class=\"dim\">// JS: a === b 와 다름!  ==는 참조(주소) 비교</span><br>\nString a = <span class=\"seg2\">new</span> String(<span class=\"seg\">\"hi\"</span>);<br>\nString b = <span class=\"seg2\">new</span> String(<span class=\"seg\">\"hi\"</span>);<br>\na == b;        <span class=\"dim\">// false — 다른 객체</span><br>\na.equals(b);   <span class=\"dim\">// true  — 값 비교 (이걸 써야 함)</span><br>\n<br>\n<span class=\"dim\">// null 안전 비교</span><br>\nObjects.equals(a, b);   <span class=\"dim\">// a가 null이어도 NPE 안 남</span>\n</div>\n<h3><span class=\"h3-tag\">10.2</span>enum · switch 표현식</h3>\n<div class=\"serial-card\">\n<span class=\"sc-label\">ENUM &amp; SWITCH EXPRESSION (Java 14+)</span>\n<span class=\"seg2\">public enum</span> CreditStatus { ISSUED, TRADED, RETIRED }<br>\n<br>\n<span class=\"dim\">// switch가 값을 반환 — 화살표 문법, break 불필요</span><br>\nString label = <span class=\"seg2\">switch</span> (status) {<br>\n&nbsp;&nbsp;<span class=\"seg2\">case</span> ISSUED -&gt; <span class=\"seg\">\"발행됨\"</span>;<br>\n&nbsp;&nbsp;<span class=\"seg2\">case</span> TRADED -&gt; <span class=\"seg\">\"거래됨\"</span>;<br>\n&nbsp;&nbsp;<span class=\"seg2\">case</span> RETIRED -&gt; <span class=\"seg\">\"소멸됨\"</span>;<br>\n};  <span class=\"dim\">// 모든 case 처리 안 하면 컴파일 에러 (안전)</span>\n</div>\n<h3><span class=\"h3-tag\">10.3</span>문자열 처리</h3>\n<div class=\"serial-card\">\n<span class=\"sc-label\">STRING</span>\nString.join(<span class=\"seg\">\", \"</span>, list);          <span class=\"dim\">// JS arr.join(\", \")</span><br>\n<span class=\"seg\">\"a,b,c\"</span>.split(<span class=\"seg\">\",\"</span>);              <span class=\"dim\">// → String[]</span><br>\n<span class=\"seg\">\"  hi  \"</span>.trim();                  <span class=\"dim\">// \"hi\"</span><br>\n<span class=\"seg\">\"Hello\"</span>.contains(<span class=\"seg\">\"ell\"</span>);        <span class=\"dim\">// true</span><br>\nString.format(<span class=\"seg\">\"%s = %d\"</span>, k, v);  <span class=\"dim\">// JS 템플릿 리터럴 대응</span><br>\n<br>\n<span class=\"dim\">// 여러 줄 — text block (Java 15+)</span><br>\nString json = <span class=\"seg\">\"\"\"</span><br>\n<span class=\"seg\">&nbsp;&nbsp;&nbsp;&nbsp;{ \"name\": \"haeun\" }</span><br>\n<span class=\"seg\">&nbsp;&nbsp;&nbsp;&nbsp;\"\"\"</span>;\n</div>\n<div class=\"callout warn\">\n<span class=\"co-label\">== 함정</span>\n<p>JS 출신이 가장 많이 당하는 버그 — Java에서 객체 비교는 <strong>항상 <code>.equals()</code></strong>, <code>==</code>는 참조 비교입니다. 특히 <code>String</code>, <code>Integer</code>를 <code>==</code>로 비교하면 값이 같아도 <code>false</code>가 나올 수 있습니다(literal 캐싱 때문에 가끔 우연히 맞기도 해서 더 위험). 직접 만든 클래스를 <code>HashMap</code> 키나 <code>Set</code>에 넣으려면 <code>equals</code>+<code>hashCode</code>를 같이 구현해야 합니다(record는 자동).</p>\n</div>\n</section>\n\n<!-- ============ CH 11 ============ -->\n<section id=\"ch11\">\n<div class=\"ch-head\"><span class=\"ch-code\">JAVA-11</span><h2>치트시트 — 복붙용 패턴</h2></div>\n<p class=\"lede\">반복해서 찾게 되는 한 줄들. 외우기보다 \"이렇게 쓴다\"를 눈에 익혀두고 필요할 때 가져다 고치세요.</p>\n<div class=\"glossary\">\n<div class=\"g-row\"><div class=\"g-term\"><b>수정 가능 리스트</b><span>new ArrayList</span></div><div class=\"g-def\"><code>List&lt;String&gt; xs = new ArrayList&lt;&gt;();</code> — 이후 <code>xs.add(...)</code> 가능</div></div>\n<div class=\"g-row\"><div class=\"g-term\"><b>불변 리스트/맵</b><span>List.of / Map.of</span></div><div class=\"g-def\"><code>List.of(\"a\",\"b\")</code> · <code>Map.of(\"k\", 1)</code> — 수정 시 예외</div></div>\n<div class=\"g-row\"><div class=\"g-term\"><b>필터 + 변환 + 수집</b><span>stream chain</span></div><div class=\"g-def\"><code>xs.stream().filter(x -&gt; x &gt; 0).map(Obj::name).toList()</code></div></div>\n<div class=\"g-row\"><div class=\"g-term\"><b>그룹핑</b><span>groupingBy</span></div><div class=\"g-def\"><code>xs.stream().collect(Collectors.groupingBy(X::city))</code> → <code>Map&lt;K, List&lt;X&gt;&gt;</code></div></div>\n<div class=\"g-row\"><div class=\"g-term\"><b>합계</b><span>mapToInt.sum</span></div><div class=\"g-def\"><code>xs.stream().mapToInt(X::age).sum()</code></div></div>\n<div class=\"g-row\"><div class=\"g-term\"><b>없으면 기본값</b><span>Optional.orElse</span></div><div class=\"g-def\"><code>opt.orElse(def)</code> · 없으면 예외 <code>opt.orElseThrow(() -&gt; new ...())</code></div></div>\n<div class=\"g-row\"><div class=\"g-term\"><b>null 안전 체이닝</b><span>Optional.map</span></div><div class=\"g-def\"><code>opt.map(X::a).map(A::b).orElse(null)</code> — JS <code>?.</code> 대응</div></div>\n<div class=\"g-row\"><div class=\"g-term\"><b>데이터 객체</b><span>record</span></div><div class=\"g-def\"><code>public record User(String name, int age) {}</code> — getter는 <code>u.name()</code></div></div>\n<div class=\"g-row\"><div class=\"g-term\"><b>자원 자동 닫기</b><span>try-with-resources</span></div><div class=\"g-def\"><code>try (var r = open()) { ... } catch (IOException e) { ... }</code></div></div>\n<div class=\"g-row\"><div class=\"g-term\"><b>값 비교</b><span>equals</span></div><div class=\"g-def\"><code>a.equals(b)</code> (== 아님) · null 안전은 <code>Objects.equals(a, b)</code></div></div>\n<div class=\"g-row\"><div class=\"g-term\"><b>switch 표현식</b><span>switch -&gt;</span></div><div class=\"g-def\"><code>var r = switch(x) { case A -&gt; 1; default -&gt; 0; };</code></div></div>\n<div class=\"g-row\"><div class=\"g-term\"><b>비동기 체이닝</b><span>CompletableFuture</span></div><div class=\"g-def\"><code>CompletableFuture.supplyAsync(() -&gt; f()).thenApply(g)</code> — JS Promise</div></div>\n<div class=\"g-row\"><div class=\"g-term\"><b>오늘 + N일</b><span>java.time</span></div><div class=\"g-def\"><code>LocalDate.now().plusDays(7)</code> — 불변, 새 객체 반환</div></div>\n<div class=\"g-row\"><div class=\"g-term\"><b>날짜 포맷</b><span>DateTimeFormatter</span></div><div class=\"g-def\"><code>now.format(DateTimeFormatter.ofPattern(\"yyyy-MM-dd\"))</code></div></div>\n</div>\n<div class=\"callout gm\">\n<span class=\"co-label\">학습 순서 제안</span>\n<p>JS/TS 경험을 지렛대로 쓰세요 — 먼저 손에 붙일 것: <code>List/Map</code> 생성 · Stream 체이닝(<code>filter/map/toList</code>) · <code>Optional</code> · <code>record</code>. 이 넷이 일상 코드의 대부분입니다. 그다음 예외(checked/unchecked)와 <code>equals</code> vs <code>==</code> 함정을 잡으면 NPE·버그가 크게 줄어듭니다. 동시성과 와일드카드는 필요해질 때 이 문서로 돌아오면 됩니다.</p>\n</div>\n</section>\n\n<footer>\nJAVA EXAMPLES · V1 · 2026-06<br>\nSTACK : JAVA 17+ · 모든 코드는 Java 17 기준 문법 (var · record · switch expression · text block)<br>\nNOTE : JS/TS 대비 지점은 본문 표기 · COMPANION : BACKEND ENGINEERING HANDBOOK\n</footer>",
};

export default document;
