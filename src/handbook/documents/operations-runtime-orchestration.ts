import type { HandbookDocumentContent } from "../types";

const document: HandbookDocumentContent = {
  navHtml: "<div class=\"nav-brand\">OPERATIONS · 컨테이너</div>\n  <div class=\"nav-title\">컨테이너·오케스트레이션·Health Check</div>\n  <a href=\"#chindex\"><span class=\"code\">INDEX</span>문서 구조</a>\n  <a href=\"#ch1\"><span class=\"code\">RUN-00</span>컨테이너 런타임</a>\n  <a href=\"#ch2\"><span class=\"code\">RUN-01</span>오케스트레이션 객체</a>\n  <a href=\"#ch3\"><span class=\"code\">RUN-02</span>health check</a>\n  <a href=\"#ch4\"><span class=\"code\">RUN-03</span>배포 안정성</a>\n  <a href=\"#ch5\"><span class=\"code\">MODEL</span>개념 모델</a>\n  <a href=\"#ch6\"><span class=\"code\">CHECK</span>실무 체크리스트</a>\n  <a href=\"#ch7\"><span class=\"code\">PLAYBOOK</span>실무 플레이북</a>\n  <a href=\"#ch8\"><span class=\"code\">PRACTICE</span>출력 판독 훈련</a>\n  <a href=\"#ch9\"><span class=\"code\">TERM</span>용어사전</a>\n  <a href=\"#ch10\"><span class=\"code\">CASE</span>장애/운영 시나리오</a>\n  <a href=\"#ch11\"><span class=\"code\">RISK</span>자주 틀리는 판단</a>\n  <a href=\"#ch12\"><span class=\"code\">Q&A</span>면접 답변 템플릿</a>",
  mainHtml: "<header class=\"hero\">\n  <div class=\"hero-serial\">\n    <span>DOC : OPERATIONS-RUNTIME-ORCHESTRATION</span>\n    <span>SCOPE : OPERATIONS · RELIABILITY · INCIDENT RESPONSE</span>\n    <span>LANG : KO</span>\n  </div>\n  <h1>컨테이너·오케스트레이션·Health Check</h1>\n  <p class=\"hero-sub\">컨테이너를 이미지가 아니라 제한된 런타임과 복구 정책으로 이해한다</p>\n  <div class=\"hero-meta\">FORMAT : 개념 → 증거 → 플레이북 → 장애 시나리오 → 면접 답변 · UPDATED 2026-06</div>\n</header>\n\n<section id=\"chindex\">\n<div class=\"ch-head\"><span class=\"ch-code\">INDEX</span><h2>문서 구조</h2></div>\n<p class=\"lede\">이 문서는 개념 요약이 아니라 운영 판단 연습용 플레이북입니다. 각 항목은 “무엇을 아는가”보다 “장애 때 어떤 증거를 보고 어떤 가설을 버리는가”를 기준으로 읽습니다.</p>\n<ul><li>핵심 개념</li><li>주제별 운영 증거</li><li>실무 플레이북</li><li>용어사전</li><li>Incident packet</li><li>주제별 답변 구조</li></ul>\n</section>\n\n      <section id=\"ch1\">\n        <div class=\"ch-head\"><span class=\"ch-code\">RUN-00</span><h2>컨테이너 런타임</h2></div>\n        <p class=\"lede\">컨테이너는 프로세스, filesystem, network namespace, cgroup 제한을 묶은 실행 단위다. CPU, memory, file descriptor, ephemeral storage 제한을 모르고 운영하면 OOM, throttling, log 폭증을 놓친다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>컨테이너 제한이 실제 장애 신호와 연결되어 있는가?</td><td>CPU throttling, memory working set, OOMKilled, file descriptor, ephemeral storage, restart reason</td><td>컨테이너는 이미지가 아니라 제한된 프로세스라는 관점으로 봐야 한다.</td></tr>\n          <tr><td>컨테이너 런타임을 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>kubectl describe, logs --previous, cgroup metric, container exit code</td><td>재시작이 회복인지 장애 반복인지 exit reason으로 구분한다.</td></tr>\n          <tr><td>컨테이너 런타임 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>트래픽 증가 후 OOMKilled 반복</td><td>request/limit, memory leak, graceful degradation을 분리해 조치한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch2\">\n        <div class=\"ch-head\"><span class=\"ch-code\">RUN-01</span><h2>오케스트레이션 객체</h2></div>\n        <p class=\"lede\">Kubernetes 기준으로 Deployment는 desired state, ReplicaSet은 replica 유지, Pod는 실행 단위, Service는 안정된 discovery, Ingress는 외부 HTTP 진입점이다. label과 selector가 어긋나면 정상 pod도 traffic을 받지 못한다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>오케스트레이션 객체 연결이 traffic routing까지 일치하는가?</td><td>Deployment, ReplicaSet, Pod, Service, selector, endpoint, Ingress rule, target group</td><td>label/selector가 어긋나면 정상 pod도 트래픽을 받지 못한다.</td></tr>\n          <tr><td>오케스트레이션 객체를 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>kubectl get endpoints, describe service/ingress, target health, event log</td><td>desired state와 실제 traffic endpoint를 대조해야 한다.</td></tr>\n          <tr><td>오케스트레이션 객체 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>pod는 Running이나 서비스 503</td><td>selector, endpoint, ingress rule을 수정하고 smoke test한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch3\">\n        <div class=\"ch-head\"><span class=\"ch-code\">RUN-02</span><h2>health check</h2></div>\n        <p class=\"lede\">readiness는 트래픽을 받아도 되는지, liveness는 재시작이 필요한지, startup probe는 느린 부팅을 보호하는지 판단한다. health check가 DB까지 강하게 묶이면 DB 지연 때 전체 pod가 빠질 수 있다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>readiness/liveness/startup probe가 각 목적에 맞게 분리되어 있는가?</td><td>readiness status, liveness restart, startup probe, probe path dependency, failure threshold</td><td>liveness가 DB 지연까지 재시작하면 장애를 키울 수 있다.</td></tr>\n          <tr><td>health check를 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>kubectl describe pod, probe event, readiness endpoint, dependency status</td><td>traffic 수신 가능 여부와 재시작 필요 여부를 분리한다.</td></tr>\n          <tr><td>health check 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>DB 지연 때 모든 pod가 liveness 실패로 재시작</td><td>probe path를 얕게 조정하고 dependency 상태는 readiness/metric으로 분리한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch4\">\n        <div class=\"ch-head\"><span class=\"ch-code\">RUN-03</span><h2>배포 안정성</h2></div>\n        <p class=\"lede\">rolling update는 maxUnavailable, maxSurge, pod disruption budget, connection draining, graceful shutdown이 함께 맞아야 무중단에 가까워진다. HPA는 CPU만이 아니라 queue lag, request rate 같은 업무 지표도 후보가 된다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>rolling update가 drain, PDB, graceful shutdown까지 포함하는가?</td><td>maxUnavailable, maxSurge, PDB, terminationGracePeriod, preStop, target deregistration delay</td><td>새 pod 준비와 기존 pod 종료가 traffic 흐름과 맞아야 한다.</td></tr>\n          <tr><td>배포 안정성을 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>rollout status, pod events, LB target draining, connection error rate</td><td>무중단은 replica 수가 아니라 연결 종료와 readiness 타이밍으로 결정된다.</td></tr>\n          <tr><td>배포 안정성 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>배포 중 일부 요청 502</td><td>rollout pause/undo, drain 조정, graceful shutdown 보강을 수행한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch5\">\n        <div class=\"ch-head\"><span class=\"ch-code\">MODEL</span><h2>개념 모델</h2></div>\n        <p class=\"lede\">컨테이너·오케스트레이션·Health Check는 단일 지식 항목이 아니라 운영 판단의 일부다. 아래 표처럼 개념을 책임, 확인 지표, 실패 신호로 나누면 면접 답변과 실제 장애 대응이 모두 선명해진다.</p>\n        <table>\n          <tr><th>구성 요소</th><th>운영 책임</th><th>확인할 증거</th></tr>\n          <tr><td>RUN-00 · 컨테이너 런타임</td><td>컨테이너는 프로세스, filesystem, network namespace, cgroup 제한을 묶은 실행 단위다. CPU, memory, file descriptor, ephemeral storage 제한을 모르고 운영하면 OOM, throttling, log 폭증을 놓친다.</td><td>CPU throttling, memory working set, OOMKilled, file descriptor, ephemeral storage, restart reason</td></tr>\n          <tr><td>RUN-01 · 오케스트레이션 객체</td><td>Kubernetes 기준으로 Deployment는 desired state, ReplicaSet은 replica 유지, Pod는 실행 단위, Service는 안정된 discovery, Ingress는 외부 HTTP 진입점이다. label과 selector가 어긋나면 정상 pod도 traffic을 받지 못한다.</td><td>Deployment, ReplicaSet, Pod, Service, selector, endpoint, Ingress rule, target group</td></tr>\n          <tr><td>RUN-02 · health check</td><td>readiness는 트래픽을 받아도 되는지, liveness는 재시작이 필요한지, startup probe는 느린 부팅을 보호하는지 판단한다. health check가 DB까지 강하게 묶이면 DB 지연 때 전체 pod가 빠질 수 있다.</td><td>readiness status, liveness restart, startup probe, probe path dependency, failure threshold</td></tr>\n          <tr><td>RUN-03 · 배포 안정성</td><td>rolling update는 maxUnavailable, maxSurge, pod disruption budget, connection draining, graceful shutdown이 함께 맞아야 무중단에 가까워진다. HPA는 CPU만이 아니라 queue lag, request rate 같은 업무 지표도 후보가 된다.</td><td>maxUnavailable, maxSurge, PDB, terminationGracePeriod, preStop, target deregistration delay</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch6\">\n        <div class=\"ch-head\"><span class=\"ch-code\">CHECK</span><h2>실무 체크리스트</h2></div>\n        <p class=\"lede\">컨테이너·오케스트레이션·Health Check 체크리스트는 컨테이너 런타임, 오케스트레이션 객체, health check, 배포 안정성의 변경 위험을 사전에 드러내기 위한 검토표다. 각 항목은 리뷰, 배포 승인, 운영 인수에서 같은 증거로 다시 확인되어야 한다.</p>\n        <ul>\n          <li>readiness, liveness, startup probe의 목적을 분리한다.</li>\n          <li>resource request/limit과 실제 사용량을 비교한다.</li>\n          <li>service selector, ingress rule, connection draining을 점검한다.</li>\n          <li>graceful shutdown과 rolling update 조건을 부하 상황에서 검증한다.</li>\n        </ul>\n        <table>\n          <tr><th>작업 단계</th><th>필수 산출물</th><th>차단 기준</th></tr>\n          <tr><td>설계</td><td>목적, 영향 범위, 비목표, owner</td><td>영향 범위가 사용자·시스템·비용 기준으로 설명되지 않음</td></tr>\n          <tr><td>변경</td><td>diff, 승인 기록, 검증 명령</td><td>production 변경과 staging 검증의 차이가 설명되지 않음</td></tr>\n          <tr><td>복구</td><td>rollback 또는 forward fix, 담당자, 예상 시간</td><td>실패 시 되돌릴 수 없는데 사전 승인과 공지가 없음</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch7\">\n        <div class=\"ch-head\"><span class=\"ch-code\">PLAYBOOK</span><h2>실무 플레이북</h2></div>\n        <p class=\"lede\">컨테이너·오케스트레이션·Health Check 플레이북은 <strong>RUNTIME ORCHESTRATION PLAYBOOK</strong> 기준으로 증상, 범위, 최근 변경, 계층별 증거를 연결한다. 목표는 명령어 암기가 아니라 어떤 가설을 버리고 어떤 완화책을 실행할지 결정하는 것이다.</p>\n        <div class=\"serial-card\">\n          <span class=\"sc-label\">RUNTIME ORCHESTRATION PLAYBOOK</span>\n          symptom → scope → recent change → evidence<br>\n          → mitigation → verification → runbook diff\n        </div>\n        <table>\n          <tr><th>운영 표면</th><th>볼 증거</th><th>판단 기준</th></tr>\n          <tr><td>Pod / Task health</td><td>readiness, liveness, startup probe, restart reason, exit code</td><td>재시작이 회복인지 증상 악화인지 구분한다.</td></tr>\n          <tr><td>Traffic</td><td>service selector, target group, ingress rule, connection draining</td><td>정상 pod가 traffic을 받는 경로를 확인한다.</td></tr>\n          <tr><td>Capacity</td><td>request/limit, throttling, OOM, HPA signal, queue lag</td><td>CPU 평균보다 saturation과 tail latency를 본다.</td></tr>\n        </table>\n        <table>\n          <tr><th>확인 단계</th><th>명령·확인 위치</th><th>해석 기준</th></tr>\n          <tr><td>상태</td><td>kubectl get pods, describe pod, logs --previous, rollout status</td><td>CrashLoop, OOMKilled, probe failure를 분리한다.</td></tr>\n          <tr><td>네트워크</td><td>service endpoints, ingress events, target health, connection draining</td><td>selector mismatch와 readiness 누락을 찾는다.</td></tr>\n          <tr><td>리소스</td><td>CPU throttling, memory working set, ephemeral storage, file descriptor</td><td>limit만 올리기 전에 원인 지표를 확인한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch8\">\n        <div class=\"ch-head\"><span class=\"ch-code\">PRACTICE</span><h2>출력 판독 훈련</h2></div>\n        <p class=\"lede\">컨테이너·오케스트레이션·Health Check는 개념을 아는 것보다 실제 출력에서 정상과 비정상을 구분하는 훈련이 중요하다. 아래 lab은 명령, 출력, 판단, 완화, 영구 수정을 한 번에 연결한다.</p>\n        <div class=\"practice-lab\">\n          <span class=\"sc-label\">PRACTICE LAB 01 · OOMKilled 판독</span>\n          <p>배포 후 일부 pod가 재시작되고 p99 latency가 증가한다.</p>\n          <table>\n            <tr><th>Command / Query</th><th>목적</th></tr>\n            <tr><td><pre><code>kubectl describe pod api-7d9f -n production\n\nkubectl logs api-7d9f -n production --previous</code></pre></td><td>재시작 원인이 liveness 실패인지 OOMKilled인지 구분한다.</td></tr>\n          </table>\n          <table>\n            <tr><th>정상 출력</th><th>비정상 출력</th><th>판단 훈련</th></tr>\n            <tr><td><pre><code>Last State: Terminated\nReason: Completed\nRestart Count: 0</code></pre></td><td><pre><code>Last State: Terminated\nReason: OOMKilled\nExit Code: 137\nRestart Count: 8</code></pre></td><td>앱 오류가 아니라 memory limit 또는 leak, traffic spike로 인한 OOM 후보가 강하다.</td></tr>\n          </table>\n          <table>\n            <tr><th>즉시 완화</th><th>영구 수정</th><th>검증 기준</th></tr>\n            <tr><td>rollout pause, replica scale-out, memory limit 임시 상향, heavy endpoint rate limit.</td><td>memory profile, request/limit 재조정, load test, alert에 OOMKilled count 추가.</td><td>restart count 안정화, p99 회복, memory working set이 limit 아래 유지.</td></tr>\n          </table>\n        </div>\n        <div class=\"practice-lab\">\n          <span class=\"sc-label\">PRACTICE LAB 02 · Service selector mismatch 판독</span>\n          <p>pod는 Running인데 서비스가 503을 반환한다.</p>\n          <table>\n            <tr><th>Command / Query</th><th>목적</th></tr>\n            <tr><td><pre><code>kubectl get endpoints api -n production\n\nkubectl get svc api -o yaml</code></pre></td><td>Service selector가 실제 pod label과 연결되는지 확인한다.</td></tr>\n          </table>\n          <table>\n            <tr><th>정상 출력</th><th>비정상 출력</th><th>판단 훈련</th></tr>\n            <tr><td><pre><code>ENDPOINTS: 10.0.12.4:8080,10.0.13.8:8080\nselector: app=api</code></pre></td><td><pre><code>ENDPOINTS: &lt;none&gt;\nselector: app=api-v2</code></pre></td><td>runtime은 정상이나 Service가 traffic target을 찾지 못한다.</td></tr>\n          </table>\n          <table>\n            <tr><th>즉시 완화</th><th>영구 수정</th><th>검증 기준</th></tr>\n            <tr><td>selector rollback 또는 pod label hotfix.</td><td>deployment/service label contract test와 rollout smoke test를 추가한다.</td><td>endpoints populated, target health healthy, 503 감소.</td></tr>\n          </table>\n        </div>\n      </section>\n\n      <section id=\"ch9\">\n        <div class=\"ch-head\"><span class=\"ch-code\">TERM</span><h2>용어사전</h2></div>\n        <p class=\"lede\">컨테이너·오케스트레이션·Health Check에서 반복적으로 등장하는 운영 용어를 정의, 증거, 오해, 관련 항목 기준으로 정리한다. 장애 중에는 용어 자체보다 해당 용어가 어떤 로그, 지표, 설정, 책임 경계와 연결되는지를 확인한다.</p>\n        <table>\n          <tr><th>용어</th><th>정의</th><th>운영 증거</th><th>자주 하는 오해</th><th>관련 항목</th></tr>\n          <tr><td>cgroup limit</td><td>컨테이너 CPU, memory 등 리소스 사용 제한.</td><td>CPU throttling, memory working set, OOMKilled</td><td>컨테이너는 VM처럼 독립 리소스를 자동 보장한다.</td><td>request/limit, OOM, throttling</td></tr>\n          <tr><td>Deployment</td><td>Kubernetes에서 원하는 replica 상태와 rollout을 관리하는 객체.</td><td>desired state, rollout status, ReplicaSet</td><td>Deployment가 정상이면 트래픽도 정상이다.</td><td>ReplicaSet, Pod, Service</td></tr>\n          <tr><td>selector</td><td>Service가 traffic을 보낼 Pod를 label로 고르는 조건.</td><td>service selector, endpoints, pod label</td><td>pod Running이면 Service endpoint에 자동 포함된다.</td><td>label, endpoint, Service</td></tr>\n          <tr><td>readiness</td><td>Pod가 트래픽을 받을 준비가 됐는지 판단하는 probe.</td><td>readiness status, endpoint population</td><td>liveness와 같은 endpoint를 쓰면 된다.</td><td>liveness, startup probe, target health</td></tr>\n          <tr><td>liveness</td><td>Pod 재시작이 필요한지를 판단하는 probe.</td><td>liveness restart, probe event</td><td>DB 지연까지 liveness에 넣어야 안전하다.</td><td>restart, dependency status, probe path</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch10\">\n        <div class=\"ch-head\"><span class=\"ch-code\">CASE</span><h2>장애/운영 시나리오</h2></div>\n        <p class=\"lede\">배포 중 일부 요청이 502로 실패한다. 새 pod가 readiness 통과 전에 트래픽을 받는지, 종료 pod가 connection draining 없이 내려가는지, graceful shutdown 시간보다 terminationGracePeriod가 짧은지 확인한다.</p>\n        <div class=\"callout\">\n          <span class=\"co-label\">Triage frame</span>\n          <p>컨테이너·오케스트레이션·Health Check 장애는 증상 → 영향 범위 → 최근 변경 → Pod / Task health 증거 → 완화책 → 복구 검증 → 재발 방지 순서로 기록한다. 이 흐름이 있어야 담당자가 바뀌어도 같은 결론에 도달한다.</p>\n        </div>\n        <table>\n          <tr><th>처음 10분</th><th>다음 30분</th><th>종료 조건</th></tr>\n          <tr><td>readiness, liveness, startup probe, restart reason, exit code 기준으로 영향 범위를 고정한다.</td><td>kubectl get pods, describe pod, logs --previous, rollout status에서 증거를 모아 완화책을 실행한다.</td><td>다음 배포에서 자동으로 깨지는지 확인한다.</td></tr>\n        </table>\n        <table>\n          <tr><th>Incident packet</th><th>기록할 내용</th><th>판정 기준</th></tr>\n          <tr><td>증상</td><td>rolling update 중 일부 요청 502</td><td>readiness, drain, terminationGracePeriod, target deregistration delay를 본다.</td></tr>\n          <tr><td>완화</td><td>rollout pause/undo, scale out, bad pod drain</td><td>새 버전의 폭발 반경을 즉시 줄인다.</td></tr>\n          <tr><td>사후</td><td>probe와 graceful shutdown을 실제 요청으로 검증</td><td>다음 배포에서 자동으로 깨지는지 확인한다.</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch11\">\n        <div class=\"ch-head\"><span class=\"ch-code\">RISK</span><h2>자주 틀리는 판단</h2></div>\n        <p class=\"lede\">컨테이너·오케스트레이션·Health Check에서 자주 틀리는 판단은 컨테이너 런타임와 배포 안정성의 책임 경계를 흐리는 데서 시작된다. 아래 신호가 보이면 설계, runbook, 답변 구조를 다시 점검한다.</p>\n        <ul>\n          <li>liveness probe로 DB 상태까지 검사해 전체 pod를 재시작시킨다.</li>\n          <li>readiness 없이 rolling update만 설정한다.</li>\n          <li>resource limit만 두고 request를 비워 scheduling을 불안정하게 만든다.</li>\n          <li>HPA를 CPU 하나로만 판단한다.</li>\n        </ul>\n      </section>\n\n      <section id=\"ch12\">\n        <div class=\"ch-head\"><span class=\"ch-code\">Q&A</span><h2>면접 답변 템플릿</h2></div>\n        <p class=\"lede\">컨테이너 운영은 image 실행이 아니라 desired state, health check, resource isolation, traffic routing, graceful shutdown을 맞추는 일이라고 답합니다. readiness와 liveness를 구분하는 것이 무중단 배포의 기본입니다.</p>\n        <div class=\"serial-card\">\n          <span class=\"sc-label\">OPERATIONS ANSWER FRAME</span>\n          1. 오케스트레이션은 desired state, health, routing, capacity의 조합이다.<br>\n          2. readiness와 liveness를 분리해 무중단 배포를 설명한다.<br>\n          3. rolling update는 drain과 graceful shutdown까지 맞아야 한다.<br>\n        </div>\n      </section>\n<footer>\nOPERATIONS HANDBOOK · 2026-06<br>\nPRINCIPLE : 운영 지식은 경로, 증거, 복구 절차로 검증한다.\n</footer>",
};

export default document;
