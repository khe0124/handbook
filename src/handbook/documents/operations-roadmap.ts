import type { HandbookDocumentContent } from "../types";

const document: HandbookDocumentContent = {
  navHtml: "<div class=\"nav-brand\">OPERATIONS · 인프라</div>\n  <div class=\"nav-title\">인프라·운영 로드맵</div>\n  <a href=\"#chindex\"><span class=\"code\">INDEX</span>문서 구조</a>\n  <a href=\"#ch1\"><span class=\"code\">OP-00</span>운영 전체 지도</a>\n  <a href=\"#ch2\"><span class=\"code\">OP-01</span>4주 학습 순서</a>\n  <a href=\"#ch3\"><span class=\"code\">OP-02</span>계층별 실패 모드</a>\n  <a href=\"#ch4\"><span class=\"code\">OP-03</span>운영 산출물과 답변 구조</a>\n  <a href=\"#ch5\"><span class=\"code\">MODEL</span>개념 모델</a>\n  <a href=\"#ch6\"><span class=\"code\">CHECK</span>실무 체크리스트</a>\n  <a href=\"#ch7\"><span class=\"code\">PLAYBOOK</span>실무 플레이북</a>\n  <a href=\"#ch8\"><span class=\"code\">PRACTICE</span>출력 판독 훈련</a>\n  <a href=\"#ch9\"><span class=\"code\">TERM</span>용어사전</a>\n  <a href=\"#ch10\"><span class=\"code\">CASE</span>장애/운영 시나리오</a>\n  <a href=\"#ch11\"><span class=\"code\">RISK</span>자주 틀리는 판단</a>\n  <a href=\"#ch12\"><span class=\"code\">Q&A</span>면접 답변 템플릿</a>",
  mainHtml: "<header class=\"hero\">\n  <div class=\"hero-serial\">\n    <span>DOC : OPERATIONS-ROADMAP</span>\n    <span>SCOPE : OPERATIONS · RELIABILITY · INCIDENT RESPONSE</span>\n    <span>LANG : KO</span>\n  </div>\n  <h1>인프라·운영 로드맵</h1>\n  <p class=\"hero-sub\">서비스를 사용자 요청에서 장애 복구까지 하나의 운영 시스템으로 읽는 기준</p>\n  <div class=\"hero-meta\">FORMAT : 개념 → 증거 → 플레이북 → 장애 시나리오 → 면접 답변 · UPDATED 2026-06</div>\n</header>\n\n<section id=\"chindex\">\n<div class=\"ch-head\"><span class=\"ch-code\">INDEX</span><h2>문서 구조</h2></div>\n<p class=\"lede\">이 문서는 개념 요약이 아니라 운영 판단 연습용 플레이북입니다. 각 항목은 “무엇을 아는가”보다 “장애 때 어떤 증거를 보고 어떤 가설을 버리는가”를 기준으로 읽습니다.</p>\n<ul><li>핵심 개념</li><li>주제별 운영 증거</li><li>실무 플레이북</li><li>용어사전</li><li>Incident packet</li><li>주제별 답변 구조</li></ul>\n      <h3>대상 독자와 학습 계약</h3>\n        <table>\n          <tr><th>항목</th><th>내용</th><th>완료 기준</th></tr>\n          <tr><td>대상 독자</td><td>미들급 개발자. API, DB, 배포 경험은 있으나 DNS, LB, VPC, SLO, DR을 한 시스템으로 설명하지 못하는 사람</td><td>서비스 장애를 계층별 증거로 좁히고 운영 인수 산출물을 만들 수 있어야 한다.</td></tr>\n          <tr><td>선행 지식</td><td>HTTP, TLS 기초, Linux process/log, DB connection, CI/CD 기본, container 기본</td><td>이 지식이 없으면 도구명을 외우는 학습으로 흐르므로 먼저 보완한다.</td></tr>\n          <tr><td>학습 산출물</td><td>request path diagram, deploy timeline, incident packet, rollback/restore drill report, service readiness score</td><td>말로 아는 것이 아니라 다른 담당자가 재현 가능한 문서와 증거를 제출해야 한다.</td></tr>\n          <tr><td>강연 목표</td><td>청중이 장애 상황에서 어떤 계층을 먼저 확인하고 어떤 증거로 배제할지 말할 수 있게 한다.</td><td>서비스명 암기가 아니라 판단 순서, 증거, 완화, 후속 개선을 남긴다.</td></tr>\n        </table>\n      <h3>로드맵으로 읽는 순서</h3>\n        <table>\n          <tr><th>단계</th><th>읽을 문서</th><th>완료 산출물</th></tr>\n          <tr><td>1주차 · 요청 경로</td><td>서비스 요청 경로 → DNS/TLS → LB/App/DB 경계</td><td>request path diagram, dig/curl/openssl 결과, target health 판독</td></tr>\n          <tr><td>2주차 · 네트워크 경계</td><td>VPC·Subnet·Routing·NAT → 보안 경계 → VPN/Private Connectivity</td><td>route table readback, flow log, SG/NACL 판정, private endpoint DNS 검증</td></tr>\n          <tr><td>3주차 · 변경과 런타임</td><td>CI/CD·Artifact·Environment → 컨테이너·오케스트레이션 → IaC·변경관리</td><td>release note, artifact digest, health check gate, terraform plan review</td></tr>\n          <tr><td>4주차 · 관측과 복구</td><td>Observability·SLO → Incident Response·Rollback·DR → Cloud 시나리오</td><td>SLO burn alert, incident packet, rollback/restore drill, postmortem action</td></tr>\n        </table>\n      <h3>60분 강연 흐름</h3>\n        <table>\n          <tr><th>시간</th><th>주제</th><th>핵심 메시지</th></tr>\n          <tr><td>0-5분</td><td>왜 운영은 아키텍처 그림만으로 부족한가</td><td>요청 경로, 변경 경로, 복구 경로를 한 장으로 연결해야 함을 보여준다.</td></tr>\n          <tr><td>5-15분</td><td>전체 운영 지도</td><td>DNS → Edge → LB → Runtime → Data store와 CI/CD → artifact → runtime → metric gate를 겹쳐 설명한다.</td></tr>\n          <tr><td>15-30분</td><td>장애 triage 실전</td><td>5xx 증가 케이스로 사용자 영향, 최근 변경, 계층별 증거, rollback 판단을 연습한다.</td></tr>\n          <tr><td>30-42분</td><td>네트워크·보안 경계</td><td>VPC, subnet, route, SG/NACL, Private Endpoint, DNS override의 대표 오판을 짚는다.</td></tr>\n          <tr><td>42-52분</td><td>SLO·Incident·DR</td><td>burn rate, IC/scribe/comms, RPO/RTO, restore drill을 운영 성숙도 기준으로 연결한다.</td></tr>\n          <tr><td>52-60분</td><td>체크리스트와 면접 답변</td><td>운영 인수 packet과 30초/90초 답변 구조로 마무리한다.</td></tr>\n        </table>\n      <h3>계층별 실패 모드</h3>\n        <table>\n          <tr><th>계층</th><th>대표 실패</th><th>첫 확인 증거</th></tr>\n          <tr><td>DNS/TLS</td><td>NXDOMAIN, wrong CNAME, SNI/SAN mismatch, chain 누락</td><td>dig +trace, resolver 비교, openssl s_client, curl -v</td></tr>\n          <tr><td>Edge/CDN/WAF</td><td>WAF false positive, stale cache, origin policy mismatch</td><td>edge request id, WAF log, cache policy, origin status</td></tr>\n          <tr><td>Load Balancer</td><td>502 upstream fail, 503 no healthy target, 504 timeout</td><td>target health, listener rule, deregistration delay, idle timeout</td></tr>\n          <tr><td>Runtime</td><td>OOMKilled, readiness 실패, selector mismatch, graceful shutdown 누락</td><td>kubectl describe/logs --previous, endpoints, rollout status</td></tr>\n          <tr><td>Data store</td><td>connection pool 고갈, lock wait, slow query, migration lock</td><td>pool pending, DB wait event, slow query, migration version</td></tr>\n          <tr><td>Network policy</td><td>route miss, SG/NACL block, NAT saturation, private DNS 누락</td><td>route table, flow log ACCEPT/REJECT, NAT metric, DNS query log</td></tr>\n          <tr><td>Change path</td><td>잘못된 artifact/config/secret/migration 배포</td><td>commit SHA, image digest, config diff, secret version, deploy marker</td></tr>\n          <tr><td>Recovery path</td><td>rollback 불가, restore 미검증, failback 누락</td><td>rollback command, backup age, restore duration, reconciliation result</td></tr>\n        </table>\n      <h3>AWS/Azure 증거 비교</h3>\n        <table>\n          <tr><th>영역</th><th>AWS 증거</th><th>Azure 증거</th><th>판단 기준</th></tr>\n          <tr><td>DNS/Edge</td><td>Route 53, CloudFront, AWS WAF, ACM, ALB access log</td><td>Azure DNS, Front Door, WAF Policy, Managed Certificate, Application Gateway log</td><td>edge request id와 origin status를 분리해 WAF 차단과 origin 장애를 나눈다.</td></tr>\n          <tr><td>Network</td><td>VPC, subnet route table, Security Group, NACL, NAT Gateway, VPC Flow Logs, PrivateLink</td><td>VNet, subnet route table, NSG, NAT Gateway, Network Watcher flow log, Private Endpoint</td><td>route miss, policy reject, DNS public resolution, NAT saturation을 각각 다른 원인으로 본다.</td></tr>\n          <tr><td>Runtime</td><td>ECS/EKS, target group health, CloudWatch Container Insights, ECR image digest</td><td>AKS/App Service, backend health, Azure Monitor, ACR digest</td><td>readiness, rollout, image/config mismatch, connection draining을 배포 timeline에 연결한다.</td></tr>\n          <tr><td>Data/DR</td><td>RDS/Aurora metrics, Performance Insights, snapshots, PITR, replica lag</td><td>Azure SQL metrics, Query Performance Insight, backups, PITR, geo-replica lag</td><td>백업 존재가 아니라 restore duration, backup age, missing rows, failback 결과로 판단한다.</td></tr>\n        </table>\n      <h3>운영 의사결정 트리</h3>\n        <table>\n          <tr><th>질문</th><th>확인 증거</th><th>다음 선택</th></tr>\n          <tr><td>1. 사용자가 실제로 영향을 받는가?</td><td>SLI, support ticket, synthetic check, region/device 분포</td><td>영향이 없으면 ticket으로 낮추고, 영향이 있으면 incident channel과 IC를 세운다.</td></tr>\n          <tr><td>2. 최근 변경과 같은 시간축인가?</td><td>deploy marker, image digest, config diff, migration version, feature flag audit</td><td>같은 시간축이면 rollback/flag off 후보를 먼저 준비한다. 아니면 요청 경로와 의존성 장애를 본다.</td></tr>\n          <tr><td>3. 요청이 어디까지 도달했는가?</td><td>DNS answer, WAF request id, LB target health, app access log, trace span, DB wait event</td><td>도달하지 못한 첫 계층이 현재의 가장 강한 원인 후보가 된다.</td></tr>\n          <tr><td>4. 원인 확정 전 완화가 가능한가?</td><td>rollback command, traffic shift, circuit breaker, rate limit, queue pause, read-only mode</td><td>사용자 영향을 줄이는 조치가 있으면 원인 분석과 병행한다.</td></tr>\n          <tr><td>5. 복구가 검증됐는가?</td><td>SLI 회복, error budget burn 정상화, data reconciliation, customer comms, runbook diff</td><td>지표 회복만으로 종료하지 말고 데이터·공지·문서 갱신까지 확인한다.</td></tr>\n        </table>\n      <h3>운영 준비도 점수표</h3>\n        <table>\n          <tr><th>점수</th><th>상태</th><th>판정</th></tr>\n          <tr><td>0점</td><td>문서 없음 또는 개인 기억에 의존</td><td>런칭/인수 차단</td></tr>\n          <tr><td>1점</td><td>다이어그램과 담당자만 있음</td><td>장애 때 실행 불가. owner, dashboard, runbook 보강 필요</td></tr>\n          <tr><td>2점</td><td>dashboard, alert, runbook, rollback plan이 있음</td><td>기본 운영 가능. drill 증거가 없으면 위험</td></tr>\n          <tr><td>3점</td><td>rollback/restore drill, SLO, incident packet, cost/access owner까지 있음</td><td>독립 운영 가능</td></tr>\n          <tr><td>4점</td><td>game day, failback, runbook diff, error budget policy가 반복 운영됨</td><td>팀 표준화 가능</td></tr>\n        </table>\n      <h3>강연 데모 스크립트</h3>\n        <table>\n          <tr><th>데모</th><th>주제</th><th>진행 방식</th><th>확인 질문</th></tr>\n          <tr><td>데모 1</td><td>정상 요청 경로 그리기</td><td>브라우저에서 DB까지 6개 hop을 그리고 각 hop의 증거를 하나씩 붙인다.</td><td>청중이 app log 이전 계층의 증거를 말할 수 있어야 한다.</td></tr>\n          <tr><td>데모 2</td><td>5xx fast burn triage</td><td>incident snapshot을 보여주고 rollback, scale-out, DB restart 중 무엇을 먼저 할지 투표시킨다.</td><td>최근 변경과 DB baseline을 근거로 rollback 후보를 고르는지 확인한다.</td></tr>\n          <tr><td>데모 3</td><td>private subnet egress 오판</td><td>flow log ACCEPT와 NAT bytes 4x를 함께 보여준다.</td><td>네트워크 차단이 아니라 retry storm/외부 API 지연을 의심하는지 본다.</td></tr>\n          <tr><td>데모 4</td><td>restore drill 채점</td><td>backup_age, restore_duration, missing_rows를 보여주고 RPO/RTO 판정을 시킨다.</td><td>복구 성공과 목표 충족을 구분하는지 확인한다.</td></tr>\n        </table>\n      <h3>실습 채점 루브릭</h3>\n        <table>\n          <tr><th>수준</th><th>답변 특징</th><th>증거 사용</th><th>판정</th></tr>\n          <tr><td>초급</td><td>도구 이름과 서비스명을 나열한다.</td><td>장애 때 앱 로그나 콘솔 화면 하나에 의존한다.</td><td>불합격. 운영 판단 순서와 증거 연결이 없다.</td></tr>\n          <tr><td>중급</td><td>요청 경로와 변경 경로를 그리고 주요 증거를 확인한다.</td><td>rollback 후보와 owner를 말하지만 drill 증거가 약하다.</td><td>내부 운영 참여 가능. 공개 강연/출판용 설명으로는 보강 필요.</td></tr>\n          <tr><td>상급</td><td>사용자 영향, 최근 변경, 계층별 증거, 완화, 사후 개선을 incident packet으로 닫는다.</td><td>SLO burn, 비용, 보안, DR까지 같은 timeline에서 설명한다.</td><td>강연/출판 사례의 기준점으로 삼을 수 있다.</td></tr>\n          <tr><td>리뷰어</td><td>청중/독자가 같은 결론에 도달하도록 출력 샘플, 오판 사례, 채점 기준을 제공한다.</td><td>클라우드별 차이와 조직 운영 체계의 한계를 명시한다.</td><td>외부 공개 전 기술 리뷰를 맡길 수 있는 수준.</td></tr>\n        </table>\n      <h3>전문가 리뷰 질문</h3>\n        <table>\n          <tr><th>리뷰 영역</th><th>확인 질문</th><th>보강 기준</th></tr>\n          <tr><td>SRE 리뷰</td><td>SLO, burn rate, incident role, postmortem action이 실제 운영 정책으로 이어지는가?</td><td>알림 피로, error budget 정책, paging 기준이 빠져 있으면 보강한다.</td></tr>\n          <tr><td>Cloud network 리뷰</td><td>route table, SG/NACL/NSG, DNS override, private endpoint, NAT 비용 설명이 provider별로 정확한가?</td><td>AWS/Azure 차이를 서비스명 치환으로 처리한 부분을 수정한다.</td></tr>\n          <tr><td>Kubernetes/runtime 리뷰</td><td>readiness/liveness/startup, graceful shutdown, rollout, target health가 정확히 분리되어 있는가?</td><td>무중단 배포와 health check를 섞어 설명한 부분을 수정한다.</td></tr>\n          <tr><td>DB/DR 리뷰</td><td>connection pool, lock wait, migration lock, PITR, replica lag, failback 설명이 실제 복구 절차와 맞는가?</td><td>백업 존재와 복구 가능성을 혼동한 표현을 제거한다.</td></tr>\n          <tr><td>보안 리뷰</td><td>IAM/Managed Identity, secret rotation, audit log, incident comms가 과도하게 단순화되지 않았는가?</td><td>장기 키, broad permission, secret leakage 대응을 명확히 한다.</td></tr>\n        </table>\n      <h3>공식 근거 맵</h3>\n        <table>\n          <tr><th>영역</th><th>공식 문서</th><th>URL</th><th>반영 기준</th></tr>\n          <tr><td>SLO/Burn rate</td><td>Google SRE Workbook · Alerting on SLOs</td><td>https://sre.google/workbook/alerting-on-slos/</td><td>burn rate, error budget, multi-window alert는 서비스별 baseline과 page load에 맞춰 조정해야 한다.</td></tr>\n          <tr><td>Kubernetes probes</td><td>Kubernetes Docs · Liveness, Readiness, and Startup Probes</td><td>https://kubernetes.io/docs/concepts/workloads/pods/probes/</td><td>liveness는 재시작, readiness는 traffic 수신 여부, startup은 느린 시작 보호로 분리한다.</td></tr>\n          <tr><td>AWS VPC Flow Logs</td><td>AWS Docs · Flow log records</td><td>https://docs.aws.amazon.com/vpc/latest/userguide/flow-log-records.html</td><td>ACCEPT/REJECT는 aggregation interval과 log-status를 함께 봐야 하며 delivery delay가 있을 수 있다.</td></tr>\n          <tr><td>AWS PrivateLink</td><td>AWS Docs · AWS PrivateLink concepts</td><td>https://docs.aws.amazon.com/vpc/latest/privatelink/concepts.html</td><td>endpoint 생성만으로 충분하지 않고 DNS, endpoint policy, route/security 경계를 함께 확인한다.</td></tr>\n          <tr><td>Azure Private Endpoint</td><td>Microsoft Learn · Private endpoint overview</td><td>https://learn.microsoft.com/en-us/azure/private-link/private-endpoint-overview</td><td>private endpoint는 private IP로 Azure service에 연결하므로 Private DNS Zone 연결과 public access 정책을 같이 본다.</td></tr>\n          <tr><td>AWS ALB health</td><td>AWS Docs · Target group health checks</td><td>https://docs.aws.amazon.com/elasticloadbalancing/latest/application/target-group-health-checks.html</td><td>target health는 health check path, matcher, timeout, deregistration/draining과 함께 해석한다.</td></tr>\n          <tr><td>AWS RDS PITR</td><td>AWS Docs · Restoring a DB instance to a specified time</td><td>https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_PIT.html</td><td>PITR은 새 DB instance를 만들며 latest restorable time, parameter/security group, engine 제약을 확인해야 한다.</td></tr>\n          <tr><td>Azure SQL restore</td><td>Microsoft Learn · Restore a database from a backup</td><td>https://learn.microsoft.com/en-us/azure/azure-sql/database/recovery-using-backups</td><td>restore 가능성과 RPO/RTO 충족은 다르므로 restore duration과 data reconciliation을 따로 측정한다.</td></tr>\n        </table>\n      <h3>정확도 한계와 공개 시 주의 문구</h3>\n        <table>\n          <tr><th>영역</th><th>공격받기 쉬운 지점</th><th>문서의 안전한 표현</th></tr>\n          <tr><td>클라우드 서비스명</td><td>AWS/Azure 서비스명은 계속 바뀌고 기능도 region/sku에 따라 다르다.</td><td>문서의 서비스 매핑은 책임 경계 학습용이며, 실제 설계 전 공식 문서와 현재 계정 설정을 확인한다.</td></tr>\n          <tr><td>SLO 임계값</td><td>14.4x, 6x 같은 burn rate 예시는 출발점이지 절대값이 아니다.</td><td>트래픽 규모, on-call 부담, error budget policy에 맞춰 page/ticket 기준을 재산정한다.</td></tr>\n          <tr><td>Flow log 판독</td><td>flow log는 aggregation과 delivery delay가 있고 일부 metadata는 best effort다.</td><td>패킷 단위 진실로 단정하지 말고 app log, target health, DNS query log와 교차 검증한다.</td></tr>\n          <tr><td>Kubernetes probe</td><td>probe 설정은 runtime, startup 특성, downstream 의존성에 따라 달라진다.</td><td>liveness에 DB 등 외부 의존성을 넣어 전체 재시작 루프를 만들지 않도록 리뷰한다.</td></tr>\n          <tr><td>DR/RPO/RTO</td><td>백업이 존재해도 복구 목표를 만족한다는 뜻은 아니다.</td><td>restore drill, reconciliation, failback, customer communication을 따로 검증한다.</td></tr>\n          <tr><td>보안/IAM</td><td>권한과 secret 운영은 조직의 identity, audit, compliance 정책에 종속된다.</td><td>예시는 최소 기준이며 실제 운영 전 security owner review를 필수로 둔다.</td></tr>\n        </table>\n      <h3>벤더별 세부 함정</h3>\n        <table>\n          <tr><th>영역</th><th>함정</th><th>검증 방식</th></tr>\n          <tr><td>Route 53 / Azure DNS</td><td>record가 맞아도 resolver cache, delegation, split-horizon/private DNS가 다르면 사용자는 실패할 수 있다.</td><td>authoritative answer, public resolver, private resolver를 분리해 기록한다.</td></tr>\n          <tr><td>CloudFront / Front Door</td><td>edge 403, stale cache, origin 5xx는 같은 화면 오류로 보일 수 있다.</td><td>edge request id, cache status, WAF log, origin status를 같은 request timeline에 붙인다.</td></tr>\n          <tr><td>ALB / Application Gateway</td><td>health check 성공은 business readiness 보장이 아니다.</td><td>health path가 dependency-free인지, matcher/timeout/draining이 배포 전략과 맞는지 확인한다.</td></tr>\n          <tr><td>PrivateLink / Private Endpoint</td><td>endpoint를 만들어도 DNS가 public endpoint를 반환하면 사설 경로가 아니다.</td><td>private DNS, endpoint policy, route/security rule, public access setting을 함께 검증한다.</td></tr>\n          <tr><td>EKS/AKS/App Service</td><td>새 버전이 뜬 것과 traffic을 받아도 되는 것은 다르다.</td><td>readiness, startup, rollout status, target/backend health, image digest를 연결한다.</td></tr>\n          <tr><td>RDS / Azure SQL</td><td>PITR 가능과 실제 업무 복구 가능은 다르다.</td><td>latest restorable time, restore duration, schema/app compatibility, missing rows reconciliation을 기록한다.</td></tr>\n        </table>\n      <h3>재현 실습 패킷</h3>\n        <table>\n          <tr><th>항목</th><th>내용</th><th>채점/운영 기준</th></tr>\n          <tr><td>입력 데이터</td><td>incident snapshot, edge evidence, egress evidence, restore drill output</td><td>각 lab은 정상/비정상 출력과 판단 기준을 같이 제공한다.</td></tr>\n          <tr><td>학습자 제출물</td><td>원인 후보 3개, 배제한 증거, 즉시 완화 1개, 영구 수정 2개, 종료 조건</td><td>원인 정답보다 증거 기반 의사결정 과정을 채점한다.</td></tr>\n          <tr><td>채점 방식</td><td>증거 연결 40%, 완화 선택 25%, 영구 수정 20%, 커뮤니케이션/owner 15%</td><td>서비스명 암기와 단일 로그 의존 답변은 감점한다.</td></tr>\n          <tr><td>재현 범위</td><td>실제 cloud 계정 없이도 읽을 수 있는 synthetic packet</td><td>실제 실습으로 확장할 때는 sandbox 계정, 비용 한도, 삭제 스크립트, IAM boundary를 추가한다.</td></tr>\n        </table>\n      <h3>출판 전 검수 기준</h3>\n        <table>\n          <tr><th>검수 영역</th><th>확인 질문</th><th>탈락 기준</th></tr>\n          <tr><td>개념 정확성</td><td>DNS/TLS, LB, VPC, runtime, DB, SLO, DR 용어가 책임 경계와 함께 설명되는가?</td><td>서비스명 번역이나 벤더 마케팅 문구로 대체하지 않는다.</td></tr>\n          <tr><td>사례 완결성</td><td>각 케이스가 증상, timeline, 증거, 완화, 영구 수정, 종료 조건을 포함하는가?</td><td>장애 원인만 맞히는 퍼즐이 아니라 운영 판단 훈련이어야 한다.</td></tr>\n          <tr><td>실습 재현성</td><td>정상/비정상 출력, 해석, mitigation, verification이 모두 있는가?</td><td>명령어만 던지고 정답 기준을 생략하지 않는다.</td></tr>\n          <tr><td>강연 가능성</td><td>60분 흐름, 청중 수준, 데모 지점, Q&amp;A 답변 구조가 있는가?</td><td>슬라이드로 옮겼을 때 메시지가 순서대로 쌓여야 한다.</td></tr>\n          <tr><td>전문 리뷰</td><td>SRE/클라우드 네트워크/Kubernetes/IaC/DB 운영 관점의 리뷰를 받을 지점이 표시되는가?</td><td>출판 전 최소 1회 이상 실제 운영자 리뷰를 거친다.</td></tr>\n        </table>\n</section>\n\n      <section id=\"ch1\">\n        <div class=\"ch-head\"><span class=\"ch-code\">OP-00</span><h2>운영 전체 지도</h2></div>\n        <p class=\"lede\">인프라·운영은 네트워크, 배포, 모니터링을 따로 외우는 영역이 아니다. 운영자는 request path, change path, control plane, data plane, recovery path를 한 장으로 연결해야 한다. 사용자의 요청은 DNS → CDN/WAF → Load Balancer → Runtime → Data store를 지나고, 변경은 CI/CD → artifact → config → runtime → metric gate → rollback 또는 incident response로 검증된다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>요청 경로, 변경 경로, 복구 경로가 같은 운영 지도에 연결되어 있는가?</td><td>request path map, deploy marker, service catalog, dependency map, SLO, rollback/restore owner</td><td>사용자 영향과 최근 변경, 완화 후보를 한 timeline에서 설명할 수 있어야 한다.</td></tr>\n          <tr><td>운영 전체 지도를 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>service catalog, dependency map, release note, SLO dashboard, incident runbook, rollback drill record</td><td>어느 계층이 사용자 영향과 연결되는지 15분 안에 찾고, 원인 확정 전 완화책을 고를 수 있어야 한다.</td></tr>\n          <tr><td>운영 전체 지도 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>요청 실패, deploy marker, SLO burn이 같은 시간대에 발생</td><td>요청 경로와 변경 경로를 동시에 좁히고 rollback, feature off, traffic shift 중 사용자 영향을 가장 빨리 줄이는 조치를 먼저 선택한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch2\">\n        <div class=\"ch-head\"><span class=\"ch-code\">OP-01</span><h2>4주 학습 순서</h2></div>\n        <p class=\"lede\">학습 순서는 서비스 장애를 좁히는 순서와 같아야 한다. 1주차에는 브라우저에서 DB까지 요청 경로를 그리고, 2주차에는 VPC, subnet, route table, NAT, ingress, egress, private endpoint를 붙인다. 3주차에는 CI/CD, artifact, runtime, health check, IaC 변경을 배포 경로로 묶고, 4주차에는 Observability, SLO, Incident Response, Rollback, Restore, DR drill로 운영 루프를 닫는다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>학습 순서가 실제 장애 triage와 운영 인수 순서로 이어지는가?</td><td>request path diagram, VPC route table readback, deploy pipeline stage, SLO dashboard, rollback/restore drill record</td><td>개념 이름을 외운 순서가 아니라 장애 때 배제할 계층과 산출물 순서로 정리되어야 한다.</td></tr>\n          <tr><td>4주 학습 순서를 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>draw request path, list ingress/egress, check deploy pipeline, run smoke/rollback drill, review SLO burn alert</td><td>각 단계가 확인 명령, 판독 기준, 제출 산출물로 이어지면 실무 학습 경로로 본다.</td></tr>\n          <tr><td>4주 학습 순서 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>네트워크는 설명하지만 배포 marker, SLO burn, rollback 가능성을 함께 설명하지 못함</td><td>다음 학습 항목을 runbook, dashboard, drill packet으로 연결해 운영 증거를 만든다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch3\">\n        <div class=\"ch-head\"><span class=\"ch-code\">OP-02</span><h2>계층별 실패 모드</h2></div>\n        <p class=\"lede\">운영 실력은 장애 때 어떤 계층을 먼저 의심하고 어떤 증거로 배제하는지에서 드러난다. DNS/TLS, CDN/WAF, Load Balancer, runtime, DB, network policy, deploy pipeline, observability, incident process마다 대표 실패 모드와 첫 확인 증거가 다르다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>각 계층의 실패 모드와 첫 확인 증거를 구분할 수 있는가?</td><td>DNS answer, TLS chain, WAF log, target health, trace waterfall, DB wait event, flow log, deploy marker, burn-rate alert</td><td>앱 로그만 보거나 클라우드 서비스명만 나열하면 운영 판단으로 부족하다.</td></tr>\n          <tr><td>계층별 실패 모드를 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>dig/curl/openssl, LB target health, kubectl describe/logs, DB performance insight, flow log, CI release note</td><td>증거는 같은 timestamp, traceId, version, environment로 연결되어야 한다.</td></tr>\n          <tr><td>계층별 실패 모드 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>프론트 화면 오류 하나를 보고 API 코드 문제로 단정</td><td>가장 앞단 계층부터 요청이 도달한 지점을 찾고, 최근 변경과 SLO burn을 함께 본다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch4\">\n        <div class=\"ch-head\"><span class=\"ch-code\">OP-03</span><h2>운영 산출물과 답변 구조</h2></div>\n        <p class=\"lede\">운영 문서는 아키텍처 다이어그램만으로 끝나지 않는다. owner, escalation, SLO, dashboard, alert, runbook, rollback plan, RPO/RTO, 변경 이력, 비용 owner, access owner, 마지막 drill 날짜가 있어야 다음 사람이 같은 판단을 반복할 수 있다. 면접에서도 도구 이름보다 경로, 경계, 실패 모드, 증거, 완화, 후속 개선 순서로 말해야 한다.</p>\n        <table>\n          <tr><th>운영 질문</th><th>확인할 증거</th><th>판단 기준</th></tr>\n          <tr><td>운영 산출물과 면접 답변이 실행 가능한 증거로 닫히는가?</td><td>owner matrix, service catalog, SLO, dashboard, alert, runbook, rollback command, restore drill date, incident packet</td><td>담당자가 바뀌어도 같은 절차와 권한으로 대응할 수 있어야 한다.</td></tr>\n          <tr><td>운영 산출물과 답변 구조를 원인 후보에서 배제하려면 어떤 확인 결과가 필요한가?</td><td>service catalog, alert route, dashboard link, rollback command, restore drill report, postmortem, runbook diff</td><td>문서 링크만 있고 권한, owner, 마지막 drill 날짜가 없으면 운영 인수 미완료다.</td></tr>\n          <tr><td>운영 산출물과 답변 구조 장애에서 임시 완화와 영구 수정은 어떻게 나누는가?</td><td>장애 중 dashboard, alert owner, rollback owner를 찾지 못함</td><td>인수 체크리스트에 owner, 권한, 마지막 drill, incident packet, runbook diff를 필수 항목으로 추가한다.</td></tr>\n        </table>\n\n      </section>\n\n      <section id=\"ch5\">\n        <div class=\"ch-head\"><span class=\"ch-code\">MODEL</span><h2>개념 모델</h2></div>\n        <p class=\"lede\">인프라·운영 로드맵은 단일 지식 항목이 아니라 운영 판단의 일부다. 아래 표처럼 개념을 책임, 확인 지표, 실패 신호로 나누면 면접 답변과 실제 장애 대응이 모두 선명해진다.</p>\n        <table>\n          <tr><th>구성 요소</th><th>운영 책임</th><th>확인할 증거</th></tr>\n          <tr><td>OP-00 · 운영 전체 지도</td><td>인프라·운영은 네트워크, 배포, 모니터링을 따로 외우는 영역이 아니다. 운영자는 request path, change path, control plane, data plane, recovery path를 한 장으로 연결해야 한다. 사용자의 요청은 DNS → CDN/WAF → Load Balancer → Runtime → Data store를 지나고, 변경은 CI/CD → artifact → config → runtime → metric gate → rollback 또는 incident response로 검증된다.</td><td>request path map, deploy marker, service catalog, dependency map, SLO, rollback/restore owner</td></tr>\n          <tr><td>OP-01 · 4주 학습 순서</td><td>학습 순서는 서비스 장애를 좁히는 순서와 같아야 한다. 1주차에는 브라우저에서 DB까지 요청 경로를 그리고, 2주차에는 VPC, subnet, route table, NAT, ingress, egress, private endpoint를 붙인다. 3주차에는 CI/CD, artifact, runtime, health check, IaC 변경을 배포 경로로 묶고, 4주차에는 Observability, SLO, Incident Response, Rollback, Restore, DR drill로 운영 루프를 닫는다.</td><td>request path diagram, VPC route table readback, deploy pipeline stage, SLO dashboard, rollback/restore drill record</td></tr>\n          <tr><td>OP-02 · 계층별 실패 모드</td><td>운영 실력은 장애 때 어떤 계층을 먼저 의심하고 어떤 증거로 배제하는지에서 드러난다. DNS/TLS, CDN/WAF, Load Balancer, runtime, DB, network policy, deploy pipeline, observability, incident process마다 대표 실패 모드와 첫 확인 증거가 다르다.</td><td>DNS answer, TLS chain, WAF log, target health, trace waterfall, DB wait event, flow log, deploy marker, burn-rate alert</td></tr>\n          <tr><td>OP-03 · 운영 산출물과 답변 구조</td><td>운영 문서는 아키텍처 다이어그램만으로 끝나지 않는다. owner, escalation, SLO, dashboard, alert, runbook, rollback plan, RPO/RTO, 변경 이력, 비용 owner, access owner, 마지막 drill 날짜가 있어야 다음 사람이 같은 판단을 반복할 수 있다. 면접에서도 도구 이름보다 경로, 경계, 실패 모드, 증거, 완화, 후속 개선 순서로 말해야 한다.</td><td>owner matrix, service catalog, SLO, dashboard, alert, runbook, rollback command, restore drill date, incident packet</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch6\">\n        <div class=\"ch-head\"><span class=\"ch-code\">CHECK</span><h2>실무 체크리스트</h2></div>\n        <p class=\"lede\">인프라·운영 로드맵 체크리스트는 운영 전체 지도, 4주 학습 순서, 계층별 실패 모드, 운영 산출물과 답변 구조의 변경 위험을 사전에 드러내기 위한 검토표다. 각 항목은 리뷰, 배포 승인, 운영 인수에서 같은 증거로 다시 확인되어야 한다.</p>\n        <ul>\n          <li>요청 경로, 변경 경로, 복구 경로를 한 장에 연결한다.</li>\n          <li>owner, SLO, dashboard, alert, runbook, rollback plan, restore drill 날짜를 확인한다.</li>\n          <li>DNS/TLS, edge, LB, runtime, DB, network policy, deploy pipeline의 대표 실패 모드를 표시한다.</li>\n          <li>운영 인수 전에 rollback drill, restore drill, traffic shift drill 중 최소 하나를 실행한다.</li>\n          <li>비용 owner와 access owner를 서비스 카탈로그에 포함한다.</li>\n        </ul>\n        <table>\n          <tr><th>작업 단계</th><th>필수 산출물</th><th>차단 기준</th></tr>\n          <tr><td>설계</td><td>목적, 영향 범위, 비목표, owner</td><td>영향 범위가 사용자·시스템·비용 기준으로 설명되지 않음</td></tr>\n          <tr><td>변경</td><td>diff, 승인 기록, 검증 명령</td><td>production 변경과 staging 검증의 차이가 설명되지 않음</td></tr>\n          <tr><td>복구</td><td>rollback 또는 forward fix, 담당자, 예상 시간</td><td>실패 시 되돌릴 수 없는데 사전 승인과 공지가 없음</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch7\">\n        <div class=\"ch-head\"><span class=\"ch-code\">PLAYBOOK</span><h2>실무 플레이북</h2></div>\n        <p class=\"lede\">인프라·운영 로드맵 플레이북은 <strong>OPERATIONS READINESS PLAYBOOK</strong> 기준으로 증상, 범위, 최근 변경, 계층별 증거를 연결한다. 목표는 명령어 암기가 아니라 어떤 가설을 버리고 어떤 완화책을 실행할지 결정하는 것이다.</p>\n        <div class=\"serial-card\">\n          <span class=\"sc-label\">OPERATIONS READINESS PLAYBOOK</span>\n          symptom → scope → recent change → evidence<br>\n          → mitigation → verification → runbook diff\n        </div>\n        <table>\n          <tr><th>운영 표면</th><th>볼 증거</th><th>판단 기준</th></tr>\n          <tr><td>서비스 카탈로그</td><td>owner, tier, dependency map, SLO, cost owner, access owner</td><td>15분 안에 현재 상태와 담당자를 찾을 수 있어야 한다.</td></tr>\n          <tr><td>변경 경로</td><td>commit SHA, image digest, migration version, config diff, deploy marker</td><td>장애 시 어떤 변경을 의심할지 시간축으로 좁힌다.</td></tr>\n          <tr><td>복구 경로</td><td>rollback command, feature flag, traffic shift, restore drill, RTO/RPO</td><td>복구가 구두 지시가 아니라 실행 가능한 절차여야 한다.</td></tr>\n        </table>\n        <h3>운영 산출물 템플릿</h3>\n        <table>\n          <tr><th>Readiness packet</th><th>Incident packet</th></tr>\n          <tr><td><pre><code>service: checkout-api\ntier: customer-critical\nowner: payments-platform\noncall: payments-primary\ndependencies: auth-api, payment-provider, orders-db, redis-cache\nrequest_path: DNS -&gt; CDN/WAF -&gt; ALB -&gt; ECS service -&gt; RDS\nchange_path: GitHub Actions -&gt; ECR image digest -&gt; ECS deploy -&gt; metric gate\nslo: checkout_success 99.9%, p95_latency 600ms\ndashboards: sli, alb, app, db, egress, cost\nalerts: fast burn, slow burn, 5xx, db pool, provider timeout, NAT anomaly\nrollback: previous image digest + config version, verified 2026-06-20\nrestore: PITR drill pass 2026-06-21, RPO 5m, RTO 30m\ncost_owner: platform-finops\naccess_owner: security-platform\nlast_game_day: 2026-06-25\nlaunch_gate: blocked if owner, rollback, restore, alert route, or SLO is missing</code></pre></td><td><pre><code>incident: INC-2026-0715-checkout-fast-burn\nseverity: SEV1\nstarted_at: 2026-07-15T10:04+09:00\ncustomer_impact: checkout success down from 99.94% to 91.2%, KR/JP mobile users\ncurrent_sli: checkout_success burn_5m=14x, alb_5xx=3.8%, p95=2.4s\nrecent_changes:\n  - api image sha-9f31 deployed at T-8m\n  - feature flag payment_retry_v2 enabled at T-6m\nevidence:\n  - DNS/TLS ok\n  - WAF request id present, no block spike\n  - ALB target health unstable on new task set\n  - DB wait event baseline\ndecision:\n  - rollback api image to sha-prev\n  - keep feature flag off until replay test passes\nverification:\n  - burn_5m &lt; 1x for 20m\n  - missing orders reconciliation complete\n  - customer update sent\nrunbook_diff:\n  - add metric gate for checkout_success burn\n  - add target health canary stop rule</code></pre></td></tr>\n        </table>\n        <table>\n          <tr><th>확인 단계</th><th>명령·확인 위치</th><th>해석 기준</th></tr>\n          <tr><td>현재 상태</td><td>dashboard, alert, recent deploy, support ticket trend를 한 화면에서 확인</td><td>사용자 영향과 최근 변경을 먼저 고정한다.</td></tr>\n          <tr><td>운영 인수</td><td>서비스별 owner matrix와 runbook 링크를 대조</td><td>문서가 있어도 owner와 권한이 없으면 운영 준비가 아니다.</td></tr>\n          <tr><td>리허설</td><td>rollback drill, restore drill, traffic shift drill 기록 확인</td><td>마지막 성공 시간이 오래됐으면 실제 복구 가능성을 신뢰하지 않는다.</td></tr>\n        </table>\n        <table>\n          <tr><th>심화 장애 패턴</th><th>관찰 신호</th><th>판정 기준</th></tr>\n          <tr><td>SLO / Burn-rate evidence</td><td>fast burn, slow burn, error budget policy, release freeze</td><td>장애와 단순 오류를 사용자 영향 기준으로 구분한다.</td></tr>\n          <tr><td>DR DRILL / FAILBACK CHECKLIST</td><td>backup age, replica lag, traffic shift, data reconciliation, failback</td><td>백업 존재가 아니라 복구와 원복 성공이 증거다.</td></tr>\n          <tr><td>IAM/Network 증거</td><td>IAM diff, access review, flow log, exception ticket, expiry</td><td>정책 선언이 아니라 실제 접근 경로와 감사 로그로 증명한다.</td></tr>\n          <tr><td>Game Day 운영 훈련</td><td>배포 rollback, region degradation, alert fatigue, cost spike</td><td>GAME DAY REVIEW PACKET으로 runbook diff와 follow-up owner/date를 남긴다.</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch8\">\n        <div class=\"ch-head\"><span class=\"ch-code\">PRACTICE</span><h2>출력 판독 훈련</h2></div>\n        <p class=\"lede\">인프라·운영 로드맵은 개념을 아는 것보다 실제 출력에서 정상과 비정상을 구분하는 훈련이 중요하다. 아래 lab은 명령, 출력, 판단, 완화, 영구 수정을 한 번에 연결한다.</p>\n        <div class=\"practice-lab\">\n          <span class=\"sc-label\">PRACTICE LAB 01 · 운영 준비도 packet 판독</span>\n          <p>신규 서비스를 production에 올리기 전 운영 준비도 리뷰를 진행한다.</p>\n          <table>\n            <tr><th>Command / Query</th><th>목적</th></tr>\n            <tr><td><pre><code>readiness packet\nowner=team-payments\nslo=checkout_success 99.9\nrollback=artifact sha-prev\nrestore_drill=not_run\nalert_owner=empty</code></pre></td><td>런칭 전 차단해야 할 운영 공백을 찾는다.</td></tr>\n          </table>\n          <table>\n            <tr><th>정상 출력</th><th>비정상 출력</th><th>판단 훈련</th></tr>\n            <tr><td><pre><code>owner=team-payments\nslo=checkout_success 99.9\nrollback=validated\nrestore_drill=2026-06-20 pass\nalert_owner=oncall-payments</code></pre></td><td><pre><code>restore_drill=not_run\nalert_owner=empty\nrunbook=diagram-only\ncost_owner=unknown</code></pre></td><td>아키텍처는 있어도 복구 검증과 알림 owner가 없으면 운영 준비가 끝난 것이 아니다.</td></tr>\n          </table>\n          <table>\n            <tr><th>즉시 완화</th><th>영구 수정</th><th>검증 기준</th></tr>\n            <tr><td>런칭 gate를 보류하고 alert owner, rollback command, restore drill 일정을 확정한다.</td><td>운영 인수 체크리스트에 owner, SLO, rollback, restore drill, cost owner를 필수 항목으로 둔다.</td><td>readiness packet의 모든 owner와 마지막 drill 시간이 채워지고 smoke/rollback 검증이 통과한다.</td></tr>\n          </table>\n        </div>\n        <div class=\"practice-lab\">\n          <span class=\"sc-label\">PRACTICE LAB 02 · 배포 직후 5xx 증가 triage</span>\n          <p>새 artifact 배포 8분 뒤 checkout 5xx와 p95 latency가 동시에 증가한다.</p>\n          <table>\n            <tr><th>Command / Query</th><th>목적</th></tr>\n            <tr><td><pre><code>incident snapshot\nsli=checkout_success burn=14x\ndeploy_marker=api:sha-9f31 at T-8m\nalb_5xx=up\napp_error=up\ndb_pool_pending=flat\nrollback=available</code></pre></td><td>사용자 영향과 최근 변경을 같은 시간축에 두고 rollback 후보를 판단한다.</td></tr>\n          </table>\n          <table>\n            <tr><th>정상 출력</th><th>비정상 출력</th><th>판단 훈련</th></tr>\n            <tr><td><pre><code>burn=0.7x\ndeploy_marker=none\nalb_5xx=baseline\nrollback=not_needed</code></pre></td><td><pre><code>burn=14x\ndeploy_marker=api:sha-9f31\nalb_5xx=up\napp_error=up\nrollback=available</code></pre></td><td>최근 배포와 사용자 영향이 강하게 연결되어 원인 확정 전 rollback 또는 feature flag off를 검토해야 한다.</td></tr>\n          </table>\n          <table>\n            <tr><th>즉시 완화</th><th>영구 수정</th><th>검증 기준</th></tr>\n            <tr><td>canary stop, feature flag off, previous artifact rollback 중 폭발 반경이 가장 작은 조치를 실행한다.</td><td>metric gate에 checkout_success burn rate와 app 5xx를 추가하고 rollback decision log를 release runbook에 넣는다.</td><td>burn rate 1x 이하, 5xx baseline 복귀, rollback artifact와 config version 기록 완료.</td></tr>\n          </table>\n        </div>\n        <div class=\"practice-lab\">\n          <span class=\"sc-label\">PRACTICE LAB 03 · DNS/TLS edge 장애 판독</span>\n          <p>일부 지역 사용자만 접속 실패를 보고하고 앱 로그에는 요청이 없다.</p>\n          <table>\n            <tr><th>Command / Query</th><th>목적</th></tr>\n            <tr><td><pre><code>edge evidence\ndig_public=NXDOMAIN\ndig_authoritative=NOERROR ttl=60\nopenssl_verify=ok\nwaf_request_id=missing\napp_log=missing</code></pre></td><td>앱 장애와 edge 이전 장애를 분리한다.</td></tr>\n          </table>\n          <table>\n            <tr><th>정상 출력</th><th>비정상 출력</th><th>판단 훈련</th></tr>\n            <tr><td><pre><code>dig_public=NOERROR\ndig_authoritative=NOERROR\nwaf_request_id=present\napp_log=present</code></pre></td><td><pre><code>dig_public=NXDOMAIN\nwaf_request_id=missing\napp_log=missing\nresolver_region=ap-northeast</code></pre></td><td>요청이 edge/app까지 도달하지 않았으므로 DNS resolver cache, delegation, record 변경을 먼저 의심한다.</td></tr>\n          </table>\n          <table>\n            <tr><th>즉시 완화</th><th>영구 수정</th><th>검증 기준</th></tr>\n            <tr><td>이전 record 복구, TTL 전파 모니터링, status update, affected resolver 확인.</td><td>DNS 변경 티켓에 TTL 사전 조정, authoritative/public resolver 검증, rollback record를 필수화한다.</td><td>public resolver와 authoritative resolver 모두 NOERROR, WAF request id와 app access log 재등장.</td></tr>\n          </table>\n        </div>\n        <div class=\"practice-lab\">\n          <span class=\"sc-label\">PRACTICE LAB 04 · Private subnet egress 장애 판독</span>\n          <p>private subnet의 배치 작업이 외부 결제 API timeout을 반복하고 NAT 비용도 증가한다.</p>\n          <table>\n            <tr><th>Command / Query</th><th>목적</th></tr>\n            <tr><td><pre><code>egress evidence\nroute_0_0_0_0=nat-123\nflow_log=ACCEPT\nnat_bytes=4x\nretry_rate=9x\napi_timeout=up</code></pre></td><td>라우팅 성공, 정책 허용, retry storm, 비용 신호를 한 incident로 묶는다.</td></tr>\n          </table>\n          <table>\n            <tr><th>정상 출력</th><th>비정상 출력</th><th>판단 훈련</th></tr>\n            <tr><td><pre><code>nat_bytes=baseline\nretry_rate=baseline\napi_timeout=0\nflow_log=ACCEPT</code></pre></td><td><pre><code>nat_bytes=4x\nretry_rate=9x\napi_timeout=up\nprovider_429=up</code></pre></td><td>네트워크가 막힌 것이 아니라 외부 API 지연/429와 재시도가 NAT 비용과 timeout을 키우는 상황일 수 있다.</td></tr>\n          </table>\n          <table>\n            <tr><th>즉시 완화</th><th>영구 수정</th><th>검증 기준</th></tr>\n            <tr><td>retry backoff 강화, batch concurrency 축소, circuit breaker 또는 queue pause.</td><td>egress dependency map, retry budget, NAT cost anomaly alert, external API SLO를 추가한다.</td><td>retry rate baseline, NAT bytes 정상화, batch success 회복, provider 429 감소.</td></tr>\n          </table>\n        </div>\n        <div class=\"practice-lab\">\n          <span class=\"sc-label\">PRACTICE LAB 05 · SLO burn과 incident 역할 판독</span>\n          <p>결제 성공률 fast burn이 발생했는데 장애 채널에서 여러 명이 동시에 명령한다.</p>\n          <table>\n            <tr><th>Command / Query</th><th>목적</th></tr>\n            <tr><td><pre><code>incident channel\nburn_5m=18x\nIC=missing\nscribe=missing\ncommands=rollback,scale-out,db-restart\ncustomer_comms=missing</code></pre></td><td>기술 신호와 incident command 실패를 동시에 판정한다.</td></tr>\n          </table>\n          <table>\n            <tr><th>정상 출력</th><th>비정상 출력</th><th>판단 훈련</th></tr>\n            <tr><td><pre><code>burn_5m=0.8x\nIC=assigned\nscribe=assigned\ncommands=single_owner\ncustomer_comms=ready</code></pre></td><td><pre><code>burn_5m=18x\nIC=missing\nmultiple_commands=true\ncustomer_comms=missing</code></pre></td><td>fast burn 자체도 문제지만 역할과 의사결정 기록이 없어 완화 품질이 떨어지는 상태다.</td></tr>\n          </table>\n          <table>\n            <tr><th>즉시 완화</th><th>영구 수정</th><th>검증 기준</th></tr>\n            <tr><td>IC 지정, 명령권 단일화, scribe와 comms owner 지정, 완화 후보 하나를 선택한다.</td><td>incident role checklist, decision log template, fast burn paging runbook을 추가한다.</td><td>timeline, decision owner, customer update, recovery signal, runbook diff가 postmortem에 남음.</td></tr>\n          </table>\n        </div>\n        <div class=\"practice-lab\">\n          <span class=\"sc-label\">PRACTICE LAB 06 · Restore drill RPO/RTO 판독</span>\n          <p>DB 장애 대비 restore drill을 수행했지만 목표 시간을 초과했다.</p>\n          <table>\n            <tr><th>Command / Query</th><th>목적</th></tr>\n            <tr><td><pre><code>restore drill\nbackup_age=17m\nrestore_duration=42m\nreplica_lag=11m\nmissing_rows=128\nRPO=5m\nRTO=30m</code></pre></td><td>백업 존재와 복구 가능성을 구분하고 RPO/RTO 위반을 판정한다.</td></tr>\n          </table>\n          <table>\n            <tr><th>정상 출력</th><th>비정상 출력</th><th>판단 훈련</th></tr>\n            <tr><td><pre><code>backup_age=4m\nrestore_duration=18m\nreplica_lag=30s\nmissing_rows=0\nRPO=met\nRTO=met</code></pre></td><td><pre><code>backup_age=17m\nrestore_duration=42m\nreplica_lag=11m\nmissing_rows=128\nRPO=breach\nRTO=breach</code></pre></td><td>복구는 됐지만 허용 데이터 손실과 복구 시간 목표를 모두 넘었고 reconciliation이 필요하다.</td></tr>\n          </table>\n          <table>\n            <tr><th>즉시 완화</th><th>영구 수정</th><th>검증 기준</th></tr>\n            <tr><td>write freeze, 영향 고객 범위 산정, reconciliation job 실행, failback 계획 고정.</td><td>backup frequency, replica lag alert, restore automation, failback drill을 개선한다.</td><td>다음 drill에서 RPO/RTO met, missing rows 0, failback 절차 완료.</td></tr>\n          </table>\n        </div>\n      </section>\n\n      <section id=\"ch9\">\n        <div class=\"ch-head\"><span class=\"ch-code\">TERM</span><h2>용어사전</h2></div>\n        <p class=\"lede\">인프라·운영 로드맵에서 반복적으로 등장하는 운영 용어를 정의, 증거, 오해, 관련 항목 기준으로 정리한다. 장애 중에는 용어 자체보다 해당 용어가 어떤 로그, 지표, 설정, 책임 경계와 연결되는지를 확인한다.</p>\n        <table>\n          <tr><th>용어</th><th>정의</th><th>운영 증거</th><th>자주 하는 오해</th><th>관련 항목</th></tr>\n          <tr><td>request path</td><td>사용자 요청이 DNS에서 DB까지 통과하는 운영 경로.</td><td>DNS/CDN/WAF/LB/App/DB dependency map, traceId, app log</td><td>앱 로그가 없으면 앱 문제가 없다는 뜻이다.</td><td>DNS, CDN/WAF, Load Balancer, DB pool</td></tr>\n          <tr><td>change path</td><td>코드 변경이 CI/CD에서 runtime과 incident response까지 검증되는 흐름.</td><td>CI run, artifact digest, deploy marker, SLO dashboard</td><td>CI green이면 운영 성공이다.</td><td>artifact, environment, metric gate, rollback</td></tr>\n          <tr><td>SLO</td><td>사용자 경험 기준의 내부 신뢰성 목표.</td><td>SLO dashboard, error budget, alert owner</td><td>모든 오류를 같은 우선순위로 처리하면 된다.</td><td>SLI, error budget, paging</td></tr>\n          <tr><td>runbook</td><td>장애나 변경 시 반복 실행 가능한 절차 문서.</td><td>runbook owner, rollback command, restore drill date</td><td>문서 링크만 있으면 운영 인수 완료다.</td><td>owner, drill, incident packet</td></tr>\n          <tr><td>RPO/RTO</td><td>허용 가능한 데이터 손실과 복구 시간 목표.</td><td>DR plan, restore drill, replica lag, failover result</td><td>백업이 있으면 DR 준비가 끝난다.</td><td>backup, restore, DR</td></tr>\n        </table>\n      </section>\n\n      <section id=\"ch10\">\n        <div class=\"ch-head\"><span class=\"ch-code\">CASE</span><h2>장애/운영 시나리오</h2></div>\n        <p class=\"lede\">신규 서비스를 런칭하기 전 운영 준비도 리뷰를 진행한다. 요청 경로는 DNS, CDN/WAF, Load Balancer, Runtime, DB로 그리고, 변경 경로는 CI/CD, artifact, config, environment, metric gate로 확인한다. 복구 경로는 rollback, feature flag off, traffic shift, restore, failback으로 분리한다. 빠진 항목은 런칭 후 장애가 아니라 런칭 전 차단 이슈로 다룬다.</p>\n        <div class=\"callout\">\n          <span class=\"co-label\">Triage frame</span>\n          <p>인프라·운영 로드맵 장애는 증상 → 영향 범위 → 최근 변경 → 서비스 카탈로그 증거 → 완화책 → 복구 검증 → 재발 방지 순서로 기록한다. 이 흐름이 있어야 담당자가 바뀌어도 같은 결론에 도달한다.</p>\n        </div>\n        <table>\n          <tr><th>처음 10분</th><th>다음 30분</th><th>종료 조건</th></tr>\n          <tr><td>owner, tier, dependency map, SLO, cost owner, access owner 기준으로 영향 범위를 고정한다.</td><td>dashboard, alert, recent deploy, support ticket trend를 한 화면에서 확인 항목에서 증거를 모아 완화책을 실행한다.</td><td>회고가 시스템 변경으로 닫히지 않으면 반복된다.</td></tr>\n        </table>\n        <table>\n          <tr><th>Incident packet</th><th>기록할 내용</th><th>판정 기준</th></tr>\n          <tr><td>T+00</td><td>알림, SLI 값, 영향 서비스, 최근 배포를 기록</td><td>원인 추측 전에 사용자 영향과 변경 후보를 고정한다.</td></tr>\n          <tr><td>T+15</td><td>완화책과 rollback 후보를 비교</td><td>원인 확정 전이라도 영향 완화가 가능하면 실행한다.</td></tr>\n          <tr><td>T+60</td><td>postmortem owner/date와 runbook diff를 남김</td><td>회고가 시스템 변경으로 닫히지 않으면 반복된다.</td></tr>\n        </table>\n        <h3>강연/출판용 실전 케이스</h3>\n        <div class=\"practice-lab\">\n          <span class=\"sc-label\">Case 1 · 배포 후 checkout 5xx fast burn</span>\n          <p>결제 API 신규 artifact 배포 8분 뒤 checkout_success SLO가 14x fast burn을 보이고 ALB 5xx와 app error가 동시에 증가한다.</p>\n          <table>\n            <tr><th>시점</th><th>판단/행동</th><th>해석 기준</th></tr>\n          <tr><td>T+00</td><td>burn alert 수신, incident channel 생성, IC/scribe 지정</td><td>사용자 영향과 의사결정권을 먼저 고정한다.</td></tr>\n          <tr><td>T+05</td><td>deploy marker, image digest, config diff, DB migration version 확인</td><td>최근 변경이 사용자 영향과 같은 시간축인지 확인한다.</td></tr>\n          <tr><td>T+10</td><td>target health, app log, DB wait event, feature flag 상태 확인</td><td>DB 압박이 아니라 새 runtime 오류와 연결되면 rollback 후보가 강해진다.</td></tr>\n          <tr><td>T+15</td><td>canary stop 또는 previous artifact rollback</td><td>원인 확정 전이라도 사용자 영향을 줄인다.</td></tr>\n          <tr><td>T+45</td><td>postmortem에 metric gate, rollback decision log, runbook diff 추가</td><td>재발 방지가 운영 산출물로 닫혀야 한다.</td></tr>\n          </table>\n          <table>\n            <tr><th>대표 오판</th><th>종료 조건</th></tr>\n            <tr><td>앱 로그 stack trace만 보고 디버깅을 계속하면서 rollback 결정을 늦추는 것.</td><td>burn rate가 baseline으로 회복되고 rollback artifact, config version, 의사결정 시간이 incident packet에 남으면 종료한다.</td></tr>\n          </table>\n        </div>\n        <div class=\"practice-lab\">\n          <span class=\"sc-label\">Case 2 · Private subnet egress와 NAT 비용 급증</span>\n          <p>private subnet batch가 외부 결제사 API timeout을 반복하고 NAT bytes가 4배 증가한다.</p>\n          <table>\n            <tr><th>시점</th><th>판단/행동</th><th>해석 기준</th></tr>\n          <tr><td>T+00</td><td>route table, SG/NACL, DNS resolution, flow log를 확인</td><td>네트워크가 막힌 것인지, 통신은 되지만 retry storm인지 분리한다.</td></tr>\n          <tr><td>T+10</td><td>provider 429/timeout, app retry rate, batch concurrency 확인</td><td>flow log ACCEPT와 NAT bytes 증가는 차단이 아니라 과도한 재시도일 수 있다.</td></tr>\n          <tr><td>T+20</td><td>batch concurrency 축소, retry backoff, circuit breaker 적용</td><td>비용과 장애 영향을 동시에 줄이는 완화책을 선택한다.</td></tr>\n          <tr><td>T+60</td><td>egress dependency map, retry budget, NAT cost anomaly alert 추가</td><td>비용 신호를 운영 incident 신호로 편입한다.</td></tr>\n          </table>\n          <table>\n            <tr><th>대표 오판</th><th>종료 조건</th></tr>\n            <tr><td>flow log ACCEPT만 보고 네트워크는 정상이라고 끝내거나, NAT 비용을 FinOps 문제로만 분리하는 것.</td><td>retry rate, NAT bytes, batch success, 외부 API 오류율이 baseline으로 돌아오면 종료한다.</td></tr>\n          </table>\n        </div>\n        <div class=\"practice-lab\">\n          <span class=\"sc-label\">Case 3 · Restore drill RPO/RTO 실패</span>\n          <p>DB restore drill에서 backup_age 17분, restore_duration 42분, missing_rows 128건으로 RPO 5분/RTO 30분을 모두 위반한다.</p>\n          <table>\n            <tr><th>시점</th><th>판단/행동</th><th>해석 기준</th></tr>\n          <tr><td>T+00</td><td>backup age, replica lag, restore duration, reconciliation 범위 기록</td><td>복구 성공 여부보다 목표 위반을 먼저 판정한다.</td></tr>\n          <tr><td>T+20</td><td>write freeze, 영향 고객 산정, reconciliation job 실행</td><td>데이터 정합성 회복을 장애 완화와 분리해 관리한다.</td></tr>\n          <tr><td>T+60</td><td>backup frequency, replica lag alert, restore automation 개선</td><td>다음 drill에서 같은 목표로 재검증할 수 있어야 한다.</td></tr>\n          <tr><td>T+N</td><td>failback drill과 runbook diff 수행</td><td>복구 후 원래 경로로 돌아오는 절차까지 검증한다.</td></tr>\n          </table>\n          <table>\n            <tr><th>대표 오판</th><th>종료 조건</th></tr>\n            <tr><td>복구가 됐다는 사실만 보고 RPO/RTO 위반과 누락 데이터 reconciliation을 덮는 것.</td><td>다음 drill에서 RPO/RTO met, missing rows 0, failback 완료가 증거로 남아야 한다.</td></tr>\n          </table>\n        </div>\n      </section>\n\n      <section id=\"ch11\">\n        <div class=\"ch-head\"><span class=\"ch-code\">RISK</span><h2>자주 틀리는 판단</h2></div>\n        <p class=\"lede\">인프라·운영 로드맵에서 자주 틀리는 판단은 운영 전체 지도와 운영 산출물과 답변 구조의 책임 경계를 흐리는 데서 시작된다. 아래 신호가 보이면 설계, runbook, 답변 구조를 다시 점검한다.</p>\n        <ul>\n          <li>아키텍처 다이어그램은 있지만 알림 owner와 escalation 경로가 없다.</li>\n          <li>배포 절차는 있지만 artifact digest, metric gate, rollback 검증이 없다.</li>\n          <li>SLO가 없어서 장애와 단순 오류를 같은 우선순위로 처리한다.</li>\n          <li>운영 인수 문서가 사람 이름과 구두 설명에 의존한다.</li>\n          <li>백업은 있지만 restore drill과 failback 절차가 없다.</li>\n          <li>NAT/egress 비용 급증을 운영 incident가 아니라 비용 이슈로만 본다.</li>\n        </ul>\n      </section>\n\n      <section id=\"ch12\">\n        <div class=\"ch-head\"><span class=\"ch-code\">Q&A</span><h2>면접 답변 템플릿</h2></div>\n        <p class=\"lede\">인프라·운영은 요청 경로, 변경 경로, 복구 경로를 함께 보는 영역이라고 답합니다. 사용자의 요청은 DNS부터 DB까지 계층별로 흐르고, 코드 변경은 artifact와 환경 설정, 런타임, 관측, 장애 대응 절차를 거쳐 안전해집니다. 저는 각 단계의 실패 모드와 검증 증거를 연결하고, 원인 확정 전에도 사용자 영향 완화책을 선택하는 방식으로 운영 준비도를 판단하겠습니다.</p>\n        <div class=\"serial-card\">\n          <span class=\"sc-label\">OPERATIONS ANSWER FRAME</span>\n          1. 요청 경로와 변경 경로를 한 장으로 설명한다.<br>\n          2. 사용자 영향 SLI와 최근 변경을 같은 시간축에 둔다.<br>\n          3. rollback, DR, 비용, 권한 owner까지 운영 인수 산출물로 닫는다.<br>\n        </div>\n        <table>\n          <tr><th>답변 유형</th><th>예시</th></tr>\n          <tr><td>30초 답변</td><td>인프라·운영은 요청 경로, 변경 경로, 복구 경로를 한 장으로 연결해 보는 일입니다. DNS부터 DB까지 어디서 요청이 멈췄는지 증거로 좁히고, 배포 변경은 artifact와 metric gate로 추적하며, 장애 때는 원인 확정 전에도 rollback이나 feature off로 사용자 영향을 줄입니다.</td></tr>\n          <tr><td>90초 답변</td><td>저는 먼저 사용자 영향 SLI와 최근 변경을 같은 timeline에 둡니다. 그 다음 DNS/TLS, CDN/WAF, Load Balancer, runtime, DB, network policy 순서로 요청이 도달한 지점을 확인하고, 배포 관련이면 commit SHA, image digest, config diff, migration version을 봅니다. 완화는 rollback, traffic shift, feature flag off, restore 중 폭발 반경이 가장 작은 선택지를 고르고, 복구 후에는 incident packet, runbook diff, SLO/alert 개선으로 닫습니다.</td></tr>\n          <tr><td>나쁜 답변</td><td>AWS에서 ALB, ECS, RDS, CloudWatch를 써봤습니다. 장애가 나면 로그를 보고 서버를 재시작합니다.</td></tr>\n        </table>\n      </section>\n<footer>\nOPERATIONS HANDBOOK · 2026-06<br>\nPRINCIPLE : 운영 지식은 경로, 증거, 복구 절차로 검증한다.\n</footer>",
};

export default document;
