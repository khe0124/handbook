import type { HandbookDocumentContent } from "../types";

const document: HandbookDocumentContent = {
  navHtml: "<div class=\"nav-title\">프롬프팅</div>\n  <a href=\"#ch1\"><span class=\"code\">PRM-01</span>Prompt Contract</a>\n  <a href=\"#ch2\"><span class=\"code\">PRM-02</span>Structured Output</a>\n  <a href=\"#ch3\"><span class=\"code\">PRM-03</span>Repair Loop</a>\n  <a href=\"#ch4\"><span class=\"code\">PRM-04</span>Tool Use</a>\n  <a href=\"#ch5\"><span class=\"code\">PRM-05</span>Streaming</a>\n  <a href=\"#ch6\"><span class=\"code\">PRM-06</span>Economics</a>\n  <a href=\"#ch7\"><span class=\"code\">PRM-07</span>Output Sink</a>\n  <a href=\"#depth\"><span class=\"code\">DEPTH</span>계약 실전</a>",
  mainHtml: "<header class=\"hero\">\n  <div class=\"hero-serial\">\n    <span>DOC : LLM-PROMPTING-V2</span>\n    <span>SCOPE : STRUCTURED OUTPUT · TOOL USE · STREAMING · COST</span>\n    <span>LANG : KO</span>\n  </div>\n  <h1>프로덕션 프롬프팅과<br>구조화 출력</h1>\n  <p class=\"hero-sub\">프로덕션 프롬프팅은 말을 예쁘게 쓰는 기술이 아니라 입력, 정책, 출력, 검증, 재시도, 로그를 계약으로 만드는 엔지니어링입니다.</p>\n</header>\n\n<section id=\"ch1\">\n<div class=\"ch-head\"><span class=\"ch-code\">PRM-01</span><h2>PROMPT CONTRACT</h2></div>\n<p class=\"lede\">프롬프트는 문자열이 아니라 버전 관리되는 API contract입니다. 변경 시 품질, 비용, 지연, 보안 회귀가 생길 수 있으므로 코드처럼 소유자와 검증 기준이 있어야 합니다.</p>\n<table>\n<tr><th>필드</th><th>내용</th><th>검증</th></tr>\n<tr><td>version/owner</td><td>`doc-analysis@2026-06-30`, 담당 팀</td><td>로그와 릴리스 노트에 남김</td></tr>\n<tr><td>task boundary</td><td>문서 요약·추출만 수행, 외부 사실 추측 금지</td><td>scope 밖 질문 거절</td></tr>\n<tr><td>allowed inputs</td><td>사용자 질문, 문서 본문, metadata, retrieved chunks</td><td>누락·초과 필드 처리</td></tr>\n<tr><td>invariants</td><td>근거 없는 주장은 금지, citation 없는 핵심 주장 금지</td><td>groundedness 평가</td></tr>\n<tr><td>refusal shape</td><td>근거 없음, 권한 없음, 입력 부족을 구조화</td><td>no-answer 테스트</td></tr>\n<tr><td>observability</td><td>prompt_version, model, input_tokens, retry_count</td><td>trace와 cost dashboard</td></tr>\n</table>\n<div class=\"serial-card\">\n<span class=\"sc-label\">PRODUCTION PROMPT CONTRACT</span>\nversion · owner · role · task boundary · allowed inputs · forbidden behavior · output schema · examples · refusal shape · validation · logging fields\n</div>\n</section>\n\n<section id=\"ch2\">\n<div class=\"ch-head\"><span class=\"ch-code\">PRM-02</span><h2>STRUCTURED OUTPUT CONTRACT</h2></div>\n<p class=\"lede\">JSON을 “잘 써줘”라고 요청하는 것과 schema로 강제하는 것은 다릅니다. 제품 코드는 파싱 가능한 계약에 의존해야 하며, schema evolution 규칙도 필요합니다.</p>\n<pre><code>{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\"summary\", \"key_points\", \"risks\", \"confidence\", \"citations\"],\n  \"properties\": {\n    \"summary\": {\"type\": \"string\", \"maxLength\": 600},\n    \"key_points\": {\"type\": \"array\", \"items\": {\"type\": \"string\"}},\n    \"risks\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"object\",\n        \"required\": [\"level\", \"reason\", \"evidence\"],\n        \"properties\": {\n          \"level\": {\"type\": \"string\", \"enum\": [\"low\", \"medium\", \"high\"]},\n          \"reason\": {\"type\": \"string\"},\n          \"evidence\": {\"type\": [\"string\", \"null\"]}\n        }\n      }\n    },\n    \"confidence\": {\"type\": \"string\", \"enum\": [\"low\", \"medium\", \"high\"]},\n    \"citations\": {\"type\": \"array\", \"items\": {\"type\": \"string\"}}\n  }\n}</code></pre>\n<table>\n<tr><th>Schema 설계</th><th>좋은 기준</th><th>실패 예</th></tr>\n<tr><td>enum</td><td>UI 분기와 저장 값을 고정</td><td>`maybe`, `normal` 같은 임의 값</td></tr>\n<tr><td>nullable</td><td>모르는 값과 빈 문자열을 구분</td><td>근거가 없는데 `\"\"`로 저장</td></tr>\n<tr><td>additionalProperties</td><td>예상 밖 필드를 거부</td><td>모델이 만든 새 필드가 downstream을 오염</td></tr>\n<tr><td>schema evolution</td><td>새 필드는 optional로 추가 후 version bump</td><td>기존 클라이언트가 즉시 깨짐</td></tr>\n</table>\n</section>\n\n<section id=\"ch3\">\n<div class=\"ch-head\"><span class=\"ch-code\">PRM-03</span><h2>Validation And Repair Loop</h2></div>\n<p class=\"lede\">schema validation 실패는 예외 상황이 아니라 정상 운영 경로입니다. 실패할 때마다 같은 요청을 무한 재시도하면 비용만 증가합니다.</p>\n<table>\n<tr><th>검증 계층</th><th>검사</th><th>실패 처리</th></tr>\n<tr><td>Syntax</td><td>JSON parse 가능 여부</td><td>1회 repair prompt</td></tr>\n<tr><td>Schema</td><td>required, enum, type, maxLength</td><td>잘못된 필드만 수정 요청</td></tr>\n<tr><td>Semantic</td><td>citation이 실제 chunk를 가리키는가</td><td>해당 주장 제거 또는 no-answer</td></tr>\n<tr><td>Business rule</td><td>권한, 위험 등급, 저장 가능 여부</td><td>fail closed, human review</td></tr>\n</table>\n<div class=\"serial-card\">\n<span class=\"sc-label\">repair loop</span>\ninvalid output → classify failure → targeted repair once → validate again → fallback or fail closed → log schema_failure_rate\n</div>\n</section>\n\n<section id=\"ch4\">\n<div class=\"ch-head\"><span class=\"ch-code\">PRM-04</span><h2>TOOL USE BOUNDARY</h2></div>\n<p class=\"lede\">Function calling은 모델이 코드를 실행한다는 뜻이 아니라, 모델이 호출 의도를 구조화하고 애플리케이션이 검증 후 실행한다는 뜻입니다.</p>\n<table>\n<tr><th>위험</th><th>통제</th><th>로그</th></tr>\n<tr><td>잘못된 함수명</td><td>tool allowlist</td><td>blocked_tool_name</td></tr>\n<tr><td>잘못된 인자</td><td>typed schema validation</td><td>invalid_argument_path</td></tr>\n<tr><td>중복 실행</td><td>idempotency key</td><td>dedupe_result</td></tr>\n<tr><td>쓰기 작업</td><td>preview, human approval gate</td><td>approval_actor, approval_time</td></tr>\n<tr><td>도구 결과 조작</td><td>tool result is untrusted data</td><td>tool_result_hash</td></tr>\n</table>\n<div class=\"serial-card\">\n<span class=\"sc-label\">TOOL USE BOUNDARY</span>\nmodel proposes tool call → app validates schema and permission<br>\n→ tool executes with least privilege → result is marked untrusted<br>\n→ model summarizes with citation → audit log records actor/action/result\n</div>\n</section>\n\n<section id=\"ch5\">\n<div class=\"ch-head\"><span class=\"ch-code\">PRM-05</span><h2>streaming event contract</h2></div>\n<p class=\"lede\">스트리밍은 UX 개선이지만, 저장과 후속 처리는 검증된 최종 structured output만 사용해야 합니다. 부분 토큰을 곧바로 DB 상태로 믿으면 중간 실패가 제품 데이터가 됩니다.</p>\n<table>\n<tr><th>이벤트</th><th>의미</th><th>UI 처리</th></tr>\n<tr><td>message.started</td><td>요청 접수와 trace 생성</td><td>취소 버튼 활성화</td></tr>\n<tr><td>message.delta</td><td>부분 텍스트</td><td>임시 표시, 저장 금지</td></tr>\n<tr><td>tool.started</td><td>검색·도구 실행 시작</td><td>진행 상태 표시</td></tr>\n<tr><td>message.final</td><td>최종 응답 후보</td><td>검증 전 pending</td></tr>\n<tr><td>output.validated</td><td>schema와 정책 통과</td><td>저장·후속 처리 가능</td></tr>\n<tr><td>message.failed</td><td>timeout, cancel, validation fail</td><td>재시도 또는 fallback 안내</td></tr>\n</table>\n<div class=\"callout\">\n<span class=\"co-label\">운영 기준</span>\n<p>streaming endpoint는 cancellation, timeout, backpressure, client reconnect, partial/final 구분을 설계해야 합니다.</p>\n</div>\n</section>\n\n<section id=\"ch6\">\n<div class=\"ch-head\"><span class=\"ch-code\">PRM-06</span><h2>TOKEN COST LATENCY ECONOMICS</h2></div>\n<p class=\"lede\">컨설팅과 실무 리뷰에서 반드시 묻는 질문은 “한 요청에 얼마가 들고, p95는 얼마이며, 실패 재시도까지 포함했는가”입니다.</p>\n<table>\n<tr><th>요인</th><th>공식 또는 지표</th><th>최적화</th></tr>\n<tr><td>Input token</td><td>system + history + retrieval + tool result</td><td>retrieval, compression, prompt trimming</td></tr>\n<tr><td>Output token</td><td>answer + JSON + citation</td><td>schema field 제한, max output</td></tr>\n<tr><td>Retry amplification</td><td>base cost × (1 + retry rate)</td><td>targeted repair, retry budget</td></tr>\n<tr><td>Model routing</td><td>task별 quality/cost curve</td><td>작은 모델 우선, 어려운 요청 escalate</td></tr>\n<tr><td>Latency</td><td>p50/p95/p99, queue wait, provider time</td><td>cache, streaming, async job, fallback</td></tr>\n</table>\n<div class=\"serial-card\">\n<span class=\"sc-label\">Phase 1 Reference API</span>\nPOST /documents/analyze → validates input → builds prompt contract → calls model → validates structured output → logs model/token/latency/schema_retry_count\n</div>\n</section>\n<section id=\"ch7\">\n<div class=\"ch-head\"><span class=\"ch-code\">PRM-07</span><h2>OUTPUT SINK SECURITY</h2></div>\n<p class=\"lede\">모델 출력은 사용자에게 보이는 텍스트에서 끝나지 않습니다. HTML/Markdown, SQL, shell command, browser action, email, ticket, DB write 같은 sink로 흘러갈 때는 다시 비신뢰 데이터로 검증해야 합니다.</p>\n<table>\n<tr><th>sink</th><th>위험</th><th>통제</th></tr>\n<tr><td>HTML/Markdown</td><td>XSS, 악성 링크, 이미지 추적 pixel</td><td>sanitize, link allowlist, raw HTML 금지</td></tr>\n<tr><td>SQL</td><td>권한 밖 조회, destructive query, injection</td><td>read-only role, query parser, allowlisted template</td></tr>\n<tr><td>shell command</td><td>임의 명령 실행, 파일 삭제, secret 노출</td><td>승인 게이트, sandbox, command allowlist, dry-run</td></tr>\n<tr><td>browser action</td><td>폼 제출, 결제, 외부 전송</td><td>preview, user confirmation, idempotency</td></tr>\n<tr><td>email/ticket</td><td>개인정보 유출, 잘못된 수신자</td><td>recipient validation, PII scan, approval payload</td></tr>\n</table>\n<div class=\"serial-card\"><span class=\"sc-label\">OUTPUT SINK RULE</span>모델이 생성한 것은 파싱 성공 후에도 신뢰 입력이 아니다. sink 직전의 서버 코드가 정책, 권한, sanitizer, audit를 다시 적용한다.</div>\n</section>\n<section id=\"depth\">\n<div class=\"ch-head\"><span class=\"ch-code\">DEPTH</span><h2>프롬프트를 제품 계약으로 운영하기</h2></div>\n<h3>MARKER: concept-depth — Prompt contract as API contract</h3>\n<p>나쁜 프롬프트는 “이 문서를 요약하고 위험을 찾아줘”처럼 모델에게 의도를 맡깁니다. 프로덕션 프롬프트는 API contract처럼 동작해야 합니다. 어떤 입력을 신뢰할 수 있는지, 어떤 근거만 사용할 수 있는지, 어떤 경우에 no-answer로 멈춰야 하는지, 출력 schema가 무엇인지, 실패 시 repair를 몇 번 허용하는지까지 명시해야 합니다. 프롬프트가 바뀌면 API 동작도 바뀌므로 version, owner, eval dataset, rollback criteria가 필요합니다.</p>\n<p>이 관점에서 structured output은 예쁜 JSON을 받기 위한 장치가 아닙니다. downstream UI는 enum 값을 기준으로 배지를 그리고, DB는 nullable 필드와 빈 문자열을 다르게 저장하며, analytics는 confidence와 risk level을 집계합니다. 모델이 임의 필드를 추가하거나 citation 없는 risk를 만들면 화면과 데이터 품질이 동시에 무너집니다. schema는 모델 출력과 제품 코드 사이의 방화벽입니다.</p>\n<h3>MARKER: artifact-ready — Prompt contract YAML</h3>\n<pre><code>version: doc-analysis@2026-06-30\nowner: ai-platform\ntask: summarize_and_extract_risks\nallowed_inputs: [user_text, document_text, document_metadata, retrieved_chunks]\nforbidden_behavior: [external_fact_guessing, uncited_claims, hidden_policy_disclosure]\nevidence_policy: every_risk_requires_citation\nrefusal_shape: {reason: string, missing_evidence: string[], next_action: string}\noutput_schema_ref: document_analysis.v2.json\neval_dataset: golden-doc-analysis-v4\nrollback_criteria: groundedness_drop_gt_2pp or schema_failure_rate_gt_1pct\nlogs: [prompt_version, model_version, input_tokens, output_tokens, retry_count]</code></pre>\n<h3>MARKER: failure-driven — Repair loop worked example</h3>\n<p>repair loop는 “다시 JSON으로 답해”가 아닙니다. 먼저 validator가 실패 유형을 분류해야 합니다. 예를 들어 모델이 `confidence: \"normal\"`을 반환했다면 enum 위반이고, `citations: []`인데 high risk를 반환했다면 semantic validation 실패입니다. 첫 번째는 해당 필드만 고치도록 targeted repair를 보낼 수 있지만, 두 번째는 근거가 없으므로 risk를 제거하거나 no-answer로 전환해야 합니다. 두 실패를 같은 재시도로 처리하면 비용은 늘고 품질은 개선되지 않습니다.</p>\n<pre><code>validator_error: {path: \"/risks/0/evidence\", error: \"required citation missing\"}\nrepair_prompt: \"Do not add new claims. Either attach an existing citation from chunks [c12,c18] or remove the unsupported risk.\"\nsecond_failure_policy: fail_closed_and_send_to_review_queue</code></pre>\n<h3>MARKER: artifact-ready — Tool approval and audit payload</h3>\n<pre><code>tool_proposal: {tool: \"send_email\", args: {to: \"client@example.com\", template: \"renewal\"}}\napproval_payload: {risk_tier: \"write-external\", preview_required: true, rollback: \"not_possible\"}\naudit_log: {actor_id: \"usr_7\", tool: \"send_email\", args_hash: \"sha256:...\", approval_actor: \"usr_7\", result_hash: \"sha256:...\"}</code></pre>\n<p>Streaming 역시 상태 머신으로 봐야 합니다. `message.delta`는 사용자에게 보여주는 임시 상태일 뿐이고, DB 저장이나 후속 workflow는 `output.validated` 이후에만 시작해야 합니다. 사용자가 중간에 취소하거나 네트워크가 끊긴 뒤 재연결하면 같은 tool call이 두 번 실행될 수 있으므로 idempotency key가 필요합니다. 스트리밍은 UX 최적화지만, 검증되지 않은 부분 출력을 제품 상태로 승격하면 데이터 정합성 버그가 됩니다.</p>\n<h3>MARKER: artifact-ready — Cost and latency worksheet</h3>\n<pre><code>monthly_requests: 100000\navg_input_tokens: 3500\navg_output_tokens: 700\nretry_rate: 0.08\neffective_cost = base_model_cost * 1.08\np95_budget_3000ms = retrieval_450ms + model_2100ms + validation_150ms + logging_100ms + margin_200ms</code></pre>\n<h3>MARKER: failure-driven — JSON만 강제하고 의미 검증을 하지 않는 실패</h3>\n<p>구조화 출력은 “파싱 가능한 JSON”을 받는 것으로 끝나지 않습니다. 예를 들어 risk level은 enum에 맞고 citations 배열도 존재하지만, citation이 실제 근거 문장과 연결되지 않거나 confidence가 높은데 missing evidence가 있는 경우가 더 위험합니다. 이 실패는 API 레벨에서는 성공처럼 보이기 때문에 운영에서 늦게 발견됩니다. 따라서 prompt contract는 형식 검증, 의미 검증, repair policy를 함께 포함해야 합니다. schema validation은 첫 번째 관문이고, field 간 일관성, citation coverage, unsupported claim 여부, tool approval 상태까지 검증해야 프로덕션 프롬프팅이라고 부를 수 있습니다.</p>\n</section>",
};

export default document;
